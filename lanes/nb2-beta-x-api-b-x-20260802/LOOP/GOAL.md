# GOAL — nb2-beta-x-api-b-x (IMMUTABLE — re-read at the top of EVERY arc)

## Mission

Solo platform: **Cursor**. Land Gate 0 (design Arc 0 PR + merge #172/#173) then
engine Arc 1: per-trait NB2/Beta φ + shared site-X fitters, bridge+formula routed,
Julia identity tests green. Stop before Arc 2 RCall light cells.

## Headline

Twin API B under X: stop routing NB2/Beta+X through shared-φ `fit_gllvm_cov`.

## Invariants

- One write lane after Gate 0: fresh worktree
  `.worktrees/gllvmjl-nb2-beta-x-grouped-cov-20260802` on
  `fix/nb2-beta-x-grouped-cov-20260802` from post-merge `origin/main`.
- **FORBIDDEN:** Arc 2 RCall NB2+X/Beta+X cells; Gamma+X flip; Ordinal+X; X_lv;
  ADEMP; coverage; Totoro/DRAC grids; Phylo Model A; silent tol widen;
  `git add -A`; claiming full family parity; implying Julia hosts R.
- Identity tests use `hessian=:fisher` vs `fit_gllvm_cov`; production/bridge
  default stays `hessian=:observed`.
- Bridge execution R→Julia only (JuliaCall); RCall = opt-in oracle.
- Verify by reading logLik numbers / test tallies, not exit codes alone.

## Authoritative WHAT

→ `LOOP/ultra-plan.md` (approved plan copy). Detail wins there; this file wins
on “what must never be lost.”

## Definition of done

1. Design Arc 0 merged (or equivalent on main).
2. #172 and #173 merged (Documenter deploy flake may be non-blocking if Julia CI green).
3. `fit_nb_gllvm_grouped_cov` / `fit_beta_gllvm_grouped_cov` land with identity tests green.
4. Bridge + formula route NB/Beta+X to grouped_cov; `fit_gllvm_cov` remains shared opt-in.
5. check-log + after-task + Rose fence; engine PR open.
6. Arc 2 not started.
