# GOAL — species-xb-light-20260804 (IMMUTABLE — re-read every arc)

## Mission

One light gllvmTMB species-XB logLik cell (Poisson) green at rtol `1e-6`
via `fit_gllvm_speciescov` and R formula `(0 + trait):x`. No B-engine rebuild.

## Headline

Parity helper for per-trait slopes + one `@testset`.

## Invariants

- Write lane: `.worktrees/gllvmjl-species-xb-arc0-20260804` on
  `parity/species-xb-light-20260804` from `origin/main` @ `a92c5040`.
- rtol `1e-6` fixed; DGP/formula repair over tolerance widen.
- FENCES: no B-engine rebuild; no ADEMP; no X_lv; no “full family parity”;
  no Dropbox protected writes; no `git add -A`; no push without ask.
- Verify = printed Δ logLik, not exit code alone.
- Compute = laptop RCall.

## Authoritative WHAT

`LOOP/ultra-plan.md` (= arc card
`docs/dev-log/plans/2026-08-04-species-xb-light-rcall-arc-card.md`).

## Definition of done

1. Helper for `(0 + trait):x` exists and is used by the cell.
2. Poisson species-XB `@testset` Pass at rtol 1e-6 with pasted Δ (or OWED).
3. #181 board/README drift closed; after-task + check-log; Rose fence.
4. Commits by path; **no push** until Shinichi asks.
