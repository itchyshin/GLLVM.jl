# GOAL — gllvmjl-capability-catchup-20260815 (IMMUTABLE — re-read at the top of EVERY arc)

Read this first, every cycle. Auto-compact eats messages, not this file. Unsure after a compaction?
Re-read THIS, then checkpoint.md, then continue.

## Mission

Solo platform: Cursor (this session). After G0, execute via /goal in a fresh Cursor chat — do not grow this planning chat into Phase 3. Do not invent Codex as executor.

Deliverable: A day-scale capability catch-up programme that moves measurable chips on docs/design/capability-status.md (MC julia-surface) with light twin-admitted evidence where possible, after PR #204 lands.

## Headline

Post-#204 hygiene (bare `implemented` tokens for zip/zinb chips) + promote or honestly fence code-already-present student/REML rows + land truncated_poisson Identity→Engine (Ada default track).

## Invariants

- One lane: catch-up / post-#204. Workspace =
  `.worktrees/gllvmjl-capability-catchup-20260815` on
  `cursor/capability-catchup-20260815` from post-merge `origin/main`.
- NEVER write Dropbox `claude/jl-bridge-capabilities-20260619`.
- Do **not** race-merge #204; poll/await only. Engine work only on post-merge main tip.
- Locked Ada defaults (G0): (1) truncated_poisson Identity→Engine (nbinom2 contingent);
  (2) local compute; (3) light Δ only when twin admits.
- IN PARALLEL (cheap): twin truncated/com_poisson/REML recon cites; board START HERE refresh;
  MC julia_surface next_safe PROPOSE text only (do not overwrite R MSPL primary).
- DEFER / FENCE: invent twin light Δ for cut ZIP/ZINB; Phylo Model A public intervals;
  ADEMP/coverage unless Totoro/DRAC sized+asked; silent rtol widen; hurdle/Tweedie+X /
  none×dep / slopes / AGHQ / multinomial as this programme's primary engine; `git add -A`;
  push without ask.
- DISCIPLINE: verify = focused suites + ledger token parse + Rose claim fence;
  compute = local tiny + light RCall unless a rung explicitly asks Totoro/DRAC;
  closure = after-task + check-log + board + Melissa reconcile + STOP.
- Stage by name; never `git add -A`. Pause at OPEN GATES (merge/push without ask; public claim).

## Authoritative WHAT

`lanes/gllvmjl-capability-catchup-20260815/LOOP/ultra-plan.md`
(durable copy: `docs/dev-log/plans/2026-08-15-gllvm-jl-gllvmtmb-capability-catchup.md`)

## Definition of done

≥1 material ledger/chip advance beyond hygiene **or** capacity exhausted on completed value
rungs with honest OWED receipts; after-task + check-log + board + Melissa plan-actual + Rose;
programme STOP. Checkpoint RESUME says DONE (or next gate).
