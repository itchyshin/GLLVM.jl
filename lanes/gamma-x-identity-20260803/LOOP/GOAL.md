# GOAL — gamma-x-identity-20260803 (IMMUTABLE — re-read at the top of EVERY arc)

## Mission

Deliver an **ACCEPTED** (or explicitly **REJECTED-with-fence**) Gamma+X
dispersion-identity decision note under `docs/dev-log/decisions/`, mirroring
`docs/dev-log/decisions/2026-08-02-nb2-beta-x-dispersion-identity.md`.

Docs-only. **NO** engine. **NO** Gamma+X light RCall cells in this goal.

## Headline

Lock the twin default for Gamma under shared site-X (**per-trait α + shared γ**
vs retain shared/bridge Option B) **before** any `fit_gamma_*_cov` work.

## Invariants

- One lane; write only in worktree
  `.worktrees/gllvmjl-gamma-x-identity-20260803` on branch
  `docs/gamma-x-identity-20260803`.
- Dropbox checkout `claude/jl-bridge-capabilities-20260619` = **PROTECTED**.
- Never `git add -A`; no push without explicit ask.
- Fences: Ordinal+X; `X_lv`; ADEMP/coverage; Phylo Model A; “full family
  parity”; merging #177 inside this goal unless already green/MERGEABLE with
  zero conflict (landing gate only — currently CONFLICTING → out of scope).
- Compute = laptop (no Totoro/DRAC).

## G0 answers (locked 2026-08-03 — Ada judgment / Shinichi defaults)

| Q | Answer |
|---|---|
| Q1 Gamma+X twin default | **per-trait α + shared γ** (API B under X) |
| Q2 no-X Option B | **named follow-up** — do not silent-flip bridge in Arc 0 |
| Q3 worktree | cut from **current `origin/main`** — do not block forever on #177 |

## Authoritative WHAT

See `LOOP/ultra-plan.md` (copy of
`docs/dev-log/plans/2026-08-03-gamma-x-identity-ultra-plan.md`).

## Definition of done

1. Decision note exists at
   `docs/dev-log/decisions/2026-08-03-gamma-x-dispersion-identity.md`.
2. Cites twin `gllvmTMB` **file:line** on fresh `origin/main`.
3. Rose fence paragraph present; no `src/` edits in the lane diff.
4. `docs/dev-log/check-log.md` + after-task + board pointer + Arc Card Actuals.
5. Local commit(s); STOP — engine Arc 1 only after acceptance.
