# GOAL — ordinal-x-engine-20260803 (IMMUTABLE — re-read at the top of EVERY arc)

## Mission

Ship **Ordinal+X engine Arc 1**: public / bridge / `@formula` default under
shared site-X = per-trait cutpoints (τ₁=0 / K−2 free log-spacings) + shared γ,
via `fit_ordinal_gllvm_pertrait_cov`, with Julia identity tests. Decision
`docs/dev-log/decisions/2026-08-03-ordinal-x-cutpoint-identity.md` (ACCEPTED;
merged #179 @ `0630f8e4`).

## Headline

Mirror Gamma/NB2 API B under X for the ordinal cutpoint estimand — **no**
light RCall Arc 2 in this GOAL.

## Invariants

- Worktree: `.worktrees/gllvmjl-ordinal-x-engine-20260803` from `origin/main`
  (post-#179). Branch: `fix/ordinal-x-pertrait-cov-20260803`.
- **NO** Ordinal+X light RCall; **NO** ADEMP/coverage; **NO** Phylo Model A.
- **NO** silent shared-cutpoint public X default; **NO** tolerance widen.
- **NO** Dropbox protected checkout writes; **NO** `git add -A`;
  **NO** push without ask.
- Compute = laptop for identity/unit tests.
- Verify = focused identity + bridge_x + capabilities + formula smoke; do not
  claim full `Pkg.test` unless run.

## Authoritative WHAT

→ `LOOP/ultra-plan.md` (approved engine shape from identity decision).

## Definition of done

1. `fit_ordinal_gllvm_pertrait_cov` + `OrdinalPerTraitCovFit` exported.
2. Bridge + `@formula` route ordinal/ordinal_probit + X through that path.
3. Identity tests green (zero-offset / absorbed-offset / zero-X fit).
4. After-task + check-log + board/AGENTS pointer; LOOP checkpoint overwritten.
5. Commits staged by explicit path; **STOP** before RCall; ask before push/PR.
