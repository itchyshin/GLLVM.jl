# GOAL — ordinal-x-identity-20260803 (IMMUTABLE — re-read at the top of EVERY arc)

## Mission

Deliverable = **ACCEPTED** (or explicitly rejected-with-fence) Ordinal+X
cutpoint/identity decision note at
`docs/dev-log/decisions/2026-08-03-ordinal-x-cutpoint-identity.md`, mirroring
`docs/dev-log/decisions/2026-08-03-gamma-x-dispersion-identity.md` — **docs-only**.

## Headline

Lock the twin default for Ordinal under shared site-X (per-trait cutpoints /
τ₁ convention / covariate path identity) **before** any `fit_ordinal_*_cov`
work.

## Invariants

- One lane: `docs/ordinal-x-identity-20260803` worktree from `origin/main`.
- **NO** `src/` engine; **NO** Ordinal+X light RCall; **NO** ADEMP.
- **NO** Gamma push/PR; **NO** force-merge #177 (landings OUTSIDE this GOAL).
- **NO** dual-PR of `fix/gamma-x-grouped-cov` Arc2 tip; **NO** Dropbox
  protected checkout writes; **NO** `git add -A`; **NO** push without ask.
- **NO** “full family parity”; **NO** silent no-X Option B flip.
- Compute = laptop (docs-only); no Totoro/DRAC.
- Verify = decision cites twin file:line + Rose fence + no `src/`.

## Authoritative WHAT

→ `LOOP/ultra-plan.md` (frozen copy of approved Ultra Plan).

G0 answers (Shinichi): Landing first = **WAIT**; Ordinal default under X =
**YES** (per-trait cutpoints + shared γ, unless twin evidence forces otherwise);
Base tip = **parallel docs from origin/main**.

## Definition of done

1. Decision note exists with ACCEPTED or rejected-with-fence status.
2. Twin `ordinal_probit` τ₁=0 + K−2 free + formula/X surface cited file:line.
3. Julia `fit_ordinal_*` + bridge “no covariate kernel” + shared vs per-trait
   cited file:line.
4. Rose fence present; `git diff` shows no `src/`.
5. After-task + check-log + board pointer; LOOP checkpoint overwritten.
6. Commits staged by explicit path; **STOP** — no Ordinal+X engine.
