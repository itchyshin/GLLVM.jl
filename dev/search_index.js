var documenterSearchIndex = {"docs":
[{"category":"section","location":"benchmarks/#Benchmarks","page":"Benchmarks","text":"The benchmark grid from the parallel gllvmTMB-julia-bench/ study compares R gllvmTMB and GLLVM.jl on the same simulated data per replicate. Both engines minimise the same Gaussian marginal log-likelihood with f_reltol = 1e-10 and g_tol = 1e-6. The R engine uses gllvmTMB::gllvmTMB (TMB Laplace + L-BFGS); the Julia engine uses GLLVM.fit_gaussian_gllvm (closed-form Gaussian marginal + Optim L-BFGS with ForwardDiff gradients).\n\nSix cells × three replicates × two engines = 36 fits. All 36 converged.","title":"Benchmarks"},{"category":"section","location":"benchmarks/#Wall-clock-(median-over-reps)","page":"Benchmarks","text":"cell_id n_sites n_species d median t_R (s) median t_Julia (s) median R / Julia\nc01smallnoX 20 5 1 0.0230 0.0001 194.9\nc02smallX 20 5 1 0.0250 0.0001 185.3\nc03mednoX 80 10 2 0.1020 0.0002 698.1\nc04medX 80 10 2 0.1120 0.0003 335.3\nc05largenoX 200 20 2 0.5140 0.0013 398.8\nc06largeX 200 20 2 0.6180 0.0037 161.2\n\nJulia is the faster engine in every cell after a per-signature warm-up. The relative speedup is largest in the smallest cells (where the R-side TMB Laplace setup dominates a small inner loop) and the absolute time saved per fit is largest in the larger cells — the metric that matters for a multi-thousand-fit simulation study.","title":"Wall-clock (median over reps)"},{"category":"section","location":"benchmarks/#Agreement-(worst-case-across-the-grid)","page":"Benchmarks","text":"cell_id max |Δ logLik| max Σ_y rel-Frobenius\nc01smallnoX 4.965e-10 5.769e-06\nc02smallX 4.443e-10 5.519e-06\nc03mednoX 3.683e-08 1.804e-05\nc04medX 3.839e-08 2.810e-05\nc05largenoX 1.923e-07 3.742e-05\nc06largeX 2.343e-07 4.424e-05\n\nWorst-case |Δ logLik| over the full grid is 2.343e-07 (gate < 1e-4: PASS). Worst-case relative Frobenius on Σ_y is 4.424e-05 (gate < 1e-3: PASS). The two engines agree to at least six significant digits on every fit.\n\nFull per-rep details, the wall-clock log-log plot, and the verification gate summary live in the source benchmark repo at gllvmTMB-julia-bench/report/grid-bench.md.","title":"Agreement (worst-case across the grid)"},{"category":"section","location":"benchmarks/#O(p)-phylogenetic-gradient-scaling","page":"Benchmarks","text":"The headline of the node-frame analytic gradient (grad_node_perspecies) is that a single exact gradient evaluation scales linearly in the number of species p on a tree — where R gllvmTMB caps near p ≈ 500. Timed on a balanced tree (one evaluation, median over BenchmarkTools samples; reproduce with julia --project=bench bench/node_gradient_bench.jl):\n\np state build (ms) gradient (ms) gradient / p (µs)\n100 0.088 0.0094 0.094\n500 0.287 0.041 0.081\n1 000 0.583 0.077 0.077\n5 000 2.935 0.393 0.079\n10 000 5.968 0.771 0.077\n\nThe per-tip time (last column) is essentially flat, and the log–log scaling slope is 0.96 for the gradient and 0.92 for the state build — both ≈ 1, confirming O(p). A single exact gradient at p = 10 000 takes 0.77 ms. (The older sparse_phy_grad path is O(p²), slope ≈ 2; it is retained as an independent-complexity cross-check, not the production path.)","title":"O(p) phylogenetic gradient scaling"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"The full public API surface, picked up automatically from docstrings on exported functions, types, and modules. As docstrings are added to the public functions during the integration pass this page populates without further edits.","title":"API reference"},{"category":"method","location":"api/#GLLVM.augmented_phy-Tuple{AbstractString}","page":"API reference","text":"augmented_phy(newick::AbstractString) :: AugmentedPhy{Float64}\n\nParse a minimal Newick string and return the augmented-state sparse precision representation.\n\nRestrictions\n\nBinary (bifurcating) trees only — every internal node has exactly two children. Multifurcations and unary nodes are rejected.\nLeaf names follow [A-Za-z0-9_.\\-]+. Internal node labels are tolerated but discarded.\nBranch lengths must all be > 0 (otherwise 1/b blows up).\nThe root has no parent branch; the optional root length in (…):0.0; is read but does not enter Q.\n\nExample\n\nphy = augmented_phy(\"((A:0.1,B:0.2):0.3,C:0.5);\")\nphy.n_leaves      # 3\nphy.n_total       # 5  (3 leaves + 2 internal)\nlength(phy.branch_lengths)   # 4\nnnz(phy.Q_topology)          # 16  (4 per edge × 4 edges)\n\n\n\n\n\n","title":"GLLVM.augmented_phy"},{"category":"method","location":"api/#GLLVM.bootstrap_ci-Tuple{GllvmFit}","page":"API reference","text":"bootstrap_ci(fit::GllvmFit;\n             n_boot = 100,\n             level = 0.95,\n             seed = 0,\n             y = nothing,\n             n_sites = nothing,\n             X = nothing,\n             Σ_phy = nothing,\n             parms = nothing,\n             verbose = false)\n    -> NamedTuple\n\nParametric bootstrap CIs for the fitted parameters in fit. Returns a NamedTuple with fields:\n\nterm::Vector{String}         — parameter names (θ_packed order)\nestimate::Vector{Float64}    — original MLE (fit.pars.θ_packed)\nlower::Vector{Float64}       — percentile 100·(1-level)/2\nupper::Vector{Float64}       — percentile 100·(1+level)/2\nn_converged::Int             — number of bootstrap fits that converged\nreplicates::Matrix{Float64}  — n_boot × n_params matrix of bootstrap θ̂_b\n\nn_sites is required because GllvmFit does not record it. Supply either n_sites directly, or pass the original y (or X) — the function infers n_sites from size(y, 2) or size(X, 2).\n\nX and Σ_phy must be supplied when the original fit had fixed effects (q > 0) or a phylogenetic block (K_phy > 0 or has_phy_unique). Otherwise the bootstrap model spec would not match the fitted spec.\n\nparms selects a subset of returned terms (default nothing = all). Accepts a String (single term name) or Vector{String}.\n\nn_boot defaults to 100; publication-grade is 500–2000.\n\nAlgorithm\n\nReconstruct Σ̂y at the fitted parameters (`Λ̂ Λ̂' + diag(dtotal)`, plus the phy block when present).\nFor b = 1..n_boot:\nSimulate yb ~ N(μ̂, Σ̂y) using a Cholesky factor of Σ̂ysite (independent across sites for J1 / J2; J3 adds species-shared phylogenetic contributions).\nRefit via fit_gaussian_gllvm(y_b; K, K_W, has_diag, K_phy, has_phy_unique, Σ_phy, X) so the bootstrap model matches the original spec.\nRecord the resulting θ_packed and convergence flag.\nCompute percentile CIs over the non-NaN replicates per parameter.\n\nReplicates whose refit errors out are recorded as NaN (and excluded from the percentile calculation); a parameter with fewer than 10 converged replicates returns NaN bounds.\n\nExample\n\nfit = fit_gaussian_gllvm(y; K = 1)\nci  = bootstrap_ci(fit; y = y, n_boot = 200, seed = 42)\nci.term      # parameter names\nci.lower     # 2.5% percentile bounds\nci.upper     # 97.5% percentile bounds\n\n\n\n\n\n","title":"GLLVM.bootstrap_ci"},{"category":"method","location":"api/#GLLVM.bootstrap_ci_derived-Tuple{GllvmFit, Function}","page":"API reference","text":"bootstrap_ci_derived(fit::GllvmFit, derived_fn::Function;\n                     n_boot = 500, seed = 0, level = 0.95,\n                     y = nothing, n_sites = nothing,\n                     X = nothing, Σ_phy = nothing,\n                     verbose = false)\n    -> NamedTuple\n\nParametric bootstrap percentile CI for a scalar-valued derived quantity of a fitted Gaussian GLLVM. Wraps the parametric bootstrap in src/confintbootstrap.jl: simulate yb ~ N(μ̂, Σ̂y) for b = 1..nboot, refit, evaluate derived_fn on each replicate, return percentile CIs.\n\nderived_fn is called as derived_fn(fit_b) first and, if that errors with a MethodError, as derived_fn(fit_b.pars.θ_packed). Either form is fine — pick the more convenient.\n\nReturns a NamedTuple with fields:\n\nestimate::Float64      — the derived quantity at the original MLE\nlower::Float64         — percentile 100·(1-level)/2\nupper::Float64         — percentile 100·(1+level)/2\nn_converged::Int       — number of bootstrap fits that converged\nn_valid::Int           — number of replicates with a finite derived value\nreplicates::Vector{Float64} — the n_boot derived-quantity samples                                 (with NaN for failed refits)\n\nPass y, X, Σ_phy matching what was originally passed to fit_gaussian_gllvm (the bootstrap needs them to simulate and refit).\n\nn_boot defaults to 500 — publication-grade. Lower (e.g. 100) for quick checks. The cost is n_boot × per-fit time; PERF+I already optimised the per-fit path.\n\n\n\n\n\n","title":"GLLVM.bootstrap_ci_derived"},{"category":"method","location":"api/#GLLVM.build_node_perspecies-Tuple{GLLVM.AugmentedPhy{Float64}, AbstractVector, Real}","page":"API reference","text":"build_node_perspecies(phy::AugmentedPhy{Float64}, σ_phy, σ²_eps) -> NodePerSpecies\n\nAssemble a NodePerSpecies solver for the matched single-trait per-species model. σ_phy is the length-p per-tip phylogenetic SD (with σ²_phy folded in); σ²_eps is the residual variance. Builds the root-dropped tree precision Q_cond, the augmented node precision Λ̃ = Q_cond + σ_eps⁻² S' diag(σ_phy²) S, and their sparse Cholesky factors (O(p) on a tree).\n\n\n\n\n\n","title":"GLLVM.build_node_perspecies"},{"category":"method","location":"api/#GLLVM.build_sparse_phy_state-Tuple{AbstractMatrix, AbstractMatrix, Real}","page":"API reference","text":"build_sparse_phy_state(y, Λ_B, σ_eps; Λ_phy, σ_phy, phy, σ²_phy)\n\nAssemble the augmented-state sparse machinery for the STANDARD phylogenetic GLLVM (no W tier, no per-trait diagonal REs, scalar σ²_phy). Mirrors the construction in likelihood_sparse_phy.jl and is shared between the value (sparse_phy_value) and the analytic gradient (sparse_phy_grad).\n\n\n\n\n\n","title":"GLLVM.build_sparse_phy_state"},{"category":"method","location":"api/#GLLVM.communality-Tuple{GllvmFit}","page":"API reference","text":"communality(fit::GllvmFit) -> Vector\n\nPer-trait communality c²[t] = (Λ_B Λ_B')[t, t] / Σ_y_site[t, t]. This is the fraction of the per-site trait variance explained by the shared latent factors. Values are in [0, 1].\n\n\n\n\n\n","title":"GLLVM.communality"},{"category":"method","location":"api/#GLLVM.confint-Tuple{GllvmFit}","page":"API reference","text":"confint(fit::GllvmFit; level=0.95, parm=nothing,\n        y=nothing, X=nothing, Σ_phy=nothing) -> NamedTuple\n\nWald confidence intervals for the parameters of a fitted Gaussian GLLVM. Returns a NamedTuple with fields:\n\nterm::Vector{String}     — parameter names\nestimate::Vector{Float64} — point estimates (raw scale for SDs)\nlower::Vector{Float64}    — lower CI bound at level\nupper::Vector{Float64}    — upper CI bound at level\nse::Vector{Float64}       — standard errors (working scale)\npd_hessian::Bool          — whether the observed information matrix                               was positive definite at the MLE\n\nlevel is the nominal coverage (default 0.95 → two-sided 95% CI).\n\nparm selects a subset of parameters by name. Acceptable forms:\n\nnothing (default) — all parameters\n\"sigma_eps\" — single name\n\"Lambda\" — all Λ entries across all tiers (B, W, phy)\n\"Lambda:1,1\" — shorthand for \"Lambda_B[1,1]\"\n[\"sigma_eps\", \"Lambda:1,1\"] — mixed list\n\nWorking-scale convention: σeps, σB, σW are parameterised on the log scale internally. The CI bounds returned for those entries are on the raw (positive) scale via `exp(logθ ± z * SElog)`. σphy uses an identity (signed) link — its Wald CI is the plain θ̂ ± z * SE. β and Λ entries are reported on their native (linear) scale.\n\nThe Hessian is computed via ForwardDiff at the fitted parameter vector stored on fit.pars.θ_packed. The function needs the original data matrix y (and optionally X, Σ_phy) to reconstruct the NLL closure. If the Hessian is not positive definite, this function returns NaN bounds for the affected entries with pd_hessian = false (matching the R glmmTMB / gllvmTMB convention).\n\nWhen PERF lands with reverse-mode AD, the integration agent can swap the ForwardDiff.hessian call for the faster path; the public API stays stable.\n\n\n\n\n\n","title":"GLLVM.confint"},{"category":"method","location":"api/#GLLVM.correlation-Tuple{GllvmFit}","page":"API reference","text":"correlation(fit::GllvmFit) -> Matrix\n\nCross-trait correlation derived from Σ_y_site: ρ[i, j] = Σ_y_site[i, j] / sqrt(Σ_y_site[i, i] · Σ_y_site[j, j]).\n\nDiagonal entries are exactly 1.0. The off-diagonals are the site-level correlations driven by the shared loadings Λ_B.\n\n\n\n\n\n","title":"GLLVM.correlation"},{"category":"method","location":"api/#GLLVM.em_fa-Tuple{AbstractMatrix, Integer}","page":"API reference","text":"em_fa(y::AbstractMatrix, K::Integer;\n      λ_init = nothing, ψ_init = nothing,\n      tol = 1e-8, max_iter = 500)\n    -> (Λ, ψ, loglik, n_iter, converged)\n\nEM for factor analysis. y is (p, n). Returns Λ (p × K), ψ (p-vector of positive idiosyncratic variances), final log-likelihood, iteration count, and convergence flag.\n\nE-step (per observation s):     β = Λ'(ΛΛ' + Ψ)⁻¹                        (K × p, via Woodbury)     E[ηs | ys]      = β ys     E[ηs ηs' | ys] = I − β Λ + β ys ys' β'\n\nM-step (aggregated over s = 1, …, n):     Syy = Y Y'                              (p × p)     Syη = Syy β'                            (p × K)     Sηη = n (I − β Λ) + β Syy β'            (K × K)     Λnew = Syη Sηη⁻¹     ψnew = diag(Syy − Λnew Syη') / n\n\nBoth updates are closed-form. The log-likelihood is evaluated via Woodbury at the start of each iteration (i.e. at the parameters produced by the previous M-step) so monotone non-decrease is testable.\n\n\n\n\n\n","title":"GLLVM.em_fa"},{"category":"method","location":"api/#GLLVM.fit_gaussian_gllvm-Tuple{AbstractMatrix}","page":"API reference","text":"fit_gaussian_gllvm(y; K, K_W=0, has_diag=false, K_phy=0,\n                   has_phy_unique=false, Σ_phy=nothing, X=nothing,\n                   σ_eps_init=1.0, λ_init=nothing, λ_W_init=nothing,\n                   λ_phy_init=nothing,\n                   σ²_B_init=0.1, σ²_W_init=0.1, σ_phy_init=0.1,\n                   β_init=nothing, x_tol=1e-8, f_tol=1e-10,\n                   g_tol=1e-6, iterations=500) -> GllvmFit\n\nL-BFGS minimisation of the closed-form Gaussian marginal NLL via ForwardDiff gradients. Returns a GllvmFit with parameter estimates, convergence diagnostics, and wall-clock fit time.\n\nUnder the hood the optimisation runs on the profile NLL (σ²_eps and optionally β profiled out analytically, MixedModels.jl-style). The public API and parameter recovery semantics are unchanged.\n\nJ1 behaviour (K_W = 0, has_diag = false, X = nothing, K_phy = 0, has_phy_unique = false, Σ_phy = nothing) is preserved unchanged.\n\nOptional extensions:\n\nJ2-A-WD: K_W::Integer = 0 (W-tier rank), has_diag::Bool = false (per-trait diagonal RE σ²B, σ²W).\nJ3 phylogenetic: K_phy::Integer = 0 (Λphy rank), `hasphyunique::Bool = false` (per-trait σphy), and Σ_phy::AbstractMatrix (p × p species covariance, required when K_phy > 0 or has_phy_unique).\n\nOptional fixed effects:\n\nX::AbstractArray{<:Real, 3} of shape (p, n_sites, q).\nβ_init::AbstractVector of length q (defaults to zeros(q)).\n\nThe fit's pars NamedTuple always contains (σ_eps, Λ, β, Λ_W, σ²_B, σ²_W, Λ_phy, σ_phy, θ_packed) where Λ_W, σ²_B, σ²_W, Λ_phy, σ_phy are nothing when the corresponding flag is off.\n\n\n\n\n\n","title":"GLLVM.fit_gaussian_gllvm"},{"category":"method","location":"api/#GLLVM.gaussian_marginal_loglik-Tuple{AbstractMatrix, AbstractMatrix, Real}","page":"API reference","text":"gaussian_marginal_loglik(y, Λ_B, σ_eps; X=nothing, β=nothing,\n                          Λ_W=nothing, σ²_B=nothing, σ²_W=nothing,\n                          Λ_phy=nothing, σ_phy=nothing,\n                          Σ_phy=nothing) -> Real\n\nMarginal log-likelihood of y (size p × nsites) under the Gaussian GLLVM with unit-tier loadings `ΛB(p × K_B), residual SDσeps, and optional W tier (ΛW, p × K_W) and per-trait diagonal random effects (σ²B,σ²W`, length p, positive variances).\n\nThe per-trait diagonal contribution is     dtotal[t] = (ΛW ΛW')[t,t] + σ²B[t] + σ²W[t] + σ²eps. Without phylogeny, the site covariance is A = Λ_B Λ_B' + diag(d_total), inverted via Woodbury (cost O(p KB² + KB³) per site).\n\nFixed effects: pass both X::Array{<:Real, 3} of shape (p, n_sites, q) and β::Vector of length q, or neither.\n\nΛ_W = nothing, σ²_B = nothing, σ²_W = nothing together reproduce the J1 behaviour exactly (D = σ²_eps I).\n\nPhylogenetic extension (Σ_phy::AbstractMatrix, p × p, supplied by caller — typically a species-trait covariance derived from a tree):\n\nΛ_phy::AbstractMatrix (p × K_phy): phylo-latent loadings.\nσ_phy::AbstractVector (length p): per-trait phylo-unique SDs.\n\nWith Λphyaug = hcat(Λphy, σphy) the marginal covariance of vec(y) is I_n ⊗ A + J_n ⊗ B where B = (Λ_phy_aug Λ_phy_aug') .* Σ_phy. The rotation trick (J_n has rank 1) reduces this to two p×p Cholesky factorisations regardless of n.\n\n\n\n\n\n","title":"GLLVM.gaussian_marginal_loglik"},{"category":"method","location":"api/#GLLVM.gaussian_marginal_loglik_sparse_phy-Tuple{AbstractMatrix, AbstractMatrix, Real}","page":"API reference","text":"gaussian_marginal_loglik_sparse_phy(y, Λ_B, σ_eps;\n    X=nothing, β=nothing,\n    Λ_W=nothing, σ²_B=nothing, σ²_W=nothing,\n    Λ_phy=nothing, σ_phy=nothing,\n    phy::AugmentedPhy, σ²_phy::Real = 1.0)\n\nClosed-form Gaussian marginal log-likelihood with the phylogenetic covariance represented in augmented-state sparse precision form instead of a dense Σ_phy. Numerically equivalent to gaussian_marginal_loglik(...; Σ_phy = dense) where dense is the explicit σ²_phy · (S Q_cond⁻¹ S') corresponding to phy, but scales as O(p) thanks to the sparse Cholesky on the augmented precision.\n\nUse this on phylogenies with hundreds to tens of thousands of species, where forming or factorising the dense p × p Σ_phy is prohibitive.\n\nEvaluation-only (AD limitation)\n\nThis path is evaluation-only: CHOLMOD (Julia's sparse Cholesky) does not support ForwardDiff.Dual element types, so inputs are cast to Float64 for the sparse solve. AD-based fitting (fit_gaussian_gllvm) must therefore use the dense gaussian_marginal_loglik path. The sparse path is intended for likelihood evaluation, simulation, and verification on large trees — not for the optimiser's inner loop.\n\n\n\n\n\n","title":"GLLVM.gaussian_marginal_loglik_sparse_phy"},{"category":"method","location":"api/#GLLVM.gaussian_nll_packed-Tuple{AbstractVector, AbstractMatrix, Integer, Integer}","page":"API reference","text":"gaussian_nll_packed(params, y, p, K; X=nothing, q=0) -> Real\n\nJ1 / J2-A signature (single-tier, optional fixed effects). Parameter layout:\n\nparams[1:q]     = β (when q > 0)\nparams[q + 1]   = log σ_eps\nparams[(q+2):end] = θrr (packed ΛB, length rr_theta_len(p, K))\n\n\n\n\n\n","title":"GLLVM.gaussian_nll_packed"},{"category":"method","location":"api/#GLLVM.gaussian_nll_packed-Tuple{AbstractVector, AbstractMatrix}","page":"API reference","text":"gaussian_nll_packed(params, y; spec, X=nothing, Σ_phy=nothing) -> Real\n\nJ2-A-WD / J3 signature carrying a spec::NamedTuple with fields (q, p, K_B, K_W, has_diag) and optionally K_phy and has_phy_unique for the phylogenetic block. Parameter layout:\n\n[β               (spec.q entries)\n log_σ_eps       (1)\n log_σ_B         (p entries if spec.has_diag)\n log_σ_W         (p entries if spec.has_diag)\n θ_rr_B          (rr_theta_len(p, K_B) entries)\n θ_rr_W          (rr_theta_len(p, K_W) entries if spec.K_W > 0)\n σ_phy           (p entries if spec.has_phy_unique, identity link — signed)\n θ_rr_phy        (rr_theta_len(p, K_phy) entries if spec.K_phy > 0)]\n\nX may be passed as a keyword (required iff spec.q > 0). Σ_phy (p × p) is required iff spec.K_phy > 0 or spec.has_phy_unique.\n\nFor the J1 case (K_W = 0, has_diag = false), the layout collapses to [β; log_σ_eps; θ_rr_B] and the result matches the legacy positional method above.\n\n\n\n\n\n","title":"GLLVM.gaussian_nll_packed"},{"category":"method","location":"api/#GLLVM.gaussian_profile_nll-Tuple{AbstractVector, AbstractMatrix}","page":"API reference","text":"gaussian_profile_nll(params, y; spec, X=nothing, Σ_phy=nothing,\n                     profile_beta=true) -> Real\n\nProfile negative log-likelihood. σ²eps is profiled out analytically; β is profiled out via GLS when `profilebeta=true` and there is no phylogenetic block.\n\nParameter layout (length = profile_nparams(spec; profile_beta)):\n\nβ (spec.q entries if !profilebeta or hasphy_block)\nlogτB (p entries if spec.hasdiag) — τB = exp(2·logτB), i.e. ratios σ²B / σ²eps on the log-SD scale.\nlogτW (p entries if spec.has_diag)\nθrrB for LB (rrthetalen(p, KB) entries)\nθrrW for LW (rrthetalen(p, KW) entries if K_W > 0)\nρphy (p entries if hasphy_unique) — identity link, signed.\nθrrphy for Lphy (rrthetalen(p, Kphy) entries if K_phy > 0)\n\n\n\n\n\n","title":"GLLVM.gaussian_profile_nll"},{"category":"method","location":"api/#GLLVM.grad_node_perspecies-Tuple{NodePerSpecies, AbstractVector, Real}","page":"API reference","text":"grad_node_perspecies(st::NodePerSpecies, y::AbstractVector, μ::Real) -> Vector{Float64}\n\nPer-species gradient ∂negll/∂σ_phy[t] (length p) of the matched single-trait fixed-μ phylogenetic model at the data vector y (length p) and mean μ. Each entry is\n\ng[t] = ½ (trace_t − dataq_t),\ntrace_t = 2 σ_eps⁻² σ_phy[t] (Λ̃⁻¹)_{leaf(t),leaf(t)},\ndataq_t = 2 u_t (Σ_φ Λ_φ u)_t,   u = Σ⁻¹ (y − μ),\n\nwith the node-diagonal (Λ̃⁻¹)_{ll} from takahashi_diag (O(nnz L)) and u via a Woodbury solve through Λ̃. O(p) given the NodePerSpecies pre-factorisation. Matches the edge-frame per-species gradient to machine precision while needing only a node diagonal (no ancestor–descendant path-pairs).\n\nSign convention: returns ∂negll/∂σ_phy — the gradient of the negative log-likelihood (what an optimiser minimises; consumed by the O(p) single-trait fitter). This is the OPPOSITE sign to node_grad, whose blocks are ∂loglik/∂θ. Verified against central FD of +negll to rel < 1e-6.\n\n\n\n\n\n","title":"GLLVM.grad_node_perspecies"},{"category":"method","location":"api/#GLLVM.init_theta_rr-Tuple{Integer, Integer}","page":"API reference","text":"init_theta_rr(p::Integer, K::Integer) -> Vector{Float64}\n\nDefault initial values matching gllvmTMB::initrrtheta (R/fit-multi.R:1291-1295):\n\ndiagonal entries initialized to 0.5\nstrict-lower entries initialized to 0\n\n\n\n\n\n","title":"GLLVM.init_theta_rr"},{"category":"method","location":"api/#GLLVM.leaf_block_inv-Tuple{GLLVM.SparsePhyState}","page":"API reference","text":"leaf_block_inv(st::SparsePhyState) -> (LB_leaf, cols)\n\nDense (Kaug·p) × (Kaug·p) leaf-row × leaf-col block of M_sad⁻¹. cols are the augmented-state column indices corresponding to each column of LB_leaf (= the leaf positions, axis-stacked). Computed via the Woodbury form M_sad⁻¹ = Q_eff⁻¹ + α · X_G · S_K⁻¹ · X_G', restricted to leaf rows and columns. The leaf-block of Q_eff⁻¹ is obtained from a single batched CHOLMOD solve against the leaf unit columns (the dominant cost).\n\n\n\n\n\n","title":"GLLVM.leaf_block_inv"},{"category":"method","location":"api/#GLLVM.low_rank_chol-Tuple{AbstractMatrix, AbstractVector}","page":"API reference","text":"low_rank_chol(Λ::AbstractMatrix, d::AbstractVector)\n\nBuild the LowRankPlusDiagChol factorisation of M = Λ Λ' + Diagonal(d).\n\nPromotes eltype(Λ) and eltype(d) so AD element types (e.g. ForwardDiff.Dual) flow through. Constructs the K × K capacitance matrix I_K + Λ' D⁻¹ Λ and factorises it once.\n\n\n\n\n\n","title":"GLLVM.low_rank_chol"},{"category":"method","location":"api/#GLLVM.make_phy-Tuple{AbstractVector, Integer}","page":"API reference","text":"make_phy(edges::AbstractVector{<:Tuple}, n_leaves::Integer;\n         root_index::Integer = -1) :: AugmentedPhy{Float64}\n\nConvenience constructor: build an AugmentedPhy from a list of edges (parent_id, child_id, branch_length) with integer node ids 1..n_total (leaves first, internals last is the recommended convention but not required).\n\nIf root_index < 0 it is auto-detected as the unique node that is not a child in any edge.\n\nThis bypasses the Newick parser — useful for tests and for trees that arrive from another tool already as edge lists.\n\n\n\n\n\n","title":"GLLVM.make_phy"},{"category":"method","location":"api/#GLLVM.node_blups-Tuple{NodePerSpecies, AbstractVector, Real}","page":"API reference","text":"node_blups(st::NodePerSpecies, y::AbstractVector, μ::Real) -> (û, ẑ_tip)\n\nAncestral node posterior-mean BLUPs for the matched single-trait model. The node posterior over u (non-root augmented nodes) has precision Λ̃ and mean\n\nû = Λ̃⁻¹ (σ_eps⁻² S' Λ_φ (y − μ)),\n\nand the tip phylo BLUP on the data scale is ẑ_tip[t] = σ_phy[t] û[leaf(t)]. Returns (û, ẑ_tip) with û indexed in the root-dropped node ordering.\n\nCAVEAT. Node-frame posterior-mean BLUPs û are exact (≤8e-16 vs the dense reference). Edge-frame branch increments derived by differencing (û_child − û_parent) differ from the edge-frame (P2) representation by a √σ²_phy-scale convention; do not treat them as P2-equivalent branch BLUPs. See docs/dev-log/decisions/2026-05-30-node-gradient-5.4e-2-convention.md.\n\n\n\n\n\n","title":"GLLVM.node_blups"},{"category":"method","location":"api/#GLLVM.node_dσ_phy_only-Tuple{GLLVM.SparsePhyState}","page":"API reference","text":"node_dσ_phy_only(st::SparsePhyState) -> Vector{Float64}\n\nPer-species dσ_phy block alone (length p), sharing the O(p) cc = C⁻¹m solve. This is the headline node-diagonal object — the apples-to-apples analogue of the edge-frame per-species gradient — isolated from the global dΛ_B / dσ²_eps / dσ²_phy work for timing and scaling studies.\n\n\n\n\n\n","title":"GLLVM.node_dσ_phy_only"},{"category":"method","location":"api/#GLLVM.node_grad-Tuple{GLLVM.SparsePhyState}","page":"API reference","text":"node_grad(st::SparsePhyState) -> (; dΛ_B, dσ²_eps, dσ²_phy, dσ_phy)\n\nFull NODE-FRAME analytic gradient of the phylounique marginal log-likelihood `sparsephyvalue(st). Every block is O(p) given a sparsecholQeff:dσphyfrom the node-diagonaltakahashidiag` + rank-KB Woodbury correction; dσ²_phy / dσ²_eps from the same-leaf Takahashi selected inverse; dΛ_B from the engine's low-rank algebra (identical to sparse_phy_grad's P_A Λ_B block).\n\nReturns the same gradient as the engine's sparse_phy_grad for a phylo_unique state (verified to machine precision, ≤1e-13), but extracts the tree-coupled trace from a NODE DIAGONAL rather than a dense leaf-leaf block, so it stays O(p) on every tree shape (balanced AND caterpillar).\n\nAssumes the phylounique configuration (`st.Kaug == 1): one shared per-trait phylogenetic random effect with SDsσphy = st.Λaug[:, 1]and no separateΛ_phy` axis.\n\nEvaluation-only for ForwardDiff: the node-diagonal uses a CHOLMOD Float64 factor (see file header).\n\n\n\n\n\n","title":"GLLVM.node_grad"},{"category":"method","location":"api/#GLLVM.pack_lambda-Tuple{AbstractMatrix}","page":"API reference","text":"pack_lambda(Λ::AbstractMatrix) -> AbstractVector\n\nForward pack: given a p × K loading matrix with strict-upper = 0, return the flat θ vector of length rrthetalen(p, K).\n\n\n\n\n\n","title":"GLLVM.pack_lambda"},{"category":"method","location":"api/#GLLVM.phylo_signal-Tuple{GllvmFit}","page":"API reference","text":"phylo_signal(fit::GllvmFit; Σ_phy = nothing) -> Vector\n\nPer-trait phylogenetic signal H²[t] = (Λ_phy_aug Λ_phy_aug')[t, t] · Σ_phy[t, t] / Σ_y_site[t, t], where Λ_phy_aug = hcat(Λ_phy, σ_phy) (each piece is included only when its flag is on). Returns a vector of length p; all entries are NaN when the fit has no phylogenetic block (K_phy == 0 and has_phy_unique == false).\n\nΣ_phy defaults to the identity matrix (standardised convention, diag == 1 per trait), so the diagonal entries reduce to H²[t] = (Λ_phy_aug Λ_phy_aug')[t, t] / Σ_y_site[t, t]. Supply the fitted phylogenetic VCV explicitly when the diagonal is not unit.\n\n\n\n\n\n","title":"GLLVM.phylo_signal"},{"category":"method","location":"api/#GLLVM.ppca_init-Tuple{AbstractMatrix, Integer}","page":"API reference","text":"ppca_init(y::AbstractMatrix, K::Integer; lower_tri::Bool = true) -> (Λ, σ_eps)\n\nClosed-form PPCA initialisation. y is (p, nsites). Returns Λ (p × K) and `σeps` (positive scalar).\n\nImplements the maximum-likelihood estimator of Tipping & Bishop (1999, eqs. 8-11) for the model y ~ N(0, Λ Λ' + σ² I). Let S = y y' / n, with eigendecomposition S = U Λ_S U' and eigenvalues sorted in descending order. Then\n\nσ̂² = (1/(p - K)) Σ{k=K+1}^p ΛS[k]   Λ̂  = UK (ΛS[1:K] - σ̂² I)^{1/2}\n\nwhere UK is the leading p × K block of U. Negative residual eigenvalues (ΛS[k] < σ̂² for k ≤ K) are floored at zero, matching the standard PPCA convention.\n\nIf lower_tri = true (default), rotate Λ so its top K × K block is lower triangular with positive diagonals, matching the engine's packing convention. The rotation is orthogonal, so Λ Λ' (and therefore the model likelihood) is invariant — see rotate_to_lower_triangular.\n\nFor p ≤ K the closed-form PPCA solution is degenerate (no residual eigenvalues to estimate σ² from); this implementation requires K < p.\n\n\n\n\n\n","title":"GLLVM.ppca_init"},{"category":"method","location":"api/#GLLVM.profile_ci-Tuple{GllvmFit, AbstractString}","page":"API reference","text":"profile_ci(fit::GllvmFit, parm::AbstractString; kwargs...)\n    -> NamedTuple{(:lower, :upper, :method)}\n\nConvenience method that looks up parm by name (e.g., \"sigma_eps\", \"Lambda_B[1,1]\", \"Lambda:1,1\") and calls the integer-index method.\n\nNaming convention matches confint(fit) from src/confint.jl: SDs are reported on the raw (positive) scale, β and Λ on their native scale.\n\n\n\n\n\n","title":"GLLVM.profile_ci"},{"category":"method","location":"api/#GLLVM.profile_ci-Tuple{GllvmFit, Integer}","page":"API reference","text":"profile_ci(fit::GllvmFit, param_index::Integer;\n           level = 0.95, grid_extent = 5, max_expand = 20,\n           max_bisect = 30, y = nothing, X = nothing,\n           Σ_phy = nothing)\n    -> NamedTuple{(:lower, :upper, :method)}\n\nProfile-likelihood CI for the parameter at packed position param_index in fit.pars.θ_packed.\n\ngrid_extent controls how far (in Wald SEs from θ̂i, geometrically expanding) the initial bracket walks before bisection. Larger values help for asymmetric likelihoods; the geometric expansion keeps the total number of refits at O(log) even at large `gridextent`.\n\nlevel is the nominal coverage (default 0.95 → χ²_1 cutoff ≈ 3.841).\n\nThe data matrix y (the same y passed to fit_gaussian_gllvm) must be supplied so this function can reconstruct the NLL closure. X and Σ_phy are required iff the fit used them.\n\nReturns a NamedTuple with fields:\n\nlower::Float64 — lower CI bound on the raw scale for SD-style parameters (σeps, σB, σW, σphy), native scale for β / Λ.\nupper::Float64 — upper CI bound, same scale convention.\nmethod::Symbol — :profile if both bounds were bracketed, :partial if only one side was found (the other is NaN), or :failed if neither side could be bracketed (both NaN).\n\nFailure modes (each side independently):\n\nThe bracket never crosses the chisq cutoff within max_expand geometric expansions → that bound is NaN.\nA constrained refit at a candidate value fails → bracket contracts inward on that side, still typically yielding a finite bound.\n\n\n\n\n\n","title":"GLLVM.profile_ci"},{"category":"method","location":"api/#GLLVM.profile_ci_derived-Tuple{GllvmFit, Function}","page":"API reference","text":"profile_ci_derived(fit::GllvmFit, derived_fn::Function;\n                   level = 0.95, y = nothing,\n                   X = nothing, Σ_phy = nothing,\n                   penalty_weight = 1e6,\n                   initial_step = nothing,\n                   max_expand = 20, max_bisect = 30)\n    -> NamedTuple{(:lower, :upper, :estimate, :method)}\n\nProfile-likelihood CI for a scalar-valued derived quantity g(θ) = derived_fn(θ_packed). The constraint g(θ) = c is enforced via a quadratic penalty     NLL_pen(θ) = NLL(θ) + 0.5 · penalty_weight · (g(θ) − c)², re-optimised over θ via LBFGS at each candidate c. The profile log- likelihood at c is the unpenalised NLL evaluated at the constrained minimum θ̂(c); the deviance D(c) = 2(ℓ̂ − ℓ_p(c)) is ~ χ²₁ under g(θ) = c, so the CI is {c : D(c) ≤ qchisq(1−α, 1)}. Bracket-then-bisect on each side.\n\nderived_fn must accept a packed-parameter vector and return a scalar (Float64). For the built-in derived quantities, use the closure helpers:\n\nspec = GLLVM._derived_spec(fit)\nf_c1 = θ -> GLLVM._communality_packed(θ, spec, 1)\nci   = GLLVM.profile_ci_derived(fit, f_c1; y = y)\n\nOr, for σ²eps (sanity check vs the parameter profile CI on σeps):\n\nf_s2 = θ -> exp(2 * θ[1])    # log_σ_eps is at index 1 when q = 0\n\npenalty_weight defaults to 1e6 — the final weight at the end of an internal escalating schedule (1e2 → 1e3 → … → penalty_weight). The escalation is essential: in phylogenetically active cells, jumping straight to a large w at the warm-start θ̂ inflates the penalty term by O(w · (g(θ̂) − c)²) and pushes LBFGS into pathological regions (the constrained per-site covariance can drift non-PD), producing degenerate CIs. The schedule lets the optimiser move smoothly to the {g ≈ c} manifold first, then tightens. The internal NLL is also wrapped to return a finite barrier on PosDefException.\n\ninitial_step (default nothing → max(0.05 · |g(θ̂)|, 0.01)) seeds the bracket expansion. Smaller is better here — the geometric expansion inside the bisection grows the step rapidly, while a small first step keeps the very first constrained refit close to θ̂ (where the safe-NLL barrier is rarely triggered).\n\nReturns a NamedTuple with fields:\n\nestimate::Float64 — g(θ̂) at the original MLE\nlower::Float64    — lower CI bound (NaN if bracket failed)\nupper::Float64    — upper CI bound (NaN if bracket failed)\nmethod::Symbol    — :profile (both bounds), :partial                       (one side NaN), or :failed (both NaN)\n\n\n\n\n\n","title":"GLLVM.profile_ci_derived"},{"category":"method","location":"api/#GLLVM.profile_nparams-Tuple{NamedTuple}","page":"API reference","text":"profile_nparams(spec; profile_beta=true) -> Int\n\nNumber of parameters the profile NLL optimises over. Drops σeps (always) and β (when `profilebeta && !has_phy`).\n\n\n\n\n\n","title":"GLLVM.profile_nparams"},{"category":"method","location":"api/#GLLVM.profile_recover-Tuple{AbstractVector, AbstractMatrix}","page":"API reference","text":"profile_recover(params, y; spec, X=nothing, Σ_phy=nothing,\n                profile_beta=true) -> NamedTuple\n\nRun one final NLL pass at params and return everything needed to build the user-facing fit:   (logLik, σeps, β, ΛB, ΛW, σ²B, σ²W, Λphy, σ_phy)\n\n\n\n\n\n","title":"GLLVM.profile_recover"},{"category":"method","location":"api/#GLLVM.proportions-Tuple{GllvmFit}","page":"API reference","text":"proportions(fit::GllvmFit; component::Symbol = :shared) -> Vector\n\nPer-trait variance decomposition. Each entry is in [0, 1]; the :shared, :unique_W, :unique_B, and :residual shares sum to 1 (when has_diag and W tier are off, only :shared and :residual are non-zero).\n\ncomponent can be:\n\n:shared    — (ΛB ΛB')[t,t] / Σysite[t,t]   (== communality)\n:unique_W  — (ΛW ΛW')[t,t] / Σysite[t,t]\n:unique_B  — σ²B[t] / Σy_site[t,t]            (J2-A-WD path)\n:unique_Wd — σ²W[t] / Σy_site[t,t]            (J2-A-WD path)\n:residual  — σ²eps / Σy_site[t,t]\n\n\n\n\n\n","title":"GLLVM.proportions"},{"category":"method","location":"api/#GLLVM.random_balanced_tree-Tuple{Integer}","page":"API reference","text":"random_balanced_tree(p::Integer; branch_length::Real = 0.1) :: AugmentedPhy\n\nBuild a near-balanced binary tree with p leaves. All branch lengths equal branch_length. Used in benchmarks and scaling tests.\n\nWhen p is a power of 2 this is perfectly balanced. Otherwise the left-over leaf at each level is carried up one extra step (so the tree remains binary, just with slightly uneven depths). Branch lengths stay uniform — the goal is a representative sparse-tree topology, not an ultrametric one.\n\n\n\n\n\n","title":"GLLVM.random_balanced_tree"},{"category":"method","location":"api/#GLLVM.rotate_to_lower_triangular-Tuple{AbstractMatrix}","page":"API reference","text":"rotate_to_lower_triangular(Λ::AbstractMatrix) -> Matrix\n\nRotate a p × K loading matrix (p ≥ K) so its top K × K block is lower triangular with positive diagonals — the canonical sign / orientation used by the engine's packing convention. The rotation is orthogonal (K × K), so Λ Λ' is preserved and any GLLVM likelihood depending only on Λ Λ' is invariant.\n\nConvention used here: QR-decompose Λ' (a K × p matrix). The thin Q factor is K × K orthogonal; Q'·Λ' is upper-triangular in its first K columns, so Λ·Q has a lower-triangular top K × K block. We then flip the sign of any column whose new diagonal entry is negative, restoring the positive-diagonal sign anchor used by unpack_lambda.\n\nNote that this convention determines a sign per column but does NOT fix the overall orientation when Λ Λ' has repeated eigenvalues. In that degenerate case any rotation within the repeated-eigenvalue subspace is equally valid; the QR pivoting choice is implementation- defined.\n\n\n\n\n\n","title":"GLLVM.rotate_to_lower_triangular"},{"category":"method","location":"api/#GLLVM.rr_theta_len-Tuple{Integer, Integer}","page":"API reference","text":"rr_theta_len(p::Integer, K::Integer) -> Int\n\nNumber of parameters needed to pack a p × K lower-triangular loading matrix with zero strict upper triangle.\n\n\n\n\n\n","title":"GLLVM.rr_theta_len"},{"category":"method","location":"api/#GLLVM.sigma_phy_dense-Tuple{GLLVM.AugmentedPhy}","page":"API reference","text":"sigma_phy_dense(phy::AugmentedPhy; σ²_phy::Real = 1.0) :: Matrix\n\nBuild the dense (p × p) leaf covariance Σ_phy = σ²_phy · (S Q_cond⁻¹ S') where Q_cond is phy.Q_topology with the root row/col removed and S selects leaves. This is what the existing dense path expects; used by verification tests to compare sparse vs. dense.\n\nThis is O(p³) in storage and time — only intended for small trees in tests. Do NOT call it on the real workload; the entire point of AugmentedPhy is to avoid materialising Σ_phy.\n\n\n\n\n\n","title":"GLLVM.sigma_phy_dense"},{"category":"method","location":"api/#GLLVM.sigma_y_site-Tuple{GllvmFit}","page":"API reference","text":"sigma_y_site(fit::GllvmFit) -> Matrix\n\nThe per-site (within-species) trait covariance Σ_y_site = Λ_B Λ_B' + diag(d_total) where d_total[t] = (Λ_W Λ_W')[t,t] + σ²_B[t] + σ²_W[t] + σ²_eps. For J1, Λ_W = nothing, σ²_B = σ²_W = 0, so the diagonal collapses to σ²_eps.\n\nThe phylogenetic block is not included — for J3, the phylo contribution is rank-1 across species and is separated out for biological interpretation. Use phylo_signal to recover the phy component, and correlation for the per-site cross-trait correlations.\n\n\n\n\n\n","title":"GLLVM.sigma_y_site"},{"category":"method","location":"api/#GLLVM.takahashi_diag-Tuple{SparseArrays.CHOLMOD.Factor{Float64}}","page":"API reference","text":"takahashi_diag(ch::SparseArrays.CHOLMOD.Factor) -> Vector{Float64}\n\nConvenience: return ONLY diag(Q⁻¹) (a length-n vector, in the ORIGINAL ordering) via the Takahashi recursion. Same cost as takahashi_selinv but without materialising the full sparse output (a small allocation win when only the diagonal is needed, as in the EM E-step's per-trait variance).\n\n\n\n\n\n","title":"GLLVM.takahashi_diag"},{"category":"method","location":"api/#GLLVM.takahashi_selinv-Tuple{SparseArrays.CHOLMOD.Factor{Float64}}","page":"API reference","text":"takahashi_selinv(ch::SparseArrays.CHOLMOD.Factor) -> SparseMatrixCSC\n\nCompute the Takahashi selected inverse of the matrix Q whose sparse Cholesky factor is ch (P · Q · Pᵀ = L · Lᵀ with P = I[ch.p, :]). Returns a SparseMatrixCSC holding Q⁻¹ (in the ORIGINAL un-permuted ordering) at the union sparsity of Pᵀ (L + Lᵀ) P. Entries outside that pattern are NOT computed (and are NOT zero in general).\n\nCost: O(nnz(L)) arithmetic + O(nnz(L)·log(maxcolnnz)) for the symmetric lookups (with constant max_col_nnz on a tree this is O(nnz(L)) overall).\n\n\n\n\n\n","title":"GLLVM.takahashi_selinv"},{"category":"method","location":"api/#GLLVM.unpack_lambda-Tuple{AbstractVector, Integer, Integer}","page":"API reference","text":"unpack_lambda(θ::AbstractVector, p::Integer, K::Integer) -> AbstractMatrix\n\nInverse of pack_lambda. Returns a p × K matrix Λ with the diagonals and strict-lower entries filled from θ and the strict upper triangle = 0.\n\nAD-friendly: eltype(θ) is preserved, so θ::Vector{<:ForwardDiff.Dual} returns a matrix of Duals.\n\n\n\n\n\n","title":"GLLVM.unpack_lambda"},{"category":"method","location":"api/#LinearAlgebra.ldiv!-Tuple{AbstractVector, GLLVM.LowRankPlusDiagChol, AbstractVector}","page":"API reference","text":"ldiv!(out, F::LowRankPlusDiagChol, b)\nldiv!(out, F::LowRankPlusDiagChol, b, buf_K)\n\nIn-place Woodbury solve out = M⁻¹ b with M = F.Λ F.Λ' + Diagonal(F.d).\n\nIf buf_K is omitted a fresh K-vector is allocated; pass one in to make this fully allocation-free on the hot path.\n\n\n\n\n\n","title":"LinearAlgebra.ldiv!"},{"category":"type","location":"api/#GLLVM.AugmentedPhy","page":"API reference","text":"AugmentedPhy{T}\n\nAugmented-state sparse phylogenetic precision for a binary tree.\n\nFields\n\nn_leaves::Int               – number of tip species (p).\nn_total::Int                – 2p − 1, leaves + internal ancestor nodes.\nQ_topology::SparseMatrixCSC – (ntotal × ntotal) topology contribution to the sparse precision. The actual phylogenetic precision is Q_topology / σ²_phy. About 8p non-zeros.\nleaf_indices::Vector{Int}   – maps a leaf k ∈ 1:p to its row/col in the augmented state. Ordering matches the order leaves were encountered in the Newick string (left-to-right).\nleaf_names::Vector{String}  – species names parsed from the Newick.\nbranch_lengths::Vector{T}   – the 2p − 2 branch lengths in the order the parser walked the tree.\nroot_index::Int             – which augmented row is the root.\n\nQ_topology is positive semi-definite (rank 2p − 2). The all-ones vector is its sole zero eigenvector — fixing the root removes the degeneracy. The sparse log-likelihood path adds a positive contribution to the leaf diagonals (proportional to λ_phy² / d_total) which renders the active solve matrix positive definite without any explicit ridge.\n\n\n\n\n\n","title":"GLLVM.AugmentedPhy"},{"category":"type","location":"api/#GLLVM.GllvmFit","page":"API reference","text":"GllvmFit\n\nResult of fit_gaussian_gllvm. Holds the fitted parameters, the converged log-likelihood, convergence info, and the raw Optim result.\n\n\n\n\n\n","title":"GLLVM.GllvmFit"},{"category":"type","location":"api/#GLLVM.GllvmModel","page":"API reference","text":"GllvmModel(p, K; K_W=0, has_diag=false, K_phy=0, has_phy_unique=false)\n\nImmutable spec describing a Gaussian GLLVM. p traits, K (= KB) unit-tier latent factors, plus optional W tier (`KW), per-trait diagonal random effects (hasdiag), and phylogenetic block (Kphyaxes ofΛphyand/or per-traitσphywhenhasphyunique). The single-tier J1 case is the defaultKW = 0,hasdiag = false,Kphy = 0,hasphy_unique = false`.\n\n\n\n\n\n","title":"GLLVM.GllvmModel"},{"category":"type","location":"api/#GLLVM.LowRankPlusDiagChol","page":"API reference","text":"LowRankPlusDiagChol{T}\n\nFactorisation of M = Λ Λ' + Diagonal(d) that stores three small pieces:\n\nd::Vector{T}              — the positive diagonal of D.\nΛ::Matrix{T}              — the p × K low-rank factor.\ncholK::Cholesky{T, Matrix{T}} — Cholesky of the K × K capacitance matrix I_K + Λ' D⁻¹ Λ.\n\nUse low_rank_chol to construct, then \\, ldiv!, or logdet for the Woodbury-based operations.\n\n\n\n\n\n","title":"GLLVM.LowRankPlusDiagChol"},{"category":"section","location":"quickstart/#Quick-start","page":"Quick start","text":"This page walks through one end-to-end fit: simulate a Gaussian GLLVM, fit it with fit_gaussian_gllvm, inspect the recovered parameters, build three flavours of confidence interval, and visualise the recovered Σ_y against the truth.","title":"Quick start"},{"category":"section","location":"quickstart/#1.-Simulate-a-fixture","page":"Quick start","text":"using GLLVM, Random, LinearAlgebra\n\nRandom.seed!(20260528)\n\nn_sites   = 80\nn_species = 10\nK         = 2                  # rank of the latent factor block\nσ_eps     = 0.5\n\n# True low-rank loading matrix Λ_B (n_species × K)\nΛ_true = randn(n_species, K)\n\n# Latent factor scores per site (n_sites × K)\nη = randn(n_sites, K)\n\n# Response matrix y (n_species × n_sites) — closed-form GLLVM with no X\ny = Λ_true * η' .+ σ_eps .* randn(n_species, n_sites)","title":"1. Simulate a fixture"},{"category":"section","location":"quickstart/#2.-Fit-the-model","page":"Quick start","text":"fit = fit_gaussian_gllvm(y; K = K)\n\nfit_gaussian_gllvm returns a GllvmFit object. The Gaussian path is fit at the marginal log-likelihood maximum via the closed-form integration described in the Model page. For models without diagonal random effects or phylogeny the PPCA warm start lands on the optimum directly, so L-BFGS typically reports convergence after 0–1 iterations.","title":"2. Fit the model"},{"category":"section","location":"quickstart/#3.-Inspect-the-recovered-parameters","page":"Quick start","text":"fit.pars                  # named tuple of optimised parameters\nfit.logLik                # marginal log-likelihood at the optimum\nfit.Σ_y                   # marginal covariance Σ_y at the optimum\n\nThe recovered Λ_B can be compared with Λ_true only up to an orthogonal rotation in K-space — the latent factors are identified only up to rotation in the Gaussian model.","title":"3. Inspect the recovered parameters"},{"category":"section","location":"quickstart/#4.-Build-confidence-intervals","page":"Quick start","text":"Three CI flavours share a common interface:\n\nci_wald      = confint(fit)                                # Wald via observed information\nci_profile   = profile_ci(fit, \"sigma_eps\")                # likelihood-profile CI\nci_bootstrap = bootstrap_ci(fit; n_boot = 200)             # parametric bootstrap\n\nWald CIs are cheapest and rely on the local quadratic approximation; profile CIs are exact up to grid resolution; bootstrap CIs make no distributional assumption on the sampling distribution of the estimator.","title":"4. Build confidence intervals"},{"category":"section","location":"quickstart/#5.-Visualise-Σ_y-recovery","page":"Quick start","text":"using Plots\n\nΣ_true = Λ_true * Λ_true' + σ_eps^2 .* I(n_species)\nΣ_hat  = fit.Σ_y\n\nheatmap(\n    [Σ_true Σ_hat (Σ_hat .- Σ_true)],\n    title  = \"Σ_y true | Σ_y est | residual\",\n    aspect_ratio = :equal,\n)\n\nThe residual panel should sit close to zero across the full species-by-species surface. Per-cell agreement at the relative Frobenius scale is < 1e-3 on the benchmark grid (see Benchmarks).","title":"5. Visualise Σ_y recovery"},{"category":"section","location":"comparison/#Comparison:-R,-GLLVM.jl,-MixedModels.jl","page":"Comparison","text":"The benchmark study (see Benchmarks) runs three engines on each cell to put GLLVM.jl's speed numbers in context against the state-of-the-art Julia LMM port. The third engine is MixedModels.jl, the canonical Julia descendant of lme4.","title":"Comparison: R, GLLVM.jl, MixedModels.jl"},{"category":"section","location":"comparison/#What-each-engine-fits","page":"Comparison","text":"R gllvmTMB — fits the GLLVM exactly. The reference engine for every grid cell. TMB Laplace + L-BFGS in C++.\nGLLVM.jl — fits the same GLLVM. Closed-form Gaussian marginal + Optim L-BFGS + ForwardDiff gradients, with a PPCA closed-form warm start for the no-other-RE path.\nMixedModels.jl — cannot fit a K ≥ 1 GLLVM directly because it has no latent-factor block in the formula DSL. On K = 0 cells the GLLVM degenerates to an LMM and the three engines coincide (the K = 0 apples-to-apples sanity check vs lme4 lives in gllvmTMB-julia-bench/report/three-way-smoke.md). On K ≥ 1 cells MixedModels.jl fits the proxy LMM value ~ 1 + (1|site) (or value ~ 0 + trait + (1|site) when fixed effects are present) — a strictly easier problem than the GLLVM, with no rank-K loading parameters and no per-iteration Cholesky update on a low-rank-plus- diagonal target.\n\nThe three-way table is reported as \"what Julia's state-of-the-art LMM engine would do on a comparable-cost LMM problem\" — not a direct head-to-head on the GLLVM.","title":"What each engine fits"},{"category":"section","location":"comparison/#Headline-numbers","page":"Comparison","text":"From PERF+E (full numbers in the benchmark report):\n\nGLLVM.jl vs R gllvmTMB on the GLLVM: median speedup of ~190x on the small cells, ~520x on the mid cells, and ~280x on the large cells. Worst-case |Δ logLik| across the grid: 2.343e-07. Both engines converged on every fit.\nGLLVM.jl vs MixedModels.jl on the proxy LMM: MixedModels.jl is faster on the larger cells (the proxy is a strictly easier problem), but only by a factor of ~1.4–1.8x. The order of magnitude is the same. Where MixedModels.jl is faster, the difference is an upper bound on the cost the latent-factor structure adds; that the cost is bounded above by a small constant factor is itself a measure of how cleanly the closed-form Gaussian marginal eliminates the expensive part of the GLLVM evaluation.","title":"Headline numbers"},{"category":"section","location":"comparison/#Honest-caveats","page":"Comparison","text":"Absolute timings in the smallest cells are sub-millisecond on the Julia side; replicate noise is on the order of single GC pauses. The benchmark uses n_reps = 3 with medians — an order-of-magnitude scaling picture, not a publishable final number. A publishable speed claim wants n_reps >= 10 with median + MAD plus a cold-start row.\nBoth engines get a warm-up pass before the timed grid. The R side loads the TMB DLL and S4 method tables; the Julia side runs one per-(p, n, K, has_intercept) warm-up so the ForwardDiff specialisation is in the JIT cache. Without these warm-ups, rep 1 of every cell would be 10–100x slower than reps 2–3 on the engine that hadn't yet compiled the relevant code path.\nMixedModels.jl's logLik on each row is on a different model from the GLLVM logLik. It is not directly comparable unless K = 0. It is reported only for completeness.","title":"Honest caveats"},{"category":"section","location":"model/#Model","page":"Model","text":"The Gaussian GLLVM that this package implements decomposes the response of species s (a length-p vector across the p species at site s) into a fixed linear predictor plus a sequence of latent contributions:\n\ny_s = X_sbeta + Lambda_Beta_Bs + Lambda_Weta_W s + s_B s + s_W s + s_textphy + varepsilon s\n\nEach term is independent across sites (except where the phylogenetic covariance ties species together) and Gaussian. The latent factors and random effects are integrated out analytically, producing a closed-form marginal log-likelihood whose optimisation is the engine's main loop.","title":"Model"},{"category":"section","location":"model/#Terms","page":"Model","text":"Fixed effects X_s β — the standard linear predictor for trait or intercept effects per species. X_s is the per-site design matrix constructed by the caller (or by the R-side fixture generator); β is estimated by ML jointly with the variance components.\n\nLatent factor block Λ_B η_B[s] — the rank-K ordination axes shared across species. Λ_B is a p × K loading matrix and η_B[s] ∼ N(0, I_K) is the latent gradient at site s. Marginal contribution to Σ_y_site: Λ_B Λ_B'.\n\nUnit-obs latent factor block Λ_W η_W[:, s] — the per-site version of the latent block, used when the model has a latent(0 + trait | site_species) term. Loading matrix shape and packing are identical to Λ_B; the marginal contribution to Σ_y_site is also Λ_W Λ_W'.\n\nSite-tier diagonal random effects s_B[:, s] ∼ N(0, diag(σ²_B)) — per-species independent random effects at the site tier. The marginal contribution to Σ_y_site is diag(σ²_B).\n\nUnit-obs diagonal random effects s_W[:, s] ∼ N(0, diag(σ²_W)) — the per-site version, contributing diag(σ²_W) to Σ_y_site.\n\nPhylogenetic component s_phy ∼ N(0, σ²_phy · Σ_phy) — species are tied by a user-supplied species-by-species covariance Σ_phy. The marginal contribution at a single site is σ²_phy · Σ_phy. Across the full data the structure becomes block-diagonal in site and dense across species via Σ_phy.\n\nObservation noise ε[:, s] ∼ N(0, σ²_eps I_p) — the iid residual term.","title":"Terms"},{"category":"section","location":"model/#Closed-form-Gaussian-marginal","page":"Model","text":"Integrating out η_B, η_W, s_B, s_W and (where present) the phylogenetic random effect yields a Gaussian marginal in y_s with mean X_s β and covariance\n\ny_s sim mathcalNleft(X_sbeta Lambda_BLambda_B^top + mathrmdiag(d_texttotal)right)\n\nwhere d_total = σ²_B + σ²_W + σ²_eps collects every diagonal contribution at a single site, plus the latent-W contribution which behaves like an additional rank-K block at the unit-obs tier. The full data log-likelihood is the sum of these per-site Gaussians, plus the phylogenetic correction below.\n\nThe negative log-marginal-likelihood is evaluated via Woodbury so the expensive p × p operations are reduced to K × K inversions plus a p-vector solve, which is the same trick MixedModels.jl uses for random-effect blocks.","title":"Closed-form Gaussian marginal"},{"category":"section","location":"model/#Rotation-trick-for-phylogenetic-terms","page":"Model","text":"For models with phylo_unique() the full data covariance over the n × p long-form response is\n\nSigma_ytextfull = I_n otimes A + J_n otimes B\n\nwhere A is the iid-across-sites covariance (latent + diagonal RE + ε), B is the phylogenetic contribution σ²_phy · Σ_phy, and J_n is the n × n all-ones matrix. Diagonalising in the site-dimension (which amounts to rotating into the 1_n / √n versus orthogonal-complement basis) decomposes the determinant and quadratic form into the rank-1 component A + n·B and the (n − 1) copies of A, reducing the phylogenetic likelihood evaluation to one p × p Cholesky plus (n − 1) reuses of the iid Cholesky. The engine does this once per gradient evaluation; the marginal log-likelihood remains closed-form.","title":"Rotation trick for phylogenetic terms"},{"category":"section","location":"model/#Identifiability","page":"Model","text":"The loading matrix Λ_B is identified only up to an orthogonal rotation in K-space — the marginal covariance Λ_B Λ_B' is invariant under Λ_B → Λ_B Q for any orthogonal Q. The engine uses the standard lower-triangular packing (matching the R-side gllvmTMB::rr_theta_len(p, K)) as the identifying constraint at the optimum. The latent scores η_B[s] are not estimated; they are integrated out.\n\nThe phylogenetic variance σ²_phy is identified separately from σ²_eps only when the phylogenetic correlation structure differs materially from the identity; very flat trees collapse the identifiability and the engine will report a wide profile CI on σ²_phy in those cases.","title":"Identifiability"},{"category":"section","location":"#GLLVM.jl","page":"Home","text":"A Julia implementation of Generalised Linear Latent Variable Models for multivariate species-by-site data. GLLVM.jl targets the Gaussian + phylogenetic slice required for the benchmark study against R's gllvmTMB package, and is currently a research-stage pilot.","title":"GLLVM.jl"},{"category":"section","location":"#What-is-a-GLLVM?","page":"Home","text":"A GLLVM decomposes the response of each species at each site into a fixed linear predictor, a low-rank latent gradient shared across species (the ordination axes), optional site- and species-level random effects, and an optional phylogenetic component. The latent gradient and random-effect contributions are integrated out analytically under a Gaussian likelihood, giving a marginal model whose covariance structure encodes species-by-species correlation without an explicit species-by-species parameter.","title":"What is a GLLVM?"},{"category":"section","location":"#Why-use-this-package?","page":"Home","text":"Closed-form Gaussian marginal. The latent factors are integrated out exactly — no Laplace approximation — so the marginal log-likelihood is evaluated to machine precision and the gradient is well-conditioned.\nPPCA closed-form warm start. For models without diagonal random effects or phylogeny the probabilistic-PCA initialiser of Tipping and Bishop (1999) is the exact ML solution, so L-BFGS converges in 0–1 iterations.\nReverse-mode-ready AD. ForwardDiff.jl powers the current gradient; the engine is structured so a hand-coded analytic gradient or a ReverseDiff.jl backend can be slotted in without API change.","title":"Why use this package?"},{"category":"section","location":"#Quick-install","page":"Home","text":"using Pkg\nPkg.add(\"GLLVM\")\n\nThe package targets Julia 1.10 and above.","title":"Quick install"},{"category":"section","location":"#Status","page":"Home","text":"GLLVM.jl is a pilot for the benchmark study described in the maintainer's active plan. The API surface in this release covers the Gaussian + multi-tier + phylogenetic path needed to compare against gllvmTMB. Non-Gaussian families, SPDE / spatial random fields and the animal model are out of scope for this version.\n\nSee the Model page for the math and the Quick start page for an end-to-end fit.","title":"Status"}]
}
