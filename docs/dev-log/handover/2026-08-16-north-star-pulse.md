# North-star pulse — 2026-08-16 (Mac-light)

**Operator:** Cursor  
**Lane:** `cursor/north-star-pulse-20260816` (docs-only; sole lane OK)  
**Tip:** `ef96463b` (`origin/main` = merge of #226)

## State

- Merged through **#226** (NB1 + BetaBinom no-X `fit_gllvm` Identity — Arc 0, docs-only).
- **#227** NB1 no-X engine (`fit_gllvm` / `@formula`) in flight — owns `src/` / `test/`; do not touch those paths here.

## Next (after #227)

1. **BetaBinom no-X** — Identity from #226 locks the decision; engine admit next.
2. **ZIB bridge** — still OWED (no invent).
3. **Surface holes** — Tweedie / student-t / delta (capability gaps only until Identity + engine land).

## Fence

- Do **not** open `bridge.jl` for NB1.
- Do **not** invent twin light Δ.
- Stage by name; Mac-light (no local `Pkg.test` for this pulse).
