# Mixed-language programme integration leaf

Ada owns core070_evidence.py, core070_programme_bridge.py, main TOML manifest,
source mapping, case plan and existing metadata tests. Grace owns only
new tools/test_core070_mixed_receipts.py. No engine changes or R oracle edits.

Required IDs partition into Julia and public_r_bridge executors, with no
unknown executor or duplicate case. Julia runs cannot pay R bridge obligations.
The collection must supply exactly the declared bridge IDs and registry hash;
the bridge verifier rechecks actual runtime/model/boundary raw receipts.
Missing, extra, failed or stale evidence fails the full gate. A single Julia
run cannot claim mixed-language completion. Full source-scope freeze remains
mandatory; adding these five cases cannot freeze the incomplete programme.

Main manifest adds five explicit public-R obligations/cases, separate from
17 family-smoke cases and existing Julia interfaces/models. Update source-case
map from10 to15 links over the same five model source facts. Fix copied
truncated-NB2 normalization prose only for Poisson/Beta/NB2 contracts, retaining
all likelihood code, fixtures, data, tolerances and optimization policies.

CHECK: Python mixed-receipt regressions plus full draft-integrity suite.
EXPECT: fake/missing/mixed executor receipts reject; valid synthetic mixed
collection combines exactly its required cases; real manifest remains DRAFT.

Totoro replay: original10 native/formula cases, one Julia/BLAS thread,
estimate2–4minutes,7minute supervisor cap (per-fit batch5minute cap).
Then original public bridge3models/6routes replay, estimate1–3minutes,8minute cap.
No installs, no DRAC jobs, no runs exceeding30minutes. Before/after oracle checks
and retained failures required. Reverify affected receipt fingerprints after the
manifest change; old receipts stay archived, not relabeled current.
