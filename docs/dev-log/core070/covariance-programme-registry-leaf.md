# Bind qualified Gaussian covariance cases into the programme

OWNS: Ada owns central manifest/map, parity registry/runner/helpers, covariance
wrapper fixtures, covariance programme contract generator/validator/tests and
execution receipts. Hopper separately owns the 95-fact covariance case-plan
module, tests and JSON. No shared-file writers.

CHECK: native registry rejects missing/partial covariance groups and binds all
nine actual models to the pinned fixtures. Two old ordinary fixed-residual
models and seven full-rank fitting models retain their existing IDs and controls.
EXPECT: a fresh programme-run receipt for every selected ID, with grouped
assertions counted once, source/fixture/driver/environment hashes and real exit.

The declaration is Gaussian native-model evidence only. Formula and bridge
roles, non-Gaussian source models, complete admission grammar, calibration and
full scope review remain required. Pure parser/helper rows cannot pay fitted
model obligations. Original ordinary full covariance compares only identifiable
total covariance. Explicit tight R controls are recorded, not mislabeled default.

Before implementation retain the existing registry's failure to accept these
IDs. Pure structural checks run locally without fitting. Existing qualified
Totoro RCall environment runs the nine-model programme subset: estimate2–5minutes,
one Julia/BLAS thread, timeout600seconds. No installation/full suite/campaign.
Bridge runtime/boundary qualification is independently refreshed because the
prior live manifest-integrity gate rejected stale Julia source pins.

Full source manifest stays DRAFT until every required source/model/interface
obligation is specified and independent scope review is accepted. This leaf may
reduce unlinked native facts but cannot close their full interface domains.
