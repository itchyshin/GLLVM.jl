# AGHQ source-to-required-case implementation leaf

Goal: make the public Stage1a obligations explicit and connect the existing 39
frozen helper facts to exact Julia control calls where these exist. This is a
manifest slice; it does not implement another estimator or certify full parity.
Ultra Plan owns scope; writing-plans defines this leaf; Unlazy verifies it.

OWNS: tools/core070_aghq_case_plan.py, tools/test_core070_aghq_case_plan.py,
test/parity/core070_aghq_controls.toml, tools/core070_aghq_controls_run.jl,
docs/dev-log/core070/aghq-required-case-plan.json,
docs/dev-log/core070/aghq-required-cases.md,
docs/dev-log/core070/aghq-case-plan-evidence.json and this leaf. Ada also owns
central source-map planned links and manifest freeze integration, check-log and programme checkpoint updates.
No source engine, reference archive, existing receipts or foreign files change.

1. Write a negative test requiring every original AGHQ source fact once,
   separate control/public-fit evidence levels, retained exclusions and no
   promotion of an unbound call. Demonstrate its failure before implementation.
2. Generate a deterministic source-pinned annex: exact paired control fixtures,
   public admission and fallback obligations, family/interface obligations,
   adaptation/optimizer/inference contracts and explicit unmet dependencies.
   Existing tests are candidates, not newly passing evidence.
3. Replay the 39 original R controls and 16 native Julia normalization controls plus two explicit input-dialect distinctions.
   These are pure control checks, no fitting or simulation. R local <=30 seconds;
   Julia Totoro <=120 seconds including package startup, one thread, no allocation
   on DRAC. Fresh directories, exact pins and retained process exit/logs required.
4. Verify omitted case, stale source, missing call, helper-as-fit promotion,
   and changed fixture fail. Review source boundary with Noether independently.
5. Add planned links to the central source map only after validation, preserving
   all family links and all executable lists. Full manifest remains DRAFT.

CHECK: python3 -m unittest discover -s tools -p test_core070_aghq_case_plan.py
EXPECT: tests pass; all corruption probes rejected.
CHECK: python3 tools/core070_aghq_case_plan.py --verify-evidence
EXPECT: CORE070_AGHQ_CASE_PLAN_VERIFIED_NOT_FROZEN
Failure action: retain all outputs, repair contract/runner; never skip a required
case or mark helper evidence as a public fitted-model result. Public branches
remain required even when the current Julia route cannot express them.
