# Structured-source input qualification and capture

OWNS: new structured fixture, qualification/capture/export/verifier files and
developer contracts; parent owns programme records. No engine/foreign edits.
Frozen R b4d5fee64def88bc768dda1f1f77c29b295edd86, immutable library on Totoro.

First qualify ape/Matrix/fmesher and construct the exact deterministic mesh
with public make_mesh. Fixture file core070_structured_data.R fixes p3,
12sites/3source groups for tree/kernel; pedigree a,b founders/c,d offspring
with only c,d observed; spatial four square sites repeated across3traits.
Tree ((a:1,b:1):1,c:2); denseC=.7I+.3J; distinctK2=.6I+.4vv',v=(1,-1,1).
Mesh cutoff=.25,seed701. Qualification verifies projection dimensions/row sums,
FEM presence and retains the realized mesh and package versions. No fitting.

Then freeze the public-call inventory before capture: treeRR/denseRR/tree
propto, sparse pedigree, single kernelPsi/multikernel, spatialindep/latent/
latentPsi/dep/common. Add explicit multi-kernel unique pruning as a reference
parameter-loss diagnostic, not a validated requested model. Negative branches
must cover invalid trees/pedigrees/kernels and spatial wrapper/projection
inputs. Source transforms, observation mapping, parameter ties and random
block dimensions must be inspected from actual captured arguments.

Compute Totoro existing socket, one BLAS/OMP thread; qualification estimate
under2min,cap120s; capture estimateunder2min,cap120s. Oracle verify before/after.
No DRAC campaign or objective/optimizer in this slice. Missing dependency is
BLOCKED for the affected route, never an optional skip satisfying parity.
CHECK: python3 tools/core070_verify_structured_input.py
EXPECT: CORE070_STRUCTURED_INPUT_CONTRACT_VERIFIED
Required evidence: exact source/fixture/environment pins, all cases retained,
meaningful negative controls and fresh aggregate check. Native fits, interface
parity, identification/recovery and numerical spatial claims remain unpaid.
