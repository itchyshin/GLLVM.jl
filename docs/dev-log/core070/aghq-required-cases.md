# Stage1a required cases — current source mapping

The [machine-readable annex](aghq-required-case-plan.json) supplements the
historical [39-control inventory](aghq-control-subset.json). It is **not frozen**
and does not certify public Stage1a parity. The original archive remains unchanged.

There are 39 original helper facts: 16 now have exact paired normalization
calls, 22 still need native policy mappings, and one records the excluded
loading-ridge default. The paired calls establish control values/errors only.
R's `"auto"` maps to Julia `:auto`; whole-number R numerics map to Julia integers.
Julia rejects `9.0` and `"auto"`. These are explicit input-language distinctions,
not identical accepted types. Both additional Julia rejection checks passed.

The 32 additional public or numerical obligations account for default-off and
k1 precedence; even k2; default unique and extra random effects; absent B blocks;
predictor-informed latent scores; missing predictors; multinomial fallback;
actual graph-gate K5/K6 behavior; auto trait/affordability/family rules; ordinary
fixed predictors; Gaussian REML admission; mixed families; invalid controls;
unpenalized objectives; observed adaptation and eigenvalue repair; normalization
and refinement; frozen derivatives and convergence; multistart; unchanged-start
warnings; failed adaptation; fallback curvature choices; and fitted information.
Each has pinned source anchors, an acceptance rule, owner and required surface.
Numerical kernel checks are not artificially duplicated through R bridge APIs.

All 21 admitted family descriptors remain accounted for. Multinomial requires
matching Laplace fallback; the other descriptors require callable Stage1a domain
qualification with their own links and fixed/free nuisance parameters. The
Gaussian/Poisson/binomial candidates do not satisfy the remaining families.
A Julia unsupported result is a recorded gap, never a passing parity substitute.
Ordinary fixed predictors are not excluded by the admission chain; `b_fix` and
random-vector construction still need a matched public case. Non-Gaussian
experimental REML remains outside this programme.

Known-DGP recovery and interval coverage are separate validation designs.
Reference source code is not evidence of calibration. The binomial seed43/k5
failure and original Student-t health/density issue remain unchanged.

## Evidence and limitations

Local frozen-R replay: all39 controls passed. Totoro Julia1.12.6, one Julia/BLAS
thread: 16 normalization checks plus2 input-dialect checks passed in8.785s,
including startup. No fits or simulations ran. Retained source/process/fixture
and output pins are verified by `tools/core070_aghq_case_plan.py --verify-evidence`.

The master map gains **planned** AGHQ links; no executable entry or PASS row was
promoted. The central freeze check now also requires concrete cases for each
public/numerical and family/interface obligation. Missing calls, fixture files,
annex pin, interfaces or estimator-level evidence contracts prevent freezing.
That structural check does not replace independent semantic review or actual
fit receipts. The full known-source census is still752 overlapping facts, not
752 models; the new annex records additional obligations outside that count.

Noether (fresh Terra/high, one follow-up) found source-anchor errors and asked
for stronger graph/fixed-predictor/REML boundaries. Ada corrected the anchors,
kept source observations separate from programme acceptance requirements and
separated calibration design. Those final edits were parent-checked; there is
no new independent completion-panel signoff.

Next: bind public admission cases to exact same-model fixtures and calls, then
complete native/formula/bridge case contracts across the other source domains.
Do not spend another turn replaying these helper controls unless their inputs
or implementation change. The full programme remains ACTIVE / M1 PARTIAL.
