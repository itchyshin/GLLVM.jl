# Noether — AGHQ public identity and multistart review

Native fresh default child noether_aghq_public_contract, explicit gpt-5.6-terra,
high effort, fork_turns=none. One bounded public-source numerical design review
and one follow-up; no fits, edits, private history or runtime artifact inspection.
Parent supplied live paired measurements and owns their independent verifier.

Recommendation: extend PoissonFit with concrete optional integration metadata,
preserve positional constructors, retain final frozen caches/controls/input
identity and use objective=:fit to prevent silently rebuilding Laplace for AGHQ
inference. AD Hessian of the frozen target aligns with R sdreport; profile uses
the same target and bootstrap refits the same controls. Repair generic Wald's
insufficient positive-inverse-diagonal check with Cholesky before inverse solves.
Keep mode prediction, masks/offsets and fallback provenance explicit. No new
safety-only fallbacks or public completion claims. Numerical curvature repair
reachability remains a named obligation; mathematical Poisson H is SPD.

Parent corrected reviewer arithmetic for k9→15: -3.96228e-6, not -4.96228e-6.
The 0.0193509 omitted adaptation-chain derivative is recorded with its stable
FD check; neither reviewer nor parent interprets frozen convergence as total
stationarity. R fresh gradient1.5974e-4 meets the relative leg only.

Follow-up inspected _aghq_select_run, aghq_multistart_optimize, AM tests and
aghq-public-integration-contract.md. No remaining actionable defect in that
bounded scope. Selection filters finite usable runs, prefers converged runs,
then objective, retaining first ties. All starts are copied/validated, failures
retained, interrupts propagate, and all-unusable returns no selected fit.
Public wiring is explicitly pending. Not a completion panel or public sign-off.
