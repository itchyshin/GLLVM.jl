# Public random-slope preparation contract

OWNS: new slopes-input fixture, map extractor/checker, contract and source-case
annex; parent owns shared source mapping/checkpoint/check-log. Source reviewer
handles tree/pedigree/kernel/spatial input paths read-only. No engine edits.

Frozen R b4d5fee64def88bc768dda1f1f77c29b295edd86; 20 cases declared in
test/parity/fixtures/core070_slopes_input.R before execution. Capture actual
public MakeADFun arguments with the existing runner, no objective/optimizer.
Expected16 prepared and4 specific rejection cases; a discrepancy stops
promotion and is retained. Gaussian trait-major p3,n18,six species groups,
continuous x and x2, named dense C=.7I+.3J. Two Poisson ordinary latent cases
check default-Psi warning vs explicit unique=false; Poisson multi-slope
animal_dep is expected to reject. No non-Gaussian density/recovery claim.

The contract must distinguish ordinary augmented raw loadings over2p rows,
structured latent separate per-basis p-row raw loading blocks, dependent
log-diagonal Cholesky coordinates, independent within-trait2x2 blocks and
double-bar zero intercept/slope covariance. Pin actual random names, map
patterns, coefficient ordering, source matrix and observation design. Free
residual and unique-variance mapping must be recorded, not inferred from terms.
Rank4 ordinary is admitted against2p=6; rank4 structured is rejected againstp=3.
Full programme remainsDRAFT until exact source-to-native cases are reviewed.

Compute Totoro existing socket; preparation estimate<2min cap120s, one
BLAS/OMP thread. Verify oracle before/after. This is no scientific campaign.
CHECK: python3 tools/core070_verify_slopes_input.py
EXPECT: CORE070_SLOPES_INPUT_CONTRACT_VERIFIED
Required gate: exact case inventory, source/runtime/fixture pins, retained
failed attempts, captures, parameter maps, design/source values, warning and
negative outcomes. Full native/formula/bridge implementation and numerical
validation remain unpaid even when reference preparation passes.

## Source correction after first capture, before replay
The immutable initial20-case attempt failed5 expected outcomes. Four ordinary
indep/dep slope forms reject in the wrapper. Animal multi-slope silently
rewrites to an intercept block (use_phylo_dep_slope=0,use_phylo_rr=1,g_phy);
its theta_rr_phy has6 free intercept-loading coordinates. Retain both Gaussian/Poisson
animal calls as BLOCKED_REFERENCE_MISROUTE, never as admitted slope models.
The animal wrapper uses the single-slope LHS classifier; only phylo_dep uses
the multi-slope classifier. Add paired phylo_dep Gaussian/Poisson controls:
Gaussian should prepare9 coefficient columns/45 Cholesky parameters; Poisson
should reject its actual non-Gaussian multi-slope guard. Final22 outcomes:
12 requested models prepared,8 rejected,2 reference misroutes. No original
call removed, tolerance relaxed or R engine changed. Map inspection must flag
the2 misroutes, rather than accept every MakeADFun-boundary capture as valid.
Second-capture map inspection used R dollar partial matching, mistaking the
inactive theta_rr_phy_slope stub for theta_rr_phy. Exact double-bracket lookup
shows no invalid-length map: the genuine problem remains lost slope terms.
Third capture uses exact lookup. Both earlier archives remain unchanged.
