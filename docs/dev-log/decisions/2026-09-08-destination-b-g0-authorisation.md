# Destination B G0 authorisation receipt

**Date:** 2026-09-08

**Authority:** direct maintainer response in the Destination B execution thread
**Status:** active scope authority; not a capability qualification or release approval

## Authorised lines

The maintainer directly approved the following bounded programme lines:

1. **B1 grouping:** implement the full approved grouping programme. The prior
   B1 `unit` checkpoint remains source-alignment evidence only. Start with a
   paired Gaussian shared-`unit` fit, then `unit_obs`, `cluster`, and
   `cluster2`.
2. **R `phylo_rr` S3b:** proceed only as an R bridge adapter/test line. No
   gllvmTMB C++ or likelihood-engine change is authorised.
3. **S4:** run one Gaussian paired public-workflow validation only after the
   S3b consumer works.
4. **Dense `vcv`:** use the canonical R-ridged-once precision contract:
   retain the original condition number and warning; transport canonical
   precision, determinant, node mapping, and scale; do not independently
   invert in Julia.
5. **FRK:** remains parked at `gllvmTMB#1275`.

## Immutable limits retained

- The paired reference remains `gllvmTMB` 0.7.0 at
  `b4d5fee64def88bc768dda1f1f77c29b295edd86`.
- Public wording remains: experimental partial R-to-Julia bridge, not 0.7
  parity.
- No push, merge, release, registry action, 0.7.1 feature expansion, coverage
  campaign, generic bridge admission, or unlisted capability is authorised.
- R changes are limited to the scoped adapter/tests/documentation line; R/C++,
  its likelihood, and the frozen reference build remain protected.
- Every capability still requires its own symbolic alignment, test-first
  implementation, paired evidence where applicable, interval diagnostics,
  recovery evidence, Unlazy gate, and independent review before qualification.

## Execution consequence

This receipt unblocks the first three independent leaves only:

```text
1. Grouping engine: paired Gaussian shared-unit fit, then the four-level model.
2. Phylogenetic consumer/adapter: tree, pedigree-with-ancestors, dense-vcv
   transport/consumer under the one-inversion contract.
3. Independent fixtures: term-by-term symbolic mapping, negative controls, and
   recoverable interior interval/recovery designs.
```

S4 stays downstream of the phylogenetic consumer; non-Gaussian grouping,
recovery campaigns, and public qualification remain gated by their separate
evidence.

## Non-claims

This authorization does **not** establish that any Destination B row works,
has paired R--Julia parity, returns a valid interval, recovers a known truth,
or is ready for public use. It changes authority, not evidence.
