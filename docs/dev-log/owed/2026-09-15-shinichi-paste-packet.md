# Shinichi Paste Packet: True-Parity Gates

Paste these in chat as separate lines, in this order if you want the highest leverage first.

## 1. Delta dispersion policy

```text
accept delta dispersion A
```

Consequence: Aligns the default Delta twin path to R per-trait dispersion and authorises a bounded follow-up to wire `:species` CIs/cells and remeasure D1 without widening tolerances.

Alternatives, if you do **not** want A:

```text
accept delta dispersion B
```

Consequence: Keeps Julia shared dispersion as default and permanently fences default Delta D1 / first-order point-parity promotion.

```text
accept delta dispersion C
```

Consequence: Declares Delta second-order matched-theta OUT and keeps each-own-optimum receipts only; it does not resolve the dispersion mismatch by itself.

```text
accept delta dispersion C+B
```

Consequence: Combines each-own-optimum-only tier labelling with the permanent shared-default D1 fence.

## 2. Totoro D-139, issue #323 Track A

```text
ack Totoro D-139 #323 Track A
```

Consequence: Authorises Codex/Totoro to run the full CI-mirror frozen-R smoke path for #323 under the recorded ~90-150 min D-139 estimate; it does not change Delta, D3, S4, or `Project.toml`.

## 3. D3 `loading_profile` Stage 1

```text
G0 Stage 1
```

Consequence: Authorises the Stage 1 engineering slice after Stage 0: public `loading_profile` surface, fitter pin support, ledger bind, and joint D3 wording.

## 4. S4 public-formula probe

```text
S4 probe yes
```

Consequence: Gives the second explicit yes to run the public-formula S4 probe against the recorder on gllvmTMB #1283 / `97214679c`; it does not authorise R engine edits.

## Ranked Paste Order

1. `accept delta dispersion A`: highest leverage; resolves the current Delta policy fork and opens the path to honest D1 remeasurement.
2. `ack Totoro D-139 #323 Track A`: largest measured destination-gate movement, but spends Totoro time.
3. `G0 Stage 1`: unblocks the missing `loading_profile` surface after Stage 0.
4. `S4 probe yes`: unblocks the held public-formula probe once Shinichi is ready to spend the second explicit yes.

