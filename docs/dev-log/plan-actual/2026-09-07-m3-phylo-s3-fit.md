# Plan vs actual — M3-PHY-S3-FIT PrecisionPhy Gaussian fit

Plan: parent brief M3-PHY-S3-FIT (Destination B hygiene; diagnostic only)  
Unlazy: `.unlazy/m3-phy-s3-fit/GATES.md`  
Worktree: `~/local-scratch/lanes/GLLVM.jl-m3-phy-s3-fit-20260907`  
Branch: `cursor/m3-phy-s3-fit-20260907`  
Base: `origin/main` @ `340f3832`

## Planned

Make the admitted `PrecisionPhy` payload actually fit on Julia:
`fit_phylo_gaussian(::PrecisionPhy)` on the S3a 8-tip fixture. Finite-diff
L-BFGS if sparse Cholesky blocks AD. Compare σ² and log-likelihood to the
existing tree path at ≤ 1e-8. Optional later in the same leaf:
`bridge_fit(; phylo=payload)`.

## Actual

| Item | Planned | Actual |
|---|---|---|
| Fitter | `fit_phylo_gaussian(::PrecisionPhy)` | Shared FD L-BFGS loop; PrecisionPhy builds `PrecisionPhyFitState` from `Q` + `species_aug_id`. |
| Gradient | finite-diff if CHOLMOD blocks AD | `autodiff = :finite` unchanged. |
| Compare | σ² and loglik ≤ 1e-8 | abs Δ = 0.0 on μ, σ²_phy, σ²_eps, negll; interior nll Δ = 0.0. |
| Bridge hook | optional | Thin `bridge_fit(; phylo=payload)` consume hook; `diagnostic_only = true`. |
| Receipt | diagnostic only | `docs/dev-log/core070/phylo-transport-s3-julia-fit.md` |
| Tests | existing file or small new file | Added S3a testset to `test/test_fit_phylo.jl` (17 new assertions). `runtests.jl` already includes that file. |

## Deviations

1. **Shared optimizer extraction.** The AugmentedPhy method now calls
   `_fit_phylo_gaussian_lbfgs` so both representations share one loop. Behaviour
   of the tree path is unchanged (existing 13 tests still pass).
2. **`family` default on `bridge_fit`.** Required unless `phylo` is set; the
   non-phylo path still throws `family is required`.
3. **Collapsed MLE σ²_phy.** The 8-tip one-replicate draw used for the fit
   comparison sits on the phylo-variance boundary. Recorded honestly; interior
   matched-parameter nll is the non-boundary oracle.

## Not done / next

- R-side `phylo_rr` gate lift (S3b) — not authorised.
- M2-R2, NB2 A11 promotion, true-parity / public claim — fenced.
- Full `Pkg.test()` left to CI Julia shards.

## Verdict

Local S3-FIT is **PASS** on the Julia PrecisionPhy-vs-tree oracles.
Programme status remains diagnostic-only; NB2 A11 stays **partial**.
