# Plan vs actual — M3-PHY-S3a Julia precision-payload admission

Plan: `docs/dev-log/plans/2026-09-06-p3-next-m2-m3-slice-brief.md` (PR #308)  
Unlazy: `.unlazy/true-parity-remainder-20260906/gates/m3-phy-s3a-*.md`  
Worktree: `~/local-scratch/lanes/GLLVM.jl-m3-phy-s3a-20260906`  
Branch: `cursor/m3-phy-s3a-20260906`  
Base: `origin/main` @ `8692b639` (rebased after #297/#304/#306/#308/#309)

## Planned

Admit the R-convention `PrecisionPhy` bundle as a JuliaCall-flat payload;
validate dimensions/indices/tip map/labels/finiteness/log-det checksum;
replay against the S1 `AugmentedPhy` fixture at 1e-8. No R engine edit,
no `phylo_rr` lift. Does not claim v0.true-parity.

## Actual

| Item | Planned | Actual |
|---|---|---|
| Payload schema | `src/bridge.jl` | Implemented in `src/phylo_precision.jl` because `src/bridge.jl` was leased by #307. Same public names. |
| Validation | named failures | Six `GJL-GATE-PHYLO-PAYLOAD-*` tags, red-first then green (69 tests). |
| Replay | ≤ 1e-8 vs `AugmentedPhy` | abs/rel Δ = 0.0 at three σ²_phy points. Local suite now 78 tests (Dict, kwargs, empty label, triplet length). |
| Receipt | diagnostic only | `docs/dev-log/core070/phylo-transport-s3a-julia-payload.md` |
| Wiring | not listed | Export line in `src/GLLVM.jl`; `_shard_include` in `test/runtests.jl`. |

## Deviations

1. **`src/bridge.jl` delayed, then moved.** The first commits kept the API in `phylo_precision.jl` while #307 held the lease. After the lease expired, assembly/validation moved into `src/bridge.jl` as the brief asked.
2. **`src/GLLVM.jl` and `test/runtests.jl` edited.** Not in the brief's owned list; required so the new names export and the new test file is sharded. No other shared files.
3. **check-log not appended.** That path is in the same #307 lease.

## Not done / next

- Thin `bridge.jl` wrappers once #307 releases the lease.
- R-side `phylo_rr` gate lift (S3b/S4) — separate authorisation.
- Unlazy `--approve` / `--reverify` after the receipt lands.
- Rebase onto main if #297 or #307 merge.

## CI (PR #310, after rebase)

Required checks: Documenter pass; Julia 1.10 shards 1–4 pass; Julia 1 shards 1–4 pass.
Advisory frozen-R family smoke failed on pre-existing NB2 / Student-t cells (278 pass / 8 fail), not on this payload slice. Run conclusion: success.

## Verdict

Local S3a admission is **PASS** on the Julia payload/validation/replay
oracles. Programme status remains diagnostic-only; NB2 A11 stays **partial**.
