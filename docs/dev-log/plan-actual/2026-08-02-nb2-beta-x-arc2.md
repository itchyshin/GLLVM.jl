# Plan vs actual — NB2/Beta+X Arc 2 (2026-08-02)

**Plan:** `docs/dev-log/plans/2026-08-02-nb2-beta-x-arc2-ultra-plan.md`  
**Lane:** `nb2-beta-x-arc2-20260802`  
**Platform:** Cursor

## Material axes

| Axis | Planned | Actual | Tag |
|---|---|---|---|
| Trigger | Start only after #175 merges | Confirmed merge @ `9f5133a7` before S0 | — |
| Scope | NB2+X/Beta+X light logLik cells, twin B under X | 2/2 cells landed, no engine changes | — |
| Evidence | LOG ΔlogLik rtol 1e-6 | NB2+X Δ=1.29e-8, Beta+X Δ=4.29e-9; both ≤ rtol | — |
| Twin | Reuse `/tmp/R-gllvmtmb-x-parity-20260802` R lib | Reused as-is (gllvmTMB 0.6.0, installed 06:24); source tip `ab49638b` | — |
| DGP repair budget | Fisher-reserved one repair round per cell | Used exactly once per cell (both were Heywood-like boundary failures, not noise) | adaptive |
| Safety | No tolerance widening | Held at rtol 1e-6 throughout | — |
| Verification | `runparity.jl` + full `Pkg.test()` | 34/34 shared-X cohort; full suite 5096 pass / 1 broken (pre-existing) / 0 fail, 55m21.7s | — |
| Claims | Light logLik, twin `disp.group`, not full parity | Rose fence recorded in after-task | — |
| Handoff | LOOP kit + plan-actual + push/PR gated on owner ask | Written; owner said "please allow" → push/PR executed this turn | — |

## Adaptive deviation

Both cells needed one DGP repair round (within the plan's budgeted allowance):

- NB2+X: `K=2, r_true=6.0, n=60` → one trait's per-trait `r` ran to ~1e7
  (near-Poisson), and R's optimizer landed on a different local optimum
  (`Δ logLik = 0.645`, not a numerical-precision gap). Repaired to `K=1,
  r_true=1.5, n=120`, milder loadings.
- Beta+X: `φ_true=12.0, n=60` → one trait's per-trait `φ` ran to ~3.5e5
  (near-degenerate); R reported "false convergence (8)". Repaired to
  `φ_true=8.0, n=80`, milder loadings.

Neither repair touched `rtol` — both are DGP identifiability fixes per the
repo's no-silent-tolerance-widening rule.

## Drift

None material against the frozen ultra-plan. Push/PR was gated on an explicit
owner ask ("please allow") in a follow-up turn, per the plan's closeout gate;
executed in that follow-up rather than in the same turn as implementation.
