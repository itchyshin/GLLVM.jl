# truncated_nbinom2 Identity→Engine — APPROVED PLAN (G0 LOCKED · Ada defaults)

**G0 status:** APPROVED 2026-08-15 by Shinichi (“autonomous keep-going sounds good”) — **Ada defaults LOCKED** unless a genuine fork appears.  
**PLATFORM:** Cursor (this planning session; do not invent Codex as executor).  
**Execute via:** keep-going conductor [`7b6ad608`](7b6ad608-93e6-41c1-99b1-65ff15559437) (owns #205 merge-on-green → this arc). Fresh `/goal` only if keep-going is dead / handed off.  
**Do not** re-open G0; **do not** race keep-going engine files; this artifact is plan-only.

Locked Ada defaults:
1. HEADLINE = truncated_nbinom2 Identity→Engine (clone truncated_poisson pattern)
2. Compute = local tiny (+ light RCall only if twin cell is cheap)
3. Twin admits truncated_nbinom2 (fid 11) → light Δ **allowed**, not required to land engine
4. Engine Arc1 dispersion = **shared scalar `r`** (Julia `r` ≡ twin `φ`); Identity documents twin’s **per-trait** `log_phi_truncnb2` as the twin default; per-trait fitter = Arc1b / OWED if light Δ needs it
5. Rose fences below — hard STOP on ZIP/ZINB invent-Δ, Phylo #127, ADEMP, silent rtol

---

```
🎯 GOAL
Solo platform: Cursor (this session = plan; execute = keep-going 7b6ad608 on post-#205 main WT). Do not invent Codex as executor. Dropbox fork claude/jl-bridge-capabilities-20260619 PROTECTED — never write.

Deliverable: ACCEPTED truncated_nbinom2 Identity + TruncatedNegBin2 (zero-truncated NB2) Laplace engine + focused tests + capability-status `truncated_nbinom2` planned→implemented + after-task / check-log / Rose fence.

HEADLINE: Land twin-aligned zero-truncated NB2 (fid 11) Identity→Engine by cloning truncated_poisson (2026-08-15) + HurdleNB positive-block score/weight, flipping one ledger chip.

IN PARALLEL (cheap): twin fid-11 dens cites; reuse TruncatedPoisson / HurdleNB / NBFit packing patterns; board START HERE refresh; MC julia_surface next_safe PROPOSE only (do not overwrite R MSPL primary).

DEFER / FENCE: invent ZIP/ZINB twin Δ; Phylo Model A / #127; ADEMP/coverage; silent rtol widen; truncated_nbinom1 / delta_truncated_*; hurdle re-open; confint for truncated families; X/+cov variants; race-merge #205; git add -A; Dropbox protected checkout writes; force-push.

DISCIPLINE: verify = Identity twin cites + FD ≤1e-6 + focused suite tally + ledger token parse + Rose claim fence · compute = local tiny (Totoro/DRAC N/A unless asked) · closure = after-task + check-log + board + Melissa reconcile + STOP (or hand Arc1b).
```

**ARC PROGRAM:** size · **~6–9 h** · Arc 0 = await/confirm #205 MERGED + Identity ACCEPTED · Arc 1 = engine + tests + ledger flip · see Arc Card.

**Mode note:** Phases 0–2 plan-only. Phase 3+ owned by keep-going (or paste-ready `/goal` if that lane dies).

---

## Part A — Arc Card

## ARC CARD — truncated_nbinom2 Identity→Engine

**Mode:** size  
**Requested outcome:** `truncated_nbinom2` Status `planned` → `implemented` with Identity lock + Laplace engine + focused FD/recovery-smoke tests  
**Mechanism authority:** Cursor keep-going lane post-#205; local tiny + optional light RCall; docs + `src/families/` + `test/`; **no** ADEMP; **no** invent ZIP/ZINB twin Δ; **no** Phylo #127; **no** Dropbox stale-fork writes  
**Recommended arc:** **~6–9 hours** (range 5–10)  
**Time contract:** ceiling ~10 h; close after ledger flip + after-task even if light RCall deferred  
**Estimate confidence:** **measured** (truncated_poisson Identity ~docs + engine focused 10/10 + FD ≤1e-6 in catch-up Rung 3–4) + **inferred** for NB2 truncation score/weight + shared-`r` pack  
**Arc 0 outcome:** #205 MERGED (or tip already on `origin/main`) + ACCEPTED Identity decision  
**State transition:** capability-status `truncated_nbinom2: planned` → `implemented` (shared-`r` Julia engine; twin per-trait noted)  
**Executable rung and evidence:** Identity decision cites twin fid 11 dens; engine FD ≤1e-6; y=0 reject; smoke fit + `fit_gllvm` dispatch; ledger flip; Rose fence

### Capacity ladder

| Order | Budget | Outcome | Trigger / definition of done |
| --- | ---: | --- | --- |
| Arc 0a | 15–45 min (+ CI wait) | #205 merge-on-green recorded; fresh WT off post-merge `origin/main` (or resume keep-going WT) | `gh pr view 205` MERGED; Dropbox fork untouched |
| Arc 0b Identity | 60–90 min | ACCEPTED `docs/dev-log/decisions/2026-08-15-truncated-nbinom2-identity.md` | Twin cites + Julia estimand table + Rose fence; **STOP before engine only if Identity alone** — this programme CONTINUES |
| Arc 1 Engine | 4–6 h | `src/families/truncated_nbinom2.jl` (+ wire exports / `fit_gllvm`) + `test/test_truncated_nbinom2.jl` + Status flip | FD ≤1e-6; focused Pass tally; y≥1 reject; Λ=0 exact vs truncated NB2 ll |
| Arc 1b (contingent) | 1–2 h | Light RCall Δ **or** per-trait φ path | Only if twin cell cheap **and** Arc 1 early; else OWED |
| Integrate/close | 30–45 min | check-log + after-task + board + Melissa | STOP; Actuals filled |
| **Total** | **~6–9 h** | | |

### Budget (Identity + Engine core)

| Segment | Minutes | Output / stop point |
| --- | ---: | --- |
| Orient / #205 | 20–40 | Merge SHA; WT on post-#205 main; lane receipt |
| Identity core | 45–60 | Decision cloned from truncated_poisson Identity; lock estimand + shared-`r` Ada default |
| Engine core | 180–300 | Marker + score/weight/logpdf + fitter + pack `log r` + tests |
| Verify | 40–60 | Focused suite + FD gate; optional full `Pkg.test` if time |
| Repair reserve | 30–45 | FD / mode / log(1−p0) numerics |
| Closeout | 30 | ledger + after-task + check-log + board + Actuals |
| **Total** | **~6–9 h** | |

**In scope:** Identity ACCEPTED; zero-truncated NB2 Laplace fitter (shared `r`); focused tests; capability-status flip; board/check-log/after-task; optional light RCall if twin admits cell is cheap.  
**Not in this arc:** ZIP/ZINB invent-Δ; Phylo #127; ADEMP/coverage; truncated_nbinom1; delta_truncated_*; truncated confint; X/cov; hurdle redesign; silent rtol; Dropbox protected branch; race-merge #205.  
**Evidence used (Phase 0.25):**
- Catch-up after-task: next_safe = truncated_nbinom2 Identity→Engine; `truncated_nbinom2` still `planned`; truncated_poisson = `implemented`
- Clone: `docs/dev-log/decisions/2026-08-15-truncated-poisson-identity.md` + `src/families/truncated_poisson.jl` + `test/test_truncated_poisson.jl`
- Twin `gllvmTMB` Dropbox: `R/enum.R` `truncated_nbinom2 = 11L`; `R/families.R` log-link only + truncated-mean `linkinv`; `src/gllvmTMB.cpp` fid==11: `dnbinom_robust − logspace_sub(0, log_p0)` with per-trait `log_phi_truncnb2`; registry **partial**
- Julia reuse: `HurdleNB` positive block (`p0=(r/(r+μ))^r`, `μ_tr`, `W^c`); `NBFit` / `fit_nb_gllvm` pack pattern for `log r`
- Keep-going `7b6ad608`: owns #205 → this arc (do not double-merge; do not parallel-write `src/families/truncated_nbinom2.jl`)

**Risk branch:** If Identity twin scale (`φ` vs Julia `r`) mismatches, STOP after Identity + diagnosis — no silent rtol. If shared-`r` light RCall fails against twin per-trait default, do **not** widen atol — OWED Arc1b per-trait or gate parity. If #205 red >1 session, keep Identity docs-only on catch-up tip; defer engine until main has truncated_poisson.

**Done when:** Identity ACCEPTED + engine focused green (FD ≤1e-6) + Status `truncated_nbinom2` = `implemented` + after-task + Rose OK/fence list.  
**First action (execute owner):** Poll `#205`; on MERGED, `git fetch origin main` + worktree/branch for this arc; write Identity; then engine. **Plan writer does not execute.**

### Actuals (complete at close)

**Recommended / actual:** 6–9 h / ~2–3 h wall · **Requested / used:** autonomous keep-going / keep-going conductor · **Rungs completed:** S1–S5 (S0 #205 merge pending CI)  
**Under-run event:** HurdleNB + truncated_poisson clone shortened engine  
**Calibration:** Dual-typed TruncatedNegBin2{T} one repair; FD 1.12e-7  
**Metric movement:** planned −1 / implemented +1 (`truncated_nbinom2`)  
**Result:** focused 11/11 Pass; ledger flip; after-task written · **Next arc:** Arc1b / REML OWED / truncated confint (post-#205 PR)

**HAND TO EXECUTE:** keep-going conductor `7b6ad608` — CONTINUE (do not spawn competing `/goal` unless that agent is dead). Fresh `/goal` paste block in Part C if needed.

---

## Part B — Ultra Plan (Phases 0–2)

### Phase 0.2 — Shannon lane preflight receipt

```
PLATFORM: cursor | ON BRANCH (Dropbox checkout): claude/jl-bridge-capabilities-20260619 (PROTECTED — never write)
LANE THIS PLAN TAKES: docs-only plan artifact under catch-up WT
  path: .worktrees/gllvmjl-capability-catchup-20260815/docs/dev-log/plans/2026-08-15-truncated-nbinom2-identity-engine.md
OTHER LANES: cursor/capability-catchup-20260815 (PR #205 OPEN, CI pending at plan time);
  keep-going 7b6ad608 (merge #205 → truncated_nbinom2 execute);
  main-direct (3 non-merge commits / 12h — do not claim);
  worktree×44 census
VERDICT: FOREIGN LANE ACTIVE (direct-to-main) + live cursor catch-up lane
  → this session claims PLAN FILE ONLY; does not touch src/ test/ or merge #205
COMMAND: ~/shinichi-brain/tools/lane_preflight.sh "/Users/z3437171/Dropbox/Github Local/GLLVM.jl"
```

### Phase 0.25 — Prior-work sweep receipt (gate for Phase 1)

| Surface | Evidence ran | Finding | Call |
| --- | --- | --- | --- |
| **repo git state** | `git worktree list`; catch-up WT `@ b2b99463` `cursor/capability-catchup-20260815`; `gh pr view 205` → OPEN, CI pending; `branch_drift_check` on Dropbox fork → 137 ahead / 449 behind (PROTECTED); `origin/main` has **no** `truncated_*` yet (lives on #205 tip) | truncated_poisson Identity+engine on #205 tip; truncated_nbinom2 **not** started in `src/` | **resume** post-#205 via keep-going; **build-the-gap** = nbinom2 Identity+engine |
| **twin / sister** | Dropbox `gllvmTMB` `R/families.R` L347–369; `R/enum.R` L17; `src/gllvmTMB.cpp` L823–826, L2285–2294; `docs/design/02-family-registry.md` Truncated nbinom2 = **partial** | Twin **admits** truncated_nbinom2 (fid 11); per-trait `log_phi_truncnb2`; y≥1 | **reuse** twin estimand; light RCall Δ allowed |
| **brain** | MCP `search_notes` q=`truncated_nbinom2 truncated_poisson GLLVM Identity engine` `search_all_projects:true`; greps: `memory/DECISIONS.md` (drmTMB family counts mention truncated_nbinom2); `AGENT_LOG`/`OPEN_QUESTIONS`/`journal` → empty for GLLVM truncated_nbinom2; catch-up after-task names next_safe | drmTMB has truncated_nbinom2 history (different package); GLLVM.jl next_safe already points here | **reuse** twin/drmTMB estimand intuition; **build** GLLVM.jl Identity+engine gap |
| **deterministic log greps** | `grep -in truncated_nbinom\|truncated_poisson memory/{AGENT_LOG,DECISIONS,OPEN_QUESTIONS}.md`; `grep -rin truncated_* journal/`; `grep -in truncated projects/deep-research/README.md` | No GLLVM.jl Identity decision yet; DECISIONS only drmTMB census hit | **build-the-gap** |
| **code inventory** | `rg truncated_nbinom` → no Julia engine; `truncated_poisson.jl` + tests exist on catch-up tip; `HurdleNB` positive block formulas in `twopart.jl` | Clone substrate ready | **co-opt** TruncatedPoisson + HurdleNB + NBFit |
| **Verdict** | | Genuinely new = GLLVM truncated_nbinom2 Identity lock + Laplace engine + ledger flip | **reuse** pattern + twin cites; **resume** keep-going after #205; **build** nbinom2 gap only |

### Phase 0.3 / 0.3b — Model roster + two-bar

- PLATFORM Cursor; scout/recon → Composer/Grok (**Cursor Models**); Identity/engine judgment → Auto Cost / pinned Claude-Sonnet (**Other Models**); orchestrate/verify close → Claude Opus if needed.  
- Phase 0.3b Usage glance: **AGENT-INFERRED** — CLI cannot read Settings → Usage; keep-going should glance both bars before long engine slice (~3%/day Cursor Models heuristic).  
- `ULTRA EFFORT: no` · `LUNA SUITABILITY: yes` for recon/mechanical verify only (if Codex child used; Cursor parent does not require Luna).

### Phase 0.4 — Dialogue (Ada defaults LOCKED)

Shinichi already approved autonomous keep-going. No new interview unless a genuine fork appears.

#### WHAT THE BRAIN / REPO ALREADY KNOWS
- truncated_poisson Identity ACCEPTED + engine landed on #205 tip; next_safe = truncated_nbinom2.  
- Twin fid 11 admits zero-truncated NB2 with per-trait φ.  
- HurdleNB already encodes zero-truncated NB2 count score/weight for shared `r`.

#### WHAT SHINICHI TOLD US
- Autonomous keep-going OK; ultra-plan for truncated_nbinom2 Identity→engine (~6–9 h) wanted.  
- G0 = Ada defaults unless genuine fork.

#### WHAT THE TEAM RAISED
```
TEAM RAISED
  Fisher — twin admits fid 11 · light Δ possible but per-trait φ vs shared-r is the parity trap · recommend Identity documents twin per-trait; Arc1 shared-r for speed · Q: per-trait in Arc1? · default: shared-r Arc1, Arc1b OWED
  Rose   — ≠ invent ZIP/ZINB Δ ≠ Phylo #127 ≠ ADEMP ≠ silent rtol · claim = Julia Laplace + Identity cite; light Δ optional · Q: none if fences hold · default: fence list in after-task
  Gauss  — clone TruncatedPoisson FD outer + HurdleNB μ_tr/W^c; watch log(1−p0) underflows · recommend FD ≤1e-6 gate · Q: analytic grad now? · default: FD outer like truncated_poisson
  Ada    — LOCK shared-r Arc1; Identity cites twin per-trait; keep-going owns execute; plan-only this lane
```

#### ADA'S RECOMMENDATION / DECISIONS LOCKED
1. Identity ACCEPTED → CONTINUE engine (same programme).  
2. Arc1 = shared scalar `r` (pack `log r`).  
3. Light RCall optional; if fails vs twin per-trait, OWED Arc1b — no rtol widen.  
4. Execute = CONTINUE keep-going `7b6ad608` (not a competing `/goal` unless dead).

#### QUESTIONS STILL OPEN (pause only if hit)
- Genuine fork: must Arc1 ship per-trait φ to claim twin-default parity in the same PR? **Ada default = no** (shared-r sufficient for `implemented` with honest Notes).  
- Full `Pkg.test` before PR vs focused-only + CI: keep-going judgment; prefer focused + full if wall-clock allows (~55 min).

### Phase 0.5 — Grounded search
**Offered, not required:** NotebookLM not needed — twin dens + in-repo HurdleNB formulas are load-bearing. Inline twin cite sufficient. SEARCH: inline twin/repo only.

### Phase 1 — Decompose

**Julia estimand (Identity lock — draft for decision doc):**

| Item | Lock |
| --- | --- |
| Support | `{1,2,…}` — reject `y=0` fail-loud |
| Link | log only |
| Mean | untruncated `μ = exp(η)` |
| Dispersion | Julia `r` with `Var = μ + μ²/r` ≡ twin `φ`; **Arc1 fitter = shared scalar `r`**; twin default = per-trait `log_phi_truncnb2` (document; Arc1b OWED) |
| Log-pmf | `log NB2(y; μ, r) − log(1 − p0)`, `p0 = (r/(r+μ))^r` |
| Score / weight (log link) | reuse HurdleNB count formulas: `μ_tr = μ/(1−p0)`; `s = y − μ_tr`; `W = (V+μ²)/(1−p0) − μ_tr²`, `V = μ + μ²/r` |
| Twin light Δ | allowed (partial); not required for engine land |

**Slices:**

| ID | In → Out | Dep |
| --- | --- | --- |
| S0 | #205 MERGED SHA + WT on post-merge main | — |
| S1 Identity | decision md ACCEPTED | S0 (or catch-up tip if merge lag + poisson already present) |
| S2 Engine | `truncated_nbinom2.jl` + GLLVM.jl include/export + `fit_gllvm` dispatch | S1 |
| S3 Tests | `test/test_truncated_nbinom2.jl` (+ runtests include) | S2 |
| S4 Ledger | capability-status flip + Notes | S3 |
| S5 Close | check-log + after-task + board + Melissa | S4 |
| S6 optional | light RCall / Arc1b per-trait | S3 early |

PARALLEL: twin cite harvest ∥ WT orient (before S1). SEQUENTIAL: S0→S1→S2→S3→S4→S5.

### Phase 2 — Slice table (runnable)

| Slice | Member | Model+effort | Bar | Dispatch | Time | Detail | Dep |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RECON | Shannon / Scout | Composer low | Cursor Models | Cursor Task | 15–30m | #205 status; twin dens lines; file inventory | — |
| S0 Merge-gate | keep-going | Composer med | Cursor Models | same agent | wait+merge | merge-on-green only if still OPEN; record SHA | RECON |
| S1 Identity | Noether + Rose | Sonnet med / Auto Cost | Other Models | keep-going | 60–90m | `docs/dev-log/decisions/2026-08-15-truncated-nbinom2-identity.md` | S0 |
| S2 Engine | Gauss + Curie | Composer/Sonnet high | Cursor + Other | keep-going | 4–6h | `src/families/truncated_nbinom2.jl`; wire module + fit_gllvm | S1 |
| S3 Tests | Curie | Composer high | Cursor Models | keep-going | in S2 | focused FD ≤1e-6; y=0 reject; smoke fit | S2 |
| S4 Ledger | Ada / Rose | Composer low | Cursor Models | keep-going | 20m | Status → implemented; Notes = shared-r / twin per-trait | S3 |
| MECHANICAL-VERIFY | Scout | Composer/Grok low | Cursor Models | Task | 15m | greps: export, Status token bare, test include | S4 |
| VERIFY judgment | Rose | Sonnet/Opus med-high | Other Models | Task | 20–30m | claim fence; FD receipt; no silent rtol | S3–S4 |
| RECONCILE | Melissa | Sonnet med | Other Models | Task | 15m | `docs/dev-log/plan-actual/2026-08-15-truncated-nbinom2-identity-engine.md` | close |
| S5 Close | Ada | Composer med | Cursor Models | keep-going | 30m | after-task + check-log + board STOP/START | VERIFY |

**FAN-OUT BUDGET:** checkpoint=`truncated-nbinom2-20260815` · new children ≤4/6 · scout=1 · build=2 · ceiling=0–1 (Rose only if claim fights) · reuse=keep-going  
**LUNA SUITABILITY:** yes — RECON + MECHANICAL-VERIFY (if any Codex child); Cursor may fold into keep-going.  
**ULTRA EFFORT:** no  
**CONTEXT BRAKE:** parent planning chat should STOP after this artifact; execute in keep-going / fresh `/goal`  
**COMPACTIONS:** plan chat = close after write · execute lane separate  
**LANE RECEIPT:** **CONTINUE keep-going `7b6ad608`** · reason = Shinichi autonomy + agent already owns #205→nbinom2 · next-task prompt = Part C only if keep-going dead  
**D-43 PANEL:** milestone = ledger flip `implemented` · fire once after focused green · 2 build + 1 ceiling if claiming public capability in PR body  
**ESTIMATE:** ~6–9 h wall · 1 keep-going session (+ optional Rose verify) · fits one day with #205 wait overlapping  
**PREFLIGHT:** see Phase 0.2  
**REVIEW (plan):** Rose — receipt non-vacuous ✅; Fisher/Gauss — shared-r Ada default recorded ✅  
**VERIFY:** focused suite Pass tally + max|FD|≤1e-6 + Status parse + Rose fence  
**CONSOLIDATE:** this plan Actuals + after-task + capability-status + board  
**RECONCILE:** Melissa required at close → `docs/dev-log/plan-actual/2026-08-15-truncated-nbinom2-identity-engine.md`

### Fences (Rose)

- ≠ invent ZIP/ZINB twin light Δ  
- ≠ Phylo Model A / issue #127 public intervals  
- ≠ ADEMP / coverage campaign  
- ≠ silent rtol / atol widen  
- ≠ truncated_nbinom1 / delta_truncated_* in this arc  
- ≠ Dropbox protected fork writes; never `git add -A`  
- ≠ race-merge #205 (keep-going sole merger if still open)  
- Claim text: Julia Laplace engine + Identity twin cite (fid 11); light RCall optional; shared-`r` vs twin per-trait must be honest in Notes

### DONE when

1. Identity decision ACCEPTED on disk with twin cites.  
2. `TruncatedNegBin2` / `fit_truncated_nbinom2_gllvm` (names may match house style) + exports + `fit_gllvm` dispatch.  
3. Focused tests green; packed NLL ForwardDiff vs central FD ≤ 1e-6.  
4. `docs/design/capability-status.md`: `truncated_nbinom2` → `implemented` (bare token).  
5. check-log + after-task + board update.  
6. Rose OK or explicit remaining blockers.  
7. Melissa plan-actual written.

---

## Part C — Paste-ready `/goal` (only if keep-going is dead)

```
/goal truncated_nbinom2 Identity→Engine (~6–9h)

PLATFORM: Cursor
PLAN: .worktrees/gllvmjl-capability-catchup-20260815/docs/dev-log/plans/2026-08-15-truncated-nbinom2-identity-engine.md
  (after #205 MERGED: copy/resume on a fresh WT from origin/main)

G0: APPROVED — Ada defaults LOCKED (shared-r Arc1; twin per-trait documented; local tiny; light RCall optional)

START:
1. gh pr view 205 — if OPEN wait green then merge; if MERGED record SHA
2. Worktree from post-merge origin/main (never Dropbox protected fork)
3. Write Identity decision (clone truncated_poisson Identity; lock estimand table)
4. Engine clone truncated_poisson.jl + HurdleNB positive-block formulas; pack log r
5. test/test_truncated_nbinom2.jl — FD≤1e-6; y=0 reject; smoke fit
6. Flip capability-status; after-task; check-log; board; Melissa reconcile

FENCE: ≠ ZIP/ZINB invent-Δ ≠ Phylo #127 ≠ ADEMP ≠ silent rtol ≠ git add -A
COMPUTE: local tiny
STOP: after ledger flip + Rose fence (Arc1b OWED OK)
```

**Execute routing verdict:** **CONTINUE via keep-going `7b6ad608`** — do **not** launch a competing `/goal` while that agent is alive. Use Part C only on handoff/death.

---

## File targets (execute lane)

| Path | Role |
| --- | --- |
| `docs/dev-log/decisions/2026-08-15-truncated-nbinom2-identity.md` | Identity ACCEPTED |
| `src/families/truncated_nbinom2.jl` | Engine |
| `src/GLLVM.jl` | include + export |
| `src/families/fit_gllvm.jl` | dispatch |
| `test/test_truncated_nbinom2.jl` | focused suite |
| `test/runtests.jl` | include if required |
| `docs/design/capability-status.md` | Status flip |
| `docs/dev-log/check-log.md` | entry |
| `docs/dev-log/after-task/2026-08-15-truncated-nbinom2-identity-engine.md` | closeout |
| `docs/dev-log/plan-actual/2026-08-15-truncated-nbinom2-identity-engine.md` | Melissa |

Clone references (read-only):  
`docs/dev-log/decisions/2026-08-15-truncated-poisson-identity.md` · `src/families/truncated_poisson.jl` · `test/test_truncated_poisson.jl` · `src/families/twopart.jl` (`HurdleNB`) · twin `gllvmTMB` fid 11 dens.
