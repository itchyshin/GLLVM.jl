# Destination B — approved execution programme

Status: ACTIVE; implementation authorised by Shinichi on 2026-09-07. No capability qualified by this document.

Current execution checkpoint (2026-09-07): local commits89eac4c1 andb240aa8f
retain grouped/phylogenetic development routes and reviewed precision repairs.
The first real frozen0.7.0 Gaussian dense-vcv cross-evaluation passes at matched
and R-fitted points; independent fitting/interval assessment is the active
parallel slice. This is not S3b/S4 or programme completion. Detailed current
state and receipts are in `../recovery-checkpoints/2026-09-07-destination-b-active.md`
and `../core070/destination-b-s3b-pilot/README.md`. The schedule's initial
checkpoint below is historical, not a claim that no computation has occurred.
External Sol source review is awaiting scoped transmission approval after a
safety rejection; remote compute authority is also unresolved. No bypass,
public admission, push or merge has occurred.

## Goal and immutable boundaries

Complete the statistical models behind unit, unit_obs, cluster, and cluster2 grouping, multivariate phylogenetic transport/consumer, and feasible primary intervals. Qualification oracle: gllvmTMB 0.7.0, b4d5fee64def88bc768dda1f1f77c29b295edd86. Preserve its build and likelihood. R edits are confined to the authorised phylo_rr S3b bridge adapter, tests and documentation. FRK remains parked at gllvmTMB#1275. No unlisted capability, 0.7.1 expansion, coverage certification, release, registration, push or merge is authorised.

The initial grouped family set is Gaussian, Poisson-log, Binomial-logit, Beta-logit and NB2-log. This is not every family × grouping × covariance combination. Group labels select groups; explicit model terms select covariance, and labels alone must not silently add random effects. All four levels require shared effects in joint fits, not accepted argument names.

## Implementation sequence

1. Reconcile the signed 42-row prose against its 32 enumerated IDs using signing history. Preserve every enumerated requirement; do not invent ten rows. Record exact source pins and applicability of retained receipts.
2. Freeze symbolic mappings: R formula, Julia representation, independent simulation draw, estimated coordinates, and interval extractor. Reuse SourceCovariance, TwoLevelFit, precision payload validation and completed second-order mapping.
3. Parallel Gaussian tracks: shared grouping representation and random-effect precision objective; multivariate phylogenetic consumer; independently assembled covariance fixtures. Avoid dense covariance over all responses at realistic sizes. Preserve existing fit types/routes and scalar special cases.
4. Admit separate Gaussian non-unit-height ultrametric tree, sparse pedigree with ancestors, and dense supplied covariance bridge cases. R transports canonical precision, node map, scale and determinant. Retain R's covariance regularisation; report original condition number, warn above 1e8; Julia must not independently invert again. Keep native scaling and non-ultrametric boundary unchanged. Transport alone never opens admission.
5. Run S4 once its consumer and adapter pass; do not wait for unrelated grouping. Add Gaussian intervals and public route integration.
6. Extend shared joint objective to four non-Gaussian families. Crossed groups cannot use duplicated independent per-unit mode solves. Integrate supported intervals and explicit unsupported-combination errors.
7. Retained recovery, remaining signed second-order/extractor/real-data rows, public examples, regression and independent review. Assess 0.7.1 delta separately only after Destination B.

## Separate website task — visible GLLVM.jl logo

Requested by Shinichi on 2026-09-07, with GLLVM.jl and DRM.jl homepage screenshots as the visual reference. Status: **locally implemented and rendered** in commit `cc8b95b0`; not deployed.

The response-structure mark is now a semantic, normal-flow VitePress hero
image with accessible alternative text.  It remains visible at narrower widths
rather than relying on the former desktop-only CSS pseudo-element; the small
navigation logo is unchanged.

Local `docs/make.jl --local` and the generated-asset/HTML checks pass.  This
does not substitute for browser screenshots at every physical device or a live
deployment; those remain deliberately unclaimed.  The presentation task is not
an additional Destination B statistical row and does not authorise deployment
or push.

## Interval contract

Targets are fixed effects, group variances/SDs, rotation-invariant latent covariance summaries, and identifiable phylogenetic variance/signal. Use observed marginal-objective curvature including nuisance parameters and transformations, not conditional random-effect curvature. Interior parameters may use transformed Wald; boundary-sensitive targets need a justified profile route or explicit unavailability. Singular, unidentified, unconverged and invalid-curvature cases must be diagnosed. Matching implementations, truth recovery, interval availability and nominal coverage are separate outcomes. There is no blanket 94–95% small-N coverage gate.

## Ownership and resources

Parent integration worktree: /private/tmp/destination-b-20260907-main, branch codex/destination-b-20260907; starting Julia SHA 8ef0851f60b7c2e26884b666a8bf99a9b3104294. Shared checkout is dirty and remains untouched. Builders use separate codex/destination-b-grouping-20260907 and codex/destination-b-phylo-20260907 branches. One integrator owns exports/dispatch/bridge and acceptance ledger. Maximum three workers plus parent. Terra high builds, Terra independent fixtures, Luna tiered-CLI inventory, Sol high bounded mathematical/claim review; Astra only unresolved consequential dispute. Retain actual routing receipts and avoid redundant rediscovery.

Totoro: bounded compilation/pilots/profiling; aggregate ≤150 cores, numerical libraries one thread. DRAC CPU arrays: retained independent recovery, one seed/bounded cell per task; never login-node compute, environments/results on durable project storage. Existing SSH sockets only, no Duo prompts. Estimate each run; >30 min requires measured pre-run/results approval. Pilot the most expensive shape. No GPU work is planned.

## Evidence gates

Each row records model specification, frozen R source/build, Julia candidate, data hash, seed, exact command/CWD/exit and decisive output. Gates cover model identity and constants; all grouping structures including crossed/joint cases; phylogenetic scaling/marginalisation; signed paired tolerances; intervals and negative diagnostics; independently generated recovery with failures counted; exported workflows; full/core Julia and relevant R tests; changed documentation builds. Deliberately wrong labels, determinant, scale, data hash and invalid intervals must fail the relevant checker. Reanalyse exact retained outputs before rerunning; implementation/fixture changes invalidate affected evidence.

Two fresh Terra reviewers and one Sol reviewer cover numerical, user workflow and claims at final review. Required failing rows remain incomplete. Every finished tranche updates check-log and an after-task report. Reconciliation records deviations, exact continuation, qualified/deferred rows and evidence. No programme completion until all required gates pass.

## Schedule and checkpoint

Planning estimate, not measured promise: baseline days 1–2; contracts days 2–5; Gaussian grouping/phylogeny weeks 1–3; S4/Gaussian intervals weeks 3–5; non-Gaussian integration weeks 3–7; recovery/remainder weeks 6–10. Central eight weeks, risk range 10–14 for numerical repair. Replace estimates with measured remaining work/pilots. Current checkpoint: isolated worktrees and scoped ownership established; no new numerical evidence yet. Next: kernel contracts, regression-first tests and bounded parallel builders. The generic goal launcher was not used live because its resolved path is broken and its underlying script changes unrelated permission configuration; direct Git worktrees preserve the same isolation without those mutations.
