# Required covariance replay and source-contract reconciliation

## 1. Goal
Bind the nine previously qualified native Gaussian covariance models into the
central required runner, while exposing the remaining frozen-R covariance scope.
This is a local programme increment, not full Core070 completion.

## 2. Implemented
The required runner now accepts two indivisible covariance fixture groups with
nine stable IDs. The manifest binds their actual public R calls, Julia calls,
data, drivers, free-parameter counts, controls and numerical rules. Execution
hashes include both drivers and their R fixtures. A generated annex describes
95 source facts and190 obligations:95 admission checks,37 model contracts needing
three interfaces,56 reference boundaries and2 adapter policies. These are planned
obligations, not190 passed checks. The freeze gate now requires matching native,
formula and bridge contracts for covariance, as it already did for families.

## 3a. Decisions and Rejected Alternatives
Keep existing MODE/FIT-MODE IDs and fixtures. Seven full-rank cases retain explicit
tighter public R stopping controls; the failed default-control baseline remains.
The two ordinary independent/common cases retain the reference's fixed residual.
Ordinary full covariance compares identifiable total covariance only. Never use
one native Gaussian result to certify other families, formula, bridge or recovery.
A source-label correction changes slope_level to link_code, grounded in frozen
R/fit-multi.R:488–498; it changes no fitting behavior.

## 4. Files Touched
Central manifest/map, registry/runner/helpers, two wrapper fixtures, covariance
contract generator and source-plan generator, independent verifiers and regression
tests. Fresh bridge-runtime/boundary receipt pointers, developer log/checkpoint
and targeted Julia-only Mission Control status. No numerical source, dependency
version, foreign lane or R engine changes.

## 5. Checks Run
Totoro replay:9 cases,203 assertions,50.926seconds, one Julia/BLAS thread; both
oracle checks and registry9/9 pass. Maximum absolute likelihood difference is
2.5594e-11. Independent raw-R readback checks reported values, parameter maps and
normalized Gaussian density. Draft integrity:85 tests plus evidence self-test;
24 executable bindings over12 source facts,703 non-excluded facts still unmapped.
Those counts measure catalogue structure, not percentage completion. Fresh public
bridge runtime passes12.70seconds; Gaussian boundary passes41.55seconds and still
reports model change, not parity. Local interface registry40 and covariance9 pass.
No full package suite, documentation rebuild, campaign or final panel was run.

## 6. Tests of the Tests
The pre-implementation registry red and covariance-role red are retained.
Seventy numerical/receipt/dependency negative controls reject damaged evidence,
omitted cases, nonzero exit, stale contract, missing receipt, partial execution
groups, inflated assertion counts and missing Rscript. Structural tests reject
missing covariance metadata, altered fixtures/controls, helper-only checks and
mismatched interfaces. Unlazy leaf commands are verified; programme remains unpaid.
The prose leaf preceded integration, but the exact Unlazy command ledger was
materialized afterward: do not claim perfect gate-before-dispatch discipline.

## 7a. Issue Ledger
Full manifest DRAFT; programme ACTIVE/M1 PARTIAL. Original Student health and
binomial AGHQ failures remain. Covariance modifiers, random slopes, latent
multinomial, missing data, fitted objects, remaining bridge/AGHQ, recovery/coverage,
performance, full suites and final Documenter/review panels remain unpaid.
Full-suite and previously rejected external-review boundaries are unchanged.

## 8. Consistency Audit
Native-only rows cannot close the covariance interface gate. Synthetic receipt
format tests exclude typed covariance blocks explicitly; production loading never
uses that helper. Historical receipts remain immutable; fresh bridge checks replace
stale-source inputs rather than editing their pins. Prior source-design and formula
results remain historical after the central runner/inventory changes and must be
replayed for final candidate claims. No new public API or performance claim.

## 9. What Did Not Go Smoothly
Initial integrity check correctly rejected stale bridge receipts. Later structural
runs exposed synthetic fixture scope and stale expected counts, which were repaired
without weakening production checks. First central replay failed before fitting:
a dynamically constructed module lacked include. Ordinary module wrappers fixed
that loading error; the failed run is retained. Two administrative Unlazy checks
briefly overlapped because approval also executes checks; no fits were duplicated.
A final sequential reverify is retained before checkpoint closure.

## 10. Known Residuals
The95-fact annex still has unresolved model/interface calls; it is not the frozen
finite capability manifest. The known769-fact census is not independently certified
exhaustive. Required numerical and programme acceptance cannot be inferred from
source presence, historical passes or small likelihood differences alone.

## 11. Team Learning
Hopper was requested as Terra/high with fresh context and exclusive ownership of
three annex files, plus one bounded correction. Noether was requested as Terra/high
for independent scoped review. Ada owns central integration and verification.
These are dispatch requests, not verified provider billing or actual agent-hours.
DRAC socket probes confirmed Fir/Rorqual/Trillium/Narval/Killarney/Tamia; Vulcan
timed out and Nibi had no socket. At most five systems may be used. No allocation
or job was submitted. Totoro reconnect remains authorized without Duo.

## 12. Cross-Product Coverage
This does NOT cover covariance formula/bridge/nonGaussian surfaces, recovery,
coverage, AGHQ, performance, full package suites or final documentation.
Only the isolated Julia Core070 lane is changed. R0.7.1/article/foreign work remains
protected. No push, merge, release or destructive cleanup. Mission Control receives
a targeted status update with R fields preserved; its receipt accompanies this
checkpoint: 91d17a81e18de0f7006d3ab903b727e2f61736d2, HTTP200, Rfieldsunchanged. Next: complete covariance model/interface contracts and implementation,
then the remaining approved programme, without promotion from this subset.

Noether verdict: no actionable P0–P3 findings in the scoped registry review;
independent pure checks and numerical receipt verifier pass. Rose programme completion panel: NOT RUN.
