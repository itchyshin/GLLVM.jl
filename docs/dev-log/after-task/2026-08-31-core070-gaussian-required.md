# Gaussian required-runner integration checkpoint

## 1. Goal
Connect the retained original Gaussian native/formula comparison to executable
source obligations. Programme remains ACTIVE/M1 PARTIAL; no full parity claim.

## 2. Implemented
Additional native-model and formula IDs share one required-runner execution.
The original seventeen family smoke cases remain intact. Exact fixture bytes,
reference helper, calls, nuisance scales and acceptance rules are retained.
The master source map now binds two executable cases to the Gaussian identity
family fact; its public R bridge remains unimplemented/unqualified in this map.

## 3a. Decisions and Rejected Alternatives
Do not replace the legacy smoke case or treat RCall as the public R bridge.
One shared execution reports two IDs but counts 31 assertions once. Completed
IDs are sorted by the receipt writer, while requested IDs retain input order.
Compare both exact lists according to their documented representations.

## 4. Files Touched
Gaussian fixture/reference helper, required runner/registry and inventory,
master contract/map, generated family plan and generator, receipt verifier,
registry/collection/coverage tests and named developer-log/checkpoint records.
No src/ engine files, public API, R engine or foreign worktree changed.

## 5. Checks Run
Registry metadata tests fail before integration. First Totoro replay passes31
assertions in44.02s; final replay with semantic-output checksums passes31 in32.80s.
The final registry passes28 assertions in3.88s. Frozen R oracle verification passes
before and after. Both runs use one Julia/BLAS thread and retain all outputs.
Absolute delta logLik3.8641e-9; native gradient1.5420e-10; R gradient4.4453e-5.
Direct, wide and reversed-long formula fits agree; missing/duplicate long rows
reject. Local metadata aggregate passes39 tests plus evidence self-test.

## 6. Tests of the Tests
Ten numerical/receipt corruptions reject false health, excess gradient, wrong
likelihood, changed residual scale/data, omitted cases, nonzero exit, stale
contract, doubled assertion totals and failed assertions. Registry rejects an
incomplete Gaussian execution group. Family coverage still rejects its missing
bridge. Unlazy2/3 after fresh re-verification; full-contract gate remains unpaid.

## 7a. Issue Ledger
One source fact now has two executable links, not a completed-family verdict.
714 other nonexcluded source facts remain unmapped in the master record; this
count does not measure model completeness. The generated plan still has69facts
and97planned cases, now23concrete bindings:5native,2formula,16boundary. Remaining
existing model evidence needs same-model contract and runner binding review.

## 8. Consistency Audit
Original fixture SHA256 unchanged. Source/runtime/contract/helper hashes and
external process exits bind receipts. Saved semantic outputs are hashed in the
supervised log. The full manifest stays draft. Synthetic collection tests remove
real executable tables only within their temporary synthetic manifests; no
production freeze or numerical gate is bypassed. Old receipts remain historical.

## 9. What Did Not Go Smoothly
A new negative test expected a more specific error string than the actual generic
missing-interface diagnostic; fixed the expectation without weakening rejection.
An initially mistyped family-test pattern ran zero tests and was not counted;
the correct file subsequently passed5tests. A simultaneous test-file edit caused
the integrity check to reject changed inputs, as intended; a fresh stable run
replaced that attempt. First receipt verification assumed identical list order;
inspection confirmed sorted completion IDs and the verifier was corrected.

## 10. Known Residuals
Full suites and the specific external numerical-review payload still await
approval. No independent reviewer dispatched. Shared runner changes make earlier
required receipts historical until refreshed on this candidate. Full bridge,
remaining core/AGHQ capabilities, recovery/coverage, performance and final
Documenter campaign remain unpaid. No elapsed-hours forecast updated.

## 11. Team Learning
Ada parent implemented and checked this bounded integration; no model/effort or
agent-hours receipt invented. Rose verdict NOT REQUESTED for this interim slice.
Keep source-fact coverage, executable registration, same-model numerical evidence
and full-interface completion separate; a single green label conceals that debt.

## 12. Cross-Product Coverage
This checkpoint does NOT cover the public R bridge, all Gaussian structures,
post-fit inference, recovery/coverage, other families, a complete frozen manifest,
full package suites, independent review or final documentation/performance claims.
No push, merge, release, destructive cleanup or DRAC allocation. Totoro jobs ended.
