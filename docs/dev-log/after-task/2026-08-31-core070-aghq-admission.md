# After-task — bounded public AGHQ k1 admission

## 1. Goal
Qualify matched Gaussian, Poisson and binomial k1 requests through native and
Julia formula entry points against the frozen R oracle. Programme ACTIVE,
M1 PARTIAL, full manifest DRAFT_NOT_FROZEN.

## 2. Implemented
Added a pinned three-case contract, public fit runner, retained-history verifier,
negative controls and six bounded native/formula evidence bindings. All three
final paired cases meet the unchanged fit-health and likelihood rules.
The bindings supplement the required-case annex; no whole obligation is promoted.

## 3. What Changed the Next Action
Do not repeat the successful eligible k1 fits. Continue missing admission and
source-domain contracts. Default-unique needs a genuinely matched parameter
contract before native comparison, not a substitution with has_diag=true.

## 3a. Decisions and Rejected Alternatives
Gaussian R default nlminb and tightened nlminb both failed fit-health. The public
optim/BFGS control reaches a healthy point with the same model, fixture and
tolerances. This is an explicit optimizer qualification, not default-optimizer
success. No likelihood or R source changes. Both failed attempts remain required
inputs to the verifier, bound to their original source archives.

R default unique maps shared residual scale off and adds one B unique diagonal.
Native has_diag admits both B and W diagonals plus free shared residual. Matching
total covariance alone would not prove parameter or interval equivalence.
Noether's binomial sign objection was rejected: exp(-eta-intercept) implements
logistic(eta+intercept), as declared. The realized fixtures were unchanged.

## 4. Files Touched
tools/core070_aghq_admission_run.jl, core070_aghq_admission_verify.py and
test_core070_aghq_admission_verify.py; test/parity/core070_aghq_admission_cases.toml;
developer leaf, evidence JSON, this report, check-log and Core070 checkpoint.
Mission Control received only a Julia next-action correction. No numerical src,
R engine, foreign checkout, public API, tolerance or protected-lane edits.

## 5. Checks Run
Three retained Totoro Julia1.12.6 batches: 48.306s, 48.451s and 48.555s for
the fit command, each within the declared 2–5 minute estimate/300s cap. Frozen
oracle verification passed before and after each. One Julia/BLAS/OMP thread.
Final absolute delta logLik: Gaussian 2.2737367544323206e-13, Poisson
1.9352910385350697e-9, binomial 3.218360689061228e-9. Both engines converged and
passed abs gradient<=1e-4 OR relative<=1e-6. Gaussian R gradient 2.2123295e-7;
discrete R gradients pass the relative criterion, not the absolute criterion.
Within-engine baselines and native/formula parameters/logLik are exactly equal.
Formula actual integration is Laplace; its node/reason fields were not separately
recorded. All attempts have byte-identical realized fixture and contract hashes.
Local verifier and six Python tests pass. No Pkg.test, core suite, new docs build,
recovery or timing campaign is claimed by this developer-harness slice.

## 6. Tests of the Tests
Nineteen corruptions reject across six tests, including a positive history
control: missing receipt/history/fixture/dependency, stale plan, altered archive,
nonzero exit, forged output, omitted case, negative gradient norm, hidden health
failure, false numerical status, ignored warning, wrong trials, historical
failure promotion, omitted summary interface, changed call/delta and full-scope
promotion. Semantic corruptions rebind outer hashes before checking, so a hash
mismatch alone cannot explain their rejection. Tests use copies only.

## 7a. Issue Ledger
Original binomial seed43/k5 remains failed. Original Student-t seed71 reference
health/density remains unpaid. Default-unique native case is explicitly blocked.
No new B production child, release, issue message, push, merge or cleanup.

## 8. Consistency Audit
The finite annex remains 32 public/numerical obligations and 21 family
descriptors. These six bindings pay only the declared eligible k1 comparisons.
They do not discharge the complete K1-PRECEDENCE obligation, which also includes
ineligible models and the public R bridge. R baseline is explicit aghq=FALSE;
omitted-control behavior in R is not separately replayed. No full manifest freeze.

## 9. What Did Not Go Smoothly
Tighter nlminb stopping did not improve Gaussian health and returned singular
convergence7. A value-correct R design matrix initially failed identical() due
to assign/contrasts attributes; the third run checks dimensions and values.
An expired local exec handle did not mean the fit was lost: the terminal process
receipt and complete readback existed, so no duplicate run was launched.

## 10. Known Residuals
Formula node/reason provenance, ineligible k1 cases, bridge, remaining Stage1a
families, covariance/modifiers, structured multinomial, data/postfit, recovery,
performance and final package/docs validation remain required. No general AGHQ
claim or scientific recovery claim follows from these three fixtures.

## 11. Team Learning
Noether requested native Terra/high with fresh context; one review and one repair
follow-up. Model label is requested routing, not an independently audited actual
model receipt. Parent addressed accepted findings; final BFGS/history verifier
was not independently re-reviewed. Rose completion verdict remains NOT SIGNED
OFF for the programme. No fabricated aggregate agent-hours; only measured
command durations reported. Existing Totoro and DRAC/Fir sockets responded on
resume; Fir queue empty. No new login or DRAC compute was started.

## 12. Cross-Product Coverage
Three families x two Julia entry points, each with the same R unique=false model,
p4/n120/K1, common Gaussian scale or discrete trait intercepts and eight binomial
trials. This slice does NOT cover higher-node quadrature, other dimensions or
families, default-unique native fits, missing-data routes, other covariance
structures, the public R bridge, calibrated intervals, recovery or performance.
