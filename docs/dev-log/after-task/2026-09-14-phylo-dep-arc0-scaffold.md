# After-task — phylo × dep Arc 0 scaffold (2026-09-14)

**Branch:** `cursor/phylo-dep-070-20260913`  
**Base:** `origin/main` @ `1125eafb`  
**Rose fence:** Julia Arc 0 matrix fitter only ≠ formula/bridge parity ≠ ledger promote ≠ twin Δ.

## Scope

- ACCEPTED identity `docs/dev-log/decisions/2026-09-14-phylo-dep-identity.md`.
- Exported `fit_phylo_dep_gllvm` wrapping existing Gaussian J3 phylo_latent path.
- Tests + LOOP checkpoint/arcs tick + OWED advisory Frozen R note/issue.

## Out of scope

- `@formula` / `gllvm()` / bridge; S4 probe; capability-status promotion; version bump.

## Checks

| Check | Result |
|---|---|
| Local `runtests.jl` | Not run (depot/manifest drift on agent VM) |
| CI | Await PR workflow |

## Follow-up

- ADEMP recovery cell for phylo × dep before any `partial`/`covered` promotion.
- Sparse / large-p path (avoid `sigma_phy_dense` in production).
