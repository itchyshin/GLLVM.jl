# Core070 executed per-variance documentation

## 1. Goal
Execute the new Gaussian design example and qualify a strict, deployment-free local documentation build.

## 2. Implemented
Self-contained named Documenter example; exact-byte extraction checker; strict local build path; low-level reference for36 missing bindings; broken Student reference repaired. Evidence checks, negative controls and retained rendered artifacts added.

## 3a. Decisions and Rejected Alternatives
Keep warnonly=false rather than suppress missing documentation. Separate internal helpers from public capability claims. Do not fabricate a remote URL for an unpushed decision document. Qualify core and docs environments separately.

## 4. Files Touched
Documenter make file, response-family and Student pages, new low-level reference, example/evidence tools and developer records. No numerical source, R engine or foreign lane changed in this slice.

## 5. Checks Run
Totoro example6 plus neighboring18 assertions PASS,20.77s/35.15s. Strict local Documenter/VitePress build PASS59.29s. All94 copied static files hash-verified. Example/build evidence verifiers and earlier27+14 pervar candidate verifier freshly pass. Desktop/mobile section and navigation inspected. No full package suite or deployment.

## 6. Tests of the Tests
Six negative controls reject false completion, missing process, invented parameter count, unsupported deployment, corrupt site inventory and failed process with a valid checksum. Initial actual example failed on matrix broadcasting; corrected elementwise addition. Build failures for remote/root discovery, four references and36 missing bindings retained.

## 7a. Issue Ledger
This documentation slice passes its bounded gates. C2, the full manifest and M1 remain incomplete. Search, branding, version selector and broader example/visual checks remain unpaid; numerical review and integrated parity revalidation remain outstanding.

## 8. Consistency Audit
Five-coefficient/thirteen-parameter example matches the new explicit-design semantics. Local build skips deployment, while normal deployment remains configured. Internal AGHQ helpers explicitly do not imply a public estimator. Julia-only Mission Control correction is recorded in the runtime receipt, with all R fields preserved.

## 9. What Did Not Go Smoothly
Initial example used nonbroadcast addition. Local snapshot lacked Git remotes. Strict content checking exposed inherited missing bindings and a link outside the built site. Each failure has a retained receipt. Local versions.js returned404 despite no console errors observed.

## 10. Known Residuals
Strict content build is not warning-free presentation: logo/favicon/package.json and large chunks remain. Docs Optim1.13.3 differs from core qualified runtime; neither substitutes for full Pkg.test. Screenshots cover the Gaussian section, not all pages or full mobile output. No independent review or release readiness claim.

## 11. Team Learning
Execute the actual tutorial block and retain environment provenance. Check network failures separately from console errors. Source documentation coverage can be strict without turning internal bindings into public promises.

## 12. Cross-Product Coverage
This does NOT cover complete R Core/AGHQ parity, complete Documenter polish, calibrated recovery, performance or R bridge qualification. Protected R programmes and foreign worktrees remain untouched.

Rose verdict: NOT RUN; bounded parent verification only, not milestone sign-off.
