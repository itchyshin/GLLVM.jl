# After-task — board refresh after #374 + #376

**Date:** 2026-09-16  
**Branch:** `cursor/board-refresh-374-376-a0ce` (docs-only tip)  
**Base:** `origin/main` @ `e47430735` (#377 tip on top of `#376` `47fcb23e` / `#374` `eeb7e092`)

## Scope

- Correct programme tip after #374+#376 landed and #377 merged with a stale
  “#376 CONFLICTING” claim.
- Touch only board / `LOOP/checkpoint.md` / check-log / this after-task.
- **No** engine, Wald, Totoro, Stage 1, S4, Project.toml, or gllvmTMB edits.
- Cloud fence: do **not** start the next ungated Mac slice.

## Outcome

- Verified merged: #374 `eeb7e092`, #376 `47fcb23e`, #377 `e47430735`.
- Open PRs left alone: #357 (foreign), #363 DRAFT env, #314 DRAFT handover.
- Holdouts doc already reflected #374+#376 from the engine PRs — no edit needed.
- Board + checkpoint now record six-holdout SO cells MERGED; Mac owns next ungated
  (natural candidate: Tweedie estimated-power EOO). Goal **not** complete.

## Checks

```text
git fetch origin main
git rev-parse --short origin/main   # e47430735
gh pr list --state open             # 363, 357, 314
gh pr view 374 --json state,mergeCommit
gh pr view 376 --json state,mergeCommit
```

Docs-only; no Julia suite run.

## Rose fence

Docs tip only ≠ §7 ≠ bridge CI (#357) ≠ Tweedie EOO implement ≠ paste-gated clearance.

## Follow-up (Mac)

- Choose and run the next ungated engine/Wald slice (Tweedie estimated-power EOO is the
  board candidate).
- Paste-gated remain: `accept delta dispersion A`, `G0 Stage 1`, `S4 probe yes`,
  Totoro/D-139; Project.toml stays 0.3.0; OUT rows GP-1 / free-ν / Λ raw / BB φ pairing.
