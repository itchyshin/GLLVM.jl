# After Task: Destination B foundation — interim, NOT CLOSED

## 1. Goal

Execute the full approved Destination B programme, not a kernel-only substitute.
Frozen qualification reference is gllvmTMB0.7.0 at
`b4d5fee64def88bc768dda1f1f77c29b295edd86`. The programme and acceptance ledger
remain open. This is a recoverable implementation checkpoint, not closure.

## 2. Implemented

Subsequent same-data pedigree diagnosis now separates an unsuccessful nlminb
baseline from one successful, predeclared public BFGS attempt. BFGS returns
status 0, gradient 1.857232e-7 and an optimum within 1.865e-13 of retained
independent Julia evidence. The full marginal Hessian is positive definite;
this is diagnostic only. Checker 95556 passes 23/23 assertions, including
negative controls that prevent failed status or corrupt data being accepted.
All original failures remain retained. This closes the bounded optimizer
investigation, not review, paired interval, recovery or admission gates.

The actual pedigree fitted pilot now retains two distinct R outcomes: direct
Ainv formula fails on frozen private-helper lookup; pedigree syntax preserves
the same model/data but returns singular optimizer status1. Julia marginal
cross-evaluation agrees and its independent public fit converges with all
primary Wald intervals available. This is not paired-fit qualification: R's
status remains failed, and review/refinement gates are open. All attempts and
source/data hashes are retained; no R engine/parser was changed.

Pedigree follow-on adds actual frozen-R canonical precision and an independent
structural-equation covariance fixture, including related-parent matings and
four unobserved founders. Fixed-model checks22/22PASS; dropping founders from
precision and swapping maps are discriminating negatives. This is a precursor
to fitted pedigree evidence, not its completion. The old founder-pair example
did not actually exercise inbreeding; the new explicit pedigree does.

The subsequent independent public Julia pilot also converged without R-derived
starts: own-optimum NLL difference-2.408e-11 and all12 displayed Wald rows
available (10distinct estimands). Both the pre-optimization missing-import
failure and corrected attempt are retained byte-for-byte with provenance.
Fit1.295s plus intervals1.639s excludes startup; not a campaign forecast.
Pure receipt checks18/18 and strict comparator41/41 passed; separate actual
integration was run. P7 independent source review remains unmet, so the pilot
slice and programme are not closed. No R interval or recovery certification.

Latest checkpoint: precision-contract repairs are locally committed b240aa8f
after bounded independent approval and focused verification. New frozen-R
exporter and independent JSON checker are integrated but not yet independently
approved. Actual frozen0.7.0 marginal cross-evaluation passes at fixed and
R-fitted parameters (differences0 and1.066e-14); checker41/41PASS6.1s. This
does not establish independently fitted agreement or interval feasibility.
The first export's NULL metadata fault is retained; repaired attempt02 has
identical numerical results. External Sol review was blocked before launch;
explicit scoped transmission approval is pending. Programme remains open.

Since the initial snapshot retained below: joint Gaussian/non-Gaussian fitters,
explicit public grouping and formula routes, trait-specific Beta/NB2 dispersion,
full-marginal Wald targets, structural-identification diagnostics, multivariate
phylogenetic fitting/bridge candidate, and Gaussian/non-Gaussian post-fit
methods. Public R admission is explicitly closed, including machine-readable
candidate-result fields. R changes remain adapter/tests/documentation only.
Fixed-effect `vcov`, `stderror` and structured95%Wald summaries now use the full
observed-marginal nuisance-adjusted covariance for the three new fit types.
The public Gaussian group-variance profile is now exported with explicit
natural-variance target metadata and retained constrained-refit evidence.
Native `fit_gllvm`/formula dispatch also supports explicit Gaussian precision
alone or jointly with independent ordinary grouping sources. Joint population
prediction, source-separated covariance extraction and full-nuisance fixed-effect
summaries are integrated. These do not open public R bridge admission.

Follow-up to local WIP commit `89eac4c1`: public precision fits now validate
and snapshot raw precision inputs. Precision-only fits also support a single
shared residual variance, while preserving the trait-specific default. The
shared mode is forwarded through fixed-effect information and candidate bridge
metadata. Joint covariance-tangent diagnostics are implemented; fit/inference
gating is being integrated. These repairs remain unqualified pending checks.

## 3a. Decisions and Rejected Alternatives

The covariance identity is `eta=D*beta+sum(Zq*bq)` with genuinely shared group
effects. Reject duplicated independent per-observation modes for crossed groups.
Reject conditional-field curvature as parameter uncertainty, Hessian ridges,
silent covariance substitutions, and seed/tolerance changes to erase failures.
Two-trait rank-one-plus-two-unique covariance has4 coordinates for3 entries:
known structural redundancy now yields `:nonidentifiable` Wald diagnostics.
A same-data unrestricted covariance refit is separately recorded, not a
replacement receipt. Profile mathematical and numerical contracts are approved;
the foundation and constrained refits are integrated. Callback review approved
the repaired layer, independent dense endpoints passed, and the bounded public
Gaussian variance-profile route was added; no coverage/R-parity claim follows.
Frozen R signal uses species-level latent variance: the
previous unavailable observation-variance-ratio placeholder was corrected to
`:estimand_not_admitted`, with no likelihood or estimate changed.

## 4. Files Touched

Implementation lives in the new grouped, marginal-interval, precision-multivariate,
bridge-candidate and post-fit files under `src/`; integration edits are in
`src/GLLVM.jl`, `src/families/fit_gllvm.jl`, `src/formula.jl`, `src/bridge.jl`
and `src/phylo_precision.jl`. Corresponding focused tests, independent fixtures,
test-only dependency declarations and runner registrations live under `test/`.
README, API/grouping/bridge guides, low-level references and docs navigation
are updated. The scoped R branch changes only its adapter, new test and decision
record. Exact current state is in the active checkpoint and `git status`.

## 5. Checks Run

Commands, working directories, timings and staged outcomes are retained in the
check log and ignored ledgers. Latest targeted results include quadrature36,
all-four Gaussian diagnostics63, per-trait non-Gaussian fitter63, all-four
Poisson46, public non-Gaussian20/Gaussian9, post-fit38/29, precision fitter41,
and marginal helpers17+4 assertions passing. Aqua plus existing JET targets
passed12/12 in9.4s. Docs4204 exits0, Vitepress7.51s, deployment disabled.
Subsequent parent checks: fixed-effect25+postfit29+38 (85524), quality12in10.7s
(18623), docs8.26sVitepress (34648), exact public covariance/summary example
`:available` (75903), non-aliased grouping invariance21 (12079), and NB2
conditional-replication61 (80953). The original NB2 small-data partial outcome
remains recorded. Signal correction44fitter+39bridge+5dispatch passes; all
claims remain bounded and no full/core suite or frozen-R pairing is implied.
A combined quality-env run stopped after successful marginal/grouped tests
because ForwardDiff was not a direct dependency of that isolated environment;
the remaining precision test was rerun in main and passed41/41. Full/core
suite and new-hot-path allocation/performance qualification remain owed.
Latest tranche: parent9614 foundation44+4/refit46PASS; parent9389 joint
kernel18/privatefitter26PASS; parent13836 callback receipt3/inversion66PASS.
Sol62559 found three inversion gaps despite the earlier51PASS; re-review4815
found two more receipt-integrity gaps. All were repaired and reverified before
the final approval below. Tiny joint-fit diagnostic tests are not evidence of
interval feasibility; the separate larger fixture is reported below.
Subsequent checks: focused profile208PASS51026 and independent callback review
79PASS4622; public profile8PASS30608/61150. Standalone precision failure-guard
repair44+5PASS61150, jointpostfit29PASS28601, publicprecision/grouping29+9+20
PASS73686. Joint64tip same-data refinement now runs through publicfit_gllvm:
18PASS78011 with available intervals at gradient7.419e-6; the original loose-fit
nonstationarity remains retained. Finalquality81903 passed12/12in9.8s. Docs63227
found one missing joint-summary reference entry; repaired57288 passed with
Vitepress7.52s, deployment disabled. Numerical joint review22599 subsequently
returned NOT APPROVED: raw precision admission and exact covariance aliases
were missing. Repairs are underway; broad programme gates remain open. Native
bridge regressions72312 passed39+5+80 before these later repairs. Independent
fresh-MAIN admission37PASS and shared-residual19+precision44+failure5PASS are
bounded repair evidence, not a replacement for final review/full regression.
Parent integrated rechecks then passed admission37/shared19/Gram18 (19967),
public alias15/dense oracle10/shared bridge27 (45586), quality12 (81999) and
unchanged64tip18 (24195). Docs82698 passed with Vitepress7.49s and deployment
disabled. Fresh independent review59607 subsequently APPROVED the bounded
precision-repair source scope with seven stable SHA256 hashes and no blocking
findings. This is static independent review plus separately recorded focused
checks, not full/core, recovery, performance or programme qualification.

## 6. Tests of the Tests

Prewritten tests failed before public dispatch, interval-guard and admission
metadata implementation. Independent Gaussian/QuadGK oracles check constants
and shared effects. Deliberate wrong source maps, determinant signs, scale,
source pins, row counts and invalid intervals do not pass. Structural parameter
count independently explains the retained false-positive curvature risk.
Bare-core missing QuadGK/StableRNGs is an explicit Broken skip, never a passed
numerical-evidence gate.

## 7a. Issue Ledger

No issue, PR, push, merge, release or registration mutation performed.
FRK remains parked at gllvmTMB#1275; protected adjacent work is untouched.
Exactly32 named rows were recovered from signing history; no ten extra rows
were invented to match the erroneous42-row prose count.

## 8. Consistency Audit

Current public examples were executed. The non-Gaussian example first exposed
a missing Distributions import; corrected example and zero-effect prediction,
raw residual and covariance extraction now execute. Docs distinguish these
conditional means from population-marginal means. Bridge transport uses native
precision determinant and explicit map bases; R admission remains closed.
Independent Sol transport review found no static numerical transport defect
and required the now-tested admission metadata. Final programme review is owed.

## 9. What Did Not Go Smoothly

Oracle incidence and ancestor errors, finite-difference stencils, NB2 inner-mode
stagnation, trait-fast dispersion indexing, structural factor redundancy, and
a genuine NB2 size boundary were found and retained. No failed statistical cell
was silently replaced. The first report-structure check also failed because
the historical headings below did not match the current numbered contract;
these canonical sections repair report shape, not outstanding programme gates.

## 10. Known Residuals

Full five-family/covariance qualification, combined grouping/phylogenetic models,
frozen-R fitted S4 evidence, justified phylogenetic signal, profile fallbacks,
retained recovery arrays, full/core regression, realistic performance and final
independent reviews remain unfinished. Frozen R source is isolated and clean,
but not built. Existing DRAC sockets were observed; safe status checks do not
establish a usable allocation/environment. Direct inspection was safety-blocked;
no fresh login or Duo prompt was attempted. No compute campaign was launched.

## 11. Team Learning

Count covariance coordinates before interpreting a Hessian. Validate the
independent simulator and keep failed draws alongside later declared designs.
Distinguish model identity, point convergence, interval feasibility and coverage.
Use cheap source inventories, bounded Terra builds, and Sol mathematical reviews
with retained routing receipts. Never copy stale worker files over newer parent
integration changes.

## 12. Cross-Product Coverage

This checkpoint does NOT cover every family × grouping × covariance combination.
It covers specific tested shared-group kernels, native fits, public routes and
diagnostics. The Gaussian all-four cell uses `:dep` for unit/unit_obs/cluster
and diagonal `:indep` for cluster2; the Poisson cell uses independent/common-scale
terms. These cells have fitted interval evidence; other joint-family
cells are still being tested. It does NOT cover population-marginal or BLUP
prediction, R public phylo_rr admission, full frozen-R parity, recovery/coverage
certification, profile intervals, FRK,0.7.1 expansion, releases or registration.

**Current Rose verdict: NOT CLOSED.** A well-formed report cannot discharge the
programme's unmet acceptance gates. The initial snapshot below is retained as
history; the active checkpoint and sections above supersede its live-state wording.

---

# Initial foundation snapshot (historical)

## Goal

Implement the approved full grouping/phylogenetic Destination B programme with feasible intervals, preserving the frozen R reference and scoped bridge exception. This report checkpoints the first internal foundations; it does not redefine the goal.

## Implemented

Integrated direct low-rank sparse Gaussian grouping and multivariate augmented-precision evaluation kernels, explicit covariance/precision determinant documentation, independently corrected covariance fixtures, and a full-marginal target-Wald helper. A separate isolated R branch has canonical transport and original-vcv condition helpers. No public admission or release occurred. Gaussian fitter/interval workers and a joint non-Gaussian objective worker remain active.

## Mathematical Contract

Gaussian response: y=Xbeta+sum(Zq bq)+epsilon. Independent source factors preserve shared and crossed group membership. Phylogenetic fields retain the complete augmented Q with covariance Q⁻¹ and trait covariance LL′+diag(U); observation residual is separate. Native checksum logdetQ is the negative of frozen R logdetA. Intervals derive from the complete observed marginal Hessian including nuisance coordinates, not conditional field curvature. Exact maps and formulas are in the destination-b decision records.

## Files Changed

- Source: `src/grouped_gaussian.jl`, `src/precision_multivariate.jl`, `src/marginal_target_intervals.jl`; internal includes in `src/GLLVM.jl`; convention corrections in `src/bridge.jl` and `src/phylo_precision.jl`.
- Tests: `test/test_grouped_gaussian.jl`, `test/test_precision_multivariate.jl`, `test/test_marginal_target_intervals.jl`, `test/test_destination_b_reference.jl`, `test/test_destination_b_kernel_crosscheck.jl`, `test/fixtures/destination_b_reference.jl`, payload regression and `test/runtests.jl` registration.
- Records: programme plan, destination-b decisions, check-log, active recovery checkpoint and `tools/destination_b_scope_check.mjs`. Ignored Unlazy ledgers retain exact commands/results.
- R branch only: `R/julia-bridge.R`, `tests/testthat/test-julia-bridge-destination-b.R`, bridge decision record. Frozen R/C++ likelihood untouched.

## Tests Added

Integrated deterministic kernel/oracle run passed77 assertions (11+11+17+18+7+13). Subsequent interval sentinel/constant-shift additions passed15/15 independently, superseding its earlier13. Existing payload80/80 and S1 22/22 remain regression anchors. R helper suite passed23/23 without warnings. Negative controls include wrong source labels, determinant sign/offset, scale, node map, missing ancestor, nuisance-block inverse, invalid objective, nonconvergence and singular curvature. These tests verify specific algebra/diagnostics, not recovery or public fit feasibility.

## Benchmark Numbers

Not measured. New sparse hot paths require realistic-size profiling and memory/factor-fill measurements; the few-second unit timings are not speedup evidence. No Totoro/DRAC campaign launched.

## R-Parity Verdict

Parity: NOT ESTABLISHED. R helper unit tests do not call frozen-R fitting. Retained12-cell receipts support narrow numerical comparisons but lack verified build/data provenance needed for qualification; the old M2 remainder used live0.7.1 and retains NB2 failures. See retained-evidence audit.

## JET / Allocs / Aqua Verdicts

- JET: existing Takahashi kernel gate passed in isolated check environment; new grouped hot-path review still owed.
- Allocs: not measured; no allocation claim.
- Aqua: passed together with existing JET gate,12 assertions total,10.0s; full Pkg.test and new-kernel performance checks still owed.

## Checks Run

Exact integration commands/CWD/results are in `docs/dev-log/check-log.md`. `node tools/destination_b_scope_check.mjs` exit0 confirms proposal/sign-off/current all enumerate32 identical named rows, with wrong-reference/omission/addition/duplicate controls. `git diff --check` clean. Normal Julia dependency resolution changed only ignored Manifest.toml, not tracked Project.toml. No full/core suite or documentation-build result is claimed.

## Consistency Audit

Targeted `rg -n 'log_det_A_phy_rr|shipped.*log.det' src/phylo_precision.jl docs/dev-log/core070/phylo-transport* test/test_bridge_phylo_precision.jl` located misleading historical wire wording; current source docstrings and a new correction record distinguish signs/maps. Historical receipts are preserved. README, version, public exports and rendered documentation are not promoted or updated as completed capabilities.

## GitHub Issue Maintenance

No external writes authorised or performed. Open Julia PR314 is the existing handover lane; no competing implementation PR was found at checkpoint. FRK remains parked at gllvmTMB#1275. No new leaves invented to reconcile the erroneous42-row total.

## What Did Not Go Smoothly

Generic goal launcher resolved a broken path and its fallback script would change unrelated permissions; used direct isolated Git worktrees instead. Fresh Julia worktree needed dependency resolution/cache access. Independent fixture review caught cluster2 diagonal-over-observations and no-unobserved-ancestor errors; those old oracles were invalidated and replaced before acceptance. Sol review corrected unique-field derivation/count and dense preprocessing. Parent caught the `2f0` Float32-literal trap in an unintegrated fitter Hessian; builder repair and a constant-shift test were requested before fitter acceptance.

## Team Learning

Independence is necessary but not sufficient: review the oracle's model identity before treating its green assertions as evidence. Explicit scopes, source pins and constant/scale/map negative controls exposed errors that ordinary successful runs would not.

## Remaining Risks

Full five-family grouping, public formula/unified interfaces, multivariate fitted R bridge/S4, identifiable fitted intervals, profile fallbacks, retained recovery, realistic memory/runtime, all signed workflow rows and final independent review are unfinished. Totoro/DRAC sockets were absent; user notified once, no fresh authentication attempted. Targeted tests cannot discharge full/core regression or the quality battery.

## Known Limitations

Internal Float64/CHOLMOD kernels currently evaluate named Gaussian models; they do not establish AD, outer fitting, nominal coverage, arbitrary combinations or complete Destination B parity. No new0.7.1 work, FRK, GPU route, release or registration. R transport helpers remain unconnected to public phylo_rr admission until consumer/fitter/paired evidence passes.

## Next Command

Continue from `docs/dev-log/recovery-checkpoints/2026-09-07-destination-b-active.md`: inspect live workers, review fitter/interval results, integrate only owned files, then run targeted and broader checks. Do not restart discovery or launch a campaign without its measured pre-run gate.

## Rose Verdict

Rose verdict: FAIL — programme and foundation tranche are not closed; public workflows, fitted/recovery evidence, full regression, quality battery and final independent review remain owed. This is an honest active-work checkpoint, not a completion claim.
