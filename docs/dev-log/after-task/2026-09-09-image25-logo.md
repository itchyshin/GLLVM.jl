# After Task: Image 2.5 GLLVM.jl Mark

## Goal

Use the maintainer-selected Image 2.5 GLLVM.jl mark on the landing page in the
same product-identification role as the DRM.jl mark, without changing any
model, parity, or public capability claim.

## Implemented

The transparent Image 2.5 PNG is the standard VitePress navigation logo and a
decorative desktop landing-page mark. It is rendered through the landing
page's established CSS pseudo-element rather than nested VitePress hero-image
front matter, which this site configuration did not emit into the browser DOM.
The visible `GLLVM.jl` wordmark and headline supply the accessible identity;
the mark is decorative. The earlier vector mark remains in the source tree;
this is a new selected brand asset, not a destructive replacement of the
vector source.

## Mathematical Contract

N/A — documentation identity only; no model, likelihood, parameterisation, or
statistical claim changed.

## Files Changed

- `docs/src/assets/gllvmjl-mark-logo-image25.png` — selected transparent Image
  2.5 mark, SHA-256 `b56625bb16683ed62f97edcca5a23ab5acf36f9555ed629cd7eaad51552c76d3`.
- `docs/src/assets/logo.png` — navigation copy of that selected mark.
- `docs/src/index.md` — landing-page content, with no unsupported nested hero
  image configuration.
- `docs/src/.vitepress/theme/overrides.css` — selected PNG used by the
  existing decorative hero pseudo-element, visible at ordinary desktop widths
  and hidden only below 960 px. Its route is `/assets/...`, matching the
  VitePress staging path; it is not a site-root route.
- `docs/make.jl` — seeds Documenter's normal `build/assets` path before the
  VitePress writer moves the generated source tree into `build/.documenter`.
  This preserves the package's expected `build/1` deployment layout while
  making static images visible to pre-render link validation.
- `docs/dev-log/check-log.md` — evidence and build-boundary record.
- This after-task report.

## Tests Added

No package test is appropriate for a static image substitution. The rendered
homepage was checked for the hero mark route, and the navigation configuration was
checked for `/logo.png`. The independent review additionally verified both
build assets byte-match their source image and that the old SVG remains.

## Benchmark Numbers

N/A — no runtime or hot-path code changed.

## R-Parity Verdict

Parity: N/A — no R or Julia model surface changed.

## JET / Allocs / Aqua Verdicts

- JET: N/A — docs and assets only.
- Allocs: N/A — no Julia hot path.
- Aqua: N/A — no dependency, export, or project change.

## Checks Run

```sh
git diff --check
```

Exit 0.

```sh
shasum -a 256 docs/src/assets/gllvmjl-mark-logo-image25.png \
  docs/src/assets/logo.png
```

Both files: `b56625bb16683ed62f97edcca5a23ab5acf36f9555ed629cd7eaad51552c76d3`.

The selected PNG was inspected directly. A fresh local
`julia --project=docs docs/make.jl --local` run now passes Documenter's
cross-reference stage, including the three existing figure links that had
previously failed before rendering. Browser inspection later showed that nested
VitePress `hero.image` front matter was not emitted as a DOM image in this
configuration, so the final implementation uses the site's existing
decorative hero pseudo-element instead. Both emitted public assets match the
source SHA-256 above.

The final VitePress subprocess reached its normal build step but stalled
locally before emitting its bundle. The staged public tree nevertheless
contains `gllvmjl-mark-logo-image25.png`, and the staged theme CSS now points
at `/assets/gllvmjl-mark-logo-image25.png`.

## Consistency Audit

The landing-page decorative mark, navbar, and package wordmark retain one
visual family: the four response nodes, dark hexagon, and Julia-colour
accents. The old SVG is retained rather than removed. No language about 0.7
parity, bridge scope, or Destination B was changed.

## GitHub Issue Maintenance

No issue action needed. This is a maintainer-approved local documentation
asset update, without a deployment or release.

## What Did Not Go Smoothly

The ignored docs manifest needed an explicit local `Pkg.develop(path = ".")`
to resolve this checkout. The VitePress JavaScript subprocess then stalled in
this local environment after the Documenter stages had completed.

## Remaining Risks

- The new 681 KB PNG is duplicated under the hero and conventional navigation
  asset names; this is acceptable for a small static site but can be deduped by
  a future asset-pipeline change.
- The full local VitePress bundle has not completed because its JavaScript
  subprocess stalled. CI deployment remains the final render/publish check.
- The initial public browser check showed the selected navigation mark but an
  empty hero region at 1280 px because the prior CSS hid it below 1320 px.
  The breakpoint was corrected in that follow-up.
- A post-deployment GitHub Pages tree check found a second, distinct defect:
  the stylesheet used a site-root PNG route while Documenter stages the asset
  under `assets/`. The earlier assertion that a root-served PNG returned HTTP
  200 is withdrawn. This docs-only repair must pass the normal Documenter
  workflow before the hero is claimed published.

## Known Limitations

The logo improves visual orientation only. It does not imply `engine =
"julia"` coverage, gllvmTMB 0.7 parity, or Destination B completion.

## Next Command

Push this docs-only correction, wait for its Documenter workflow, then reload
the public landing page at a desktop viewport and confirm the selected mark is
visible to the right of the hero copy.

## Rose Verdict

Rose verdict: PENDING CI — the selected mark and breakpoint remain reviewed,
but the later asset-route defect invalidates the earlier published-hero claim.
The docs-only correction has a passing staged-artifact check; the normal
Documenter workflow and a fresh desktop view are required before completion.
