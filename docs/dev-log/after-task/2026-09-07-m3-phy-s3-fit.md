# After Task: M3-PHY-S3-FIT PrecisionPhy Gaussian fit

**Date:** 2026-09-07
**Owner:** Cursor/Ada
**Branch:** `cursor/m3-phy-s3-fit-20260907`
**Worktree:** `~/local-scratch/lanes/GLLVM.jl-m3-phy-s3-fit-20260907`
**Base:** `origin/main` `340f3832`

## Goal

Make the admitted `PrecisionPhy` payload actually fit on Julia:
`fit_phylo_gaussian(::PrecisionPhy)` on the S3a 8-tip fixture, compared to
the existing tree path at ≤ 1e-8. Destination B hygiene; diagnostic only.

## Implemented

`fit_phylo_gaussian` now accepts an admitted `PrecisionPhy`. The node state
is built from `Q` and `species_aug_id` (`PrecisionPhyFitState`); the
optimizer is the same finite-difference L-BFGS loop as the `AugmentedPhy`
path (CHOLMOD still blocks AD). Optional thin consume hook:
`bridge_fit(; phylo=payload)` returns a JuliaCall-flat NamedTuple with
`diagnostic_only = true`. This does not lift the R `phylo_rr` gate.

## Mathematical Contract

Univariate single-variance phylogenetic Gaussian
`y ~ N(μ·1, σ²_eps I + σ²_phy Σ_phy_unit)`, with `Σ_phy_unit = S Q_cond⁻¹ S'`.
`PrecisionPhy.Q` is already the root-dropped Hadfield & Nakagawa (2010)
precision (R order). The Woodbury / matrix-determinant identity is unchanged.
No new likelihood.

## Files Changed

`src/`

- `src/fit_phylo.jl` — PrecisionPhy fit state, shared L-BFGS, new method
- `src/bridge.jl` — `phylo` consume hook

`test/`

- `test/test_fit_phylo.jl` — S3a PrecisionPhy vs tree path

`docs/`

- `docs/dev-log/core070/phylo-transport-s3-julia-fit.md`
- `docs/dev-log/plan-actual/2026-09-07-m3-phylo-s3-fit.md`
- `docs/dev-log/after-task/2026-09-07-m3-phy-s3-fit.md` — this report
- `docs/dev-log/check-log.md` — append

## Tests Added

`test/test_fit_phylo.jl` S3a testset (17 assertions):

- admitted payload `n_leaves == 8`
- fitted `μ`, `σ²_phy`, `σ²_eps`, `negll` vs tree path at 1e-8
- interior matched-parameter nll at `(1.2, 0.45, 0.35)`
- joint (non-profiled) path
- `bridge_fit(; phylo=payload)` consume hook

Existing O(p) testset remains 13/13.

## Benchmark Numbers

N/A — 8-tip diagnostic fixture. No hot-path claim.

## R-Parity Verdict

Parity: N/A — change does not touch the public parity surface. No live R
engine call. Frozen-field meanings cite gllvmTMB 0.7.0
`b4d5fee64def88bc768dda1f1f77c29b295edd86` as documentation only. This is
not a v0.true-parity claim.

## JET / Allocs / Aqua Verdicts

- JET: not run locally — CI Julia shards are the gate
- Allocs: N/A — no inner-loop change claimed
- Aqua: not run locally — no `Project.toml` change

## Checks Run

Worktree `~/local-scratch/lanes/GLLVM.jl-m3-phy-s3-fit-20260907`:

```text
~/.juliaup/bin/julialauncher --project=. -e \
  'using Test, GLLVM; include("test/test_fit_phylo.jl")'
# fit_phylo_gaussian — O(p) single-variance phylo          | 13 / 13
# fit_phylo_gaussian — PrecisionPhy vs tree path (S3a)     | 17 / 17
# exit 0

~/.juliaup/bin/julialauncher --project=. -e \
  'using Test, GLLVM; include("test/test_bridge_phylo_precision.jl")'
# phylo transport S3a: Julia precision payload | 78 / 78
# exit 0
```

Fit-vs-tree abs Δ = 0.0 on μ / σ²_phy / σ²_eps / negll. Interior nll Δ = 0.0.
No Totoro. No R cpp. No `Pkg.test()` in this closeout sitting (CI shards).

## Consistency Audit

`rg` on the new after-task, receipt, and plan-actual:

- `true-parity|v0.true-parity` — fenced as not claimed
- `phylo_rr` — gate not lifted
- `340.?x|machine precision` — not used as a public speed/parity claim

README, CLAUDE.md, and capability-status rows were not edited.

## GitHub Issue Maintenance

No issue action. Diagnostic S3-FIT closeout only.

## What Did Not Go Smoothly

1. The 8-tip one-replicate MLE collapses `σ²_phy` near zero. Recorded as a
   small-sample property; interior matched-parameter nll is the non-boundary
   oracle.
2. Subagent cannot `move_agent_to_root`; work stayed in the named worktree
   by absolute path.

## Team Learning

When a tiny fixture collapses a variance MLE, keep the fit-vs-fit oracle
and add an interior matched-parameter nll so agreement is not only on the
boundary.

## Remaining Risks

- CI Julia shards + Documenter not settled at write time. Merge waits for
  those greens; Frozen R advisory fail is accepted.
- `bridge_fit` `family` is now optional at the signature and required unless
  `phylo` is set. Existing callers that pass `family` are unchanged.

## Known Limitations

- Diagnostic-only. Does not claim v0.true-parity, recovery, coverage, or
  performance.
- Does not lift the R `phylo_rr` gate.
- NB2 A11 remains **partial**.
- No two-part families; no large-p non-Gaussian structured dependence.

## Next Command

Wait for Documenter + Julia shards green, then merge. Do **not** start R
`phylo_rr` S3b.

## Rose Verdict

Rose verdict: PASS WITH NOTES — S3-FIT locally 17/13 + S3a 78/78 green and
fenced diagnostic-only; public parity is not claimed; full suite left to CI.
