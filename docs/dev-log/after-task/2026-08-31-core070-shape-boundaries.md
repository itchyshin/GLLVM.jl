# Frozen family shape boundaries

## 1. Goal
Resolve exact Tweedie/Student shape-boundary obligations without turning Julia
entry probes into fitted-model evidence or deleting documented extensions.
Core070+AGHQ programme ACTIVE; M1 PARTIAL.

## 2. Implemented
Recorded all33 frozen rejected family descriptors in family-boundary-contract.json.
Nine shape rows bind exact R/Julia calls and native outcomes;24 link/descriptor
rows remain unresolved. Added a50-assertion input/kernel test and wired it into
the central runner. Added source/process/contract verification and16 negative
controls. Family plan now binds2 native,1 formula and9 domain-boundary cases;
85 specifications remain unbound. No numerical engine changed.

## 3a. Decisions and Rejected Alternatives
R rejects Tweedie1,2,Inf,NA and vectors; Julia rejects their corresponding named
fitter inputs before reading responses. R rejects Student1,Inf,NA and vectors.
Julia rejects nonfinite nu, but already documents finite fixed0<nu<=1 and
trait-length fixed-nu vectors. Preserve these two extensions. Do not impose R
constructor syntax or call a successful entry probe a healthy fitted model.
The vector extension uses two traits and nu[2,3]; a wrong-length vector rejects.

## 4. Files Touched
New family boundary contract/evidence, test/test_core070_shape_boundaries.jl,
test/runtests.jl, two boundary verification tools, family case-plan generator and
bindings, decomposition verifier/documentation, check-log and checkpoint.
Mission Control Julia section only; R fields preserved.

## 5. Checks Run
All69 frozen R family descriptors replayed from exact source using R4.6.0.
TotoroJulia1.12.6 one thread:50/50 assertions in10.190s process time, including
package compilation. First50/50 run10.240s retained; replay added an explicit
loaded-package-root assertion. Both reference-library checks before/after PASS.
Estimate<=60s, Julia hardcap120s; no fits or scientific campaign. Test totals:
9 declared outcomes,5 adjacent controls,36 independent Cauchy density comparisons
for the Studentnu1 extension. Full package checks/Documenter NOT RUN.

Prior actual NB2 required58-assertion evidence remains current and reverified.
Family decomposition archived-red check,11 regressions and aggregate selftest
PASS; no full-family promotion. Unlazy2 gates freshly reverified.

## 6. Tests of the Tests
Eight contract corruptions and eight copied-artifact corruptions rejected:
missing/duplicate cases, wrong Julia call, stale source/fixture, false full-parity
status, relabelling extensions or unresolved rows, missing/altered raw evidence,
source drift and omitted processes. Wrong Julia call initially slipped through
validation; the negative test failed and exact call matching repaired it.
Read-protected matrices prove invalid inputs reject before response access;
valid controls prove the entry point itself is reachable. Raw R replay and
Julia output are hashed; source archive, environment and loaded root verified.

## 7a. Issue Ledger
Nine native domain dispositions resolved at input/kernel scope. Integration into
the complete required evidence collection remains pending. Twenty-four other
rejected descriptors, remaining fitted family models/interfaces and Student R
optimizer/density issues remain open. These checks do not repair Student fitting.

## 8. Consistency Audit
Main manifest stays DRAFT;752 source facts/698 nonexcluded still lack complete
executable coverage. Planned source links remain separate from executed coverage.
Existing Student docstrings explicitly justify both retained extensions; no API
or likelihood edit required. Mission Control e1bc274 HTTP200/readback PASS,
R0.7.1 fields unchanged, exact-file lease released. Rose independent review NOT RUN.

## 9. What Did Not Go Smoothly
The first run lacked an explicit loaded-source assertion; strengthened and
replayed rather than inferring the loaded root solely from the manifest path.
The exact-call corruption exposed a verifier gap, now covered. Both first and
second execution records retained. No private history or foreign work modified.

## 10. Known Residuals
Input admission and a scalar density identity do not establish fitting, recovery,
coverage, formula reachability or R bridge behavior. The full required collection
must bind these domain receipts before a frozen completion claim. No17-family
full replay, AGHQ implementation, large validation or documentation render here.

## 11. Team Learning
A reference rejection may correspond to an intentional Julia extension. Record
the domain and evidence type explicitly; never mark an entry probe as fitted
parity. Name the two-trait shape when testing a vector that R cannot accept.
Parent only; no new production child, model receipt or invented agent-hours.

## 12. Cross-Product Coverage
This does NOT cover full Core+AGHQ, general Student/Tweedie fits, public bridge,
remaining covariance/multinomial, coverage, performance or polished Documenter.
R0.7.1/article/foreign Julia lanes protected. No push/merge/release/cleanup.
