# After-task — loading masks and known covariance

## 1. Goal
Establish exact frozen-R loading-mask and known-covariance contracts needed by
the approved Core070+AGHQ manifest. Programme ACTIVE, M1 PARTIAL; no full freeze.

## 2. Implemented
Seventeen public-call fixtures, retained prepared maps/rejections, sixteen
Gaussian fixed-point density/derivative comparisons, verifier and corruption
tests. Added17 classified reference obligations to the central source census.
No production Julia or R source changed.

## 3. What Changed the Next Action
Native covariance implementation must respect fixed raw loading pins, ignored
upper-triangle cells and additive fixed known covariance. All-fixed loadings
retain random effects. Separate a known Poisson log-predictor covariance from
Gaussian response sampling error. Next: finish source/rank/modifier/admission
crossings in the finite required-case manifest, then its independent scope audit.

## 3a. Decisions and Rejected Alternatives
Kept exact R preparation jitter1e-8I and preserved all failed runs. Did not
strip masks, reinterpret marker-only calls, admit documented list matrices
that the actual multivariate path rejects, or call known-Poisson admission a
Gaussian numerical pass. No arbitrary SPD/nonfinite admission claim.

## 4. Files Touched
New masks-known fixture, point runner, verifier/tests, leaf, contract, subset,
evidence and report; central source index/map/count tests and shared logs.
Mission Control changed Julia next-action text only; local vault commit
f0468523a1fad7ad9fb30a2761bdf09c36ead0ee, served HTTP200/readback PASS; all
R fields unchanged and exact-file lease released.

## 5. Checks Run
One-thread Totoro R4.5.3/TMB1.9.21 with frozen oracle before/after every batch.
Capture1 exit1 in1.167s: three diagnostic strings differed, all admission
outcomes as expected. Capture2 exit0 in1.167s:17/17. Points exit0 in0.917s:
16/16; max absolute normalized nll delta2.273736754e-13, scaled FD discrepancy
3.810410862e-9, within predeclared1e-6/1e-5. Estimates<2/<3min respected.
Six verifier tests and six central census tests pass. Previous9-mode verifier
still passes after reference-link additions. No optimizer/full package suite,
Documenter build, recovery, performance or DRAC campaign ran.

## 6. Tests of the Tests
Twenty-one corruptions reject across six tests, alongside real positive evidence:
seven point/map defects, six retained observation/matrix/coordinate defects,
three process failures, four summary/dependency failures, and one attempt to
promote the historical failure. Test module was absent at first (red import);
initial verifier then failed real positive evidence on R factor group ordering.
Corrected lexicographic group codes, retaining original row order. Fresh positive
and mutation checks pass with ResourceWarnings treated as errors.

## 7a. Issue Ledger
Original binomial seed43/k5, Student seed71 health, default-unique native
parameter mismatch and remaining AGHQ obligations remain unresolved. Native
covariance, structured multinomial, data/postfit, bridge, recovery, performance,
full package verification and visual Documenter remain programme requirements.

## 8. Consistency Audit
Source census769, nonexcluded715 (previous752/698). Exactly17 new source rows
carry reference model IDs, not executable parity IDs. Full-manifest false-freeze
tests still reject. Matrix checks inspect actual retained source and marginal
entries, response values, trait/group order, fixed/free maps and coordinates.
Unlazy MK-CONTRACT/MK-NEGATIVES/MK-CENSUS freshly reverified3met/0unmet/0abandoned.

## 9. What Did Not Go Smoothly
Three diagnostic strings copied from source did not include CLI formatting or
the exact wording. Captured first failure remains immutable; only matching
strings changed. Python initially assumed site codes equalled row order; R
factor labels instead sort u1,u10,...,u2. Independent checks now reflect that.

## 10. Known Residuals
These are reference-only intercept models, p3/n18/rank2 with one animal matrix
and four known-V fixtures. Sparse/tree/spatial/multi-block/slopes crossings,
other ranks/families and general known-V validation are not established.
Loading sign symmetries can remain; no identified-fit or recovery claim.

## 11. Team Learning
Noether independent fresh Terra/high review dispatched via canonical tiered CLI,
read-only, receipt retained. Verdict: no numerical correctness finding in this
bounded scope; independently ran the evidence verifier successfully. His
corruption-test replay could not create temporary directories in the read-only
sandbox; parent fresh writable-lane replay passes all six tests. No repair
followup needed and no Rose programme signoff. Parent command times are measured, agent-hours not invented.
Efficiency gate reported existing guardian limits; preserve a durable checkpoint
for the disk-goaled continuation rather than silently dropping work.

## 12. Cross-Product Coverage
Eight loading-mask admissions/rejections plus nine known-covariance cases;
eight Gaussian prepared models x two parameter points. This slice does NOT cover
native/formula/bridge parity, full covariance cross-products, Poisson density,
optimizer convergence, recovery or calibrated inference. A passed subset does
not make the approved Core070+AGHQ objective complete.
