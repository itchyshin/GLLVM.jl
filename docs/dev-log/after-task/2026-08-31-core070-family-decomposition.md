# Family scope and freeze-check repair

## 1. Goal
Advance the frozen source-to-case map while preventing admission-only checks
from satisfying complete family coverage. Full Core+AGHQ goal remains ACTIVE;
Milestone1 PARTIAL, full manifest DRAFT.

## 2. Implemented
Decomposed69 family facts into97 typed planned cases:63 native/formula/bridge,
one alias and33 rejection boundaries;14 constructor-only exclusions preserved.
Master map records planned links separately from executable links. Repaired the
freeze checker so an admitted family requires fitted native and interface cases
for matching model contracts. Multiple complete model variants remain allowed.

## 3a. Decisions and Rejected Alternatives
No assumed covariance/data Cartesian product. No forced Julia rejection of
valid extensions merely because R rejects a descriptor. No promotion of
historical entry probes, old candidate fixtures or plan text to passing evidence.
The two verified original native NB2 cases bind exact calls, fixtures, seeds
and receipts. The other95 specifications keep missing calls and fixtures null.

## 4. Files Touched
tools/core070_manifest_coverage.py; dedicated family coverage regression,
deterministic case-plan builder/verifier and archived-red verifier; generated
family-required-case-plan.json, planned links in required-source-case-map.json,
family-case-decomposition.md, scoped evidence/report, check-log and checkpoint.

## 5. Checks Run
Pure local metadata checks only; no fits or scientific compute. Archived parent
62e59441 accepts the invalid entry-only family mapping, producing the expected
regression failure with exit1. Repaired checker passes5 family tests plus6
existing coverage tests. Aggregate selftest passes. Saved source index rebuilds
identically:752 facts,698 non-excluded, all executable links still empty.
Deterministic plan verification confirms69 facts and97 planned cases.

## 6. Tests of the Tests
Entry-only acceptance fails before repair. New negatives reject missing formula
coverage, entry-level evidence, model substitution, missing fixture/call/source
binding and unresolved Julia disposition. Positive synthetic cases allow all
three interfaces, multiple complete models and documented Julia extensions.
These synthetic positives are checker tests, not package capability evidence.
Unlazy final reverify2/2; leaf written before repair, bound after initial tests.

## 7a. Issue Ledger
Closed the structural family entry-probe loophole in freeze validation. Native
case specifications, formula/bridge implementation, reference-boundary decisions
and other source domains remain incomplete. Student health/density still open.

## 8. Consistency Audit
No numerical source, parity fixture, fitted tolerance or R reference changes.
The earlier NB2 required pair remains independently verifiable at its own
scope. Full manifest remains DRAFT. Planned case count is never a count of
passing capabilities or all required fits. Independent Rose review NOT RUN.

## 9. What Did Not Go Smoothly
Initial inventory query expected a domain field that did not exist; corrected
to source-ID prefixes. The first code draft required one model contract per
family; corrected before closure to allow multiple complete interface triplets,
with a positive regression. Source-map decomposition still needs substantive
model decisions; generating IDs does not resolve those decisions.

## 10. Known Residuals
698 non-excluded source facts still lack complete executable coverage. The97
case specifications include two existing native-runner bindings; the other95
are not runnable cases yet. Formula and bridge remain unpaid for both bindings. Existing native fixtures
are candidate leads, including known link, shape and multinomial mismatches.
Full package checks, scientific campaigns and Documenter rendering unpaid.

## 11. Team Learning
Check the meaning of a coverage edge, not only that its target ID exists.
Admission, model fitting and interface behavior carry different obligations.
Parent execution only, no new child/panel and no invented model-hours.

## 12. Cross-Product Coverage
This does NOT cover any newly passing numerical family, formula, public bridge,
AGHQ estimator, complete manifest freeze, recovery or performance claim.
Protected R0.7.1/article/foreign lanes untouched. No push, merge, release,
destructive cleanup, R engine edit or new remote job.
