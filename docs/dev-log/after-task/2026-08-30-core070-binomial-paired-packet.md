# Core070 paired binomial packet — prepared, not fitted

## 1. Goal
Turn missing probit/cloglog and multi-trial obligations into exact executable model cases without substituting source checks for numerical parity.

## 2. Implemented
Six case definitions, fixed seeds90101–90106 and dimensions3traits/160units/1factor, single-case Totoro runner, explicit observed-curvature likelihood/health gates and retained dataset/metrics/case receipt design. Added source-only preflight and malformed-contract tests; linked packet from master draft/catalogue.

## 3a. Decisions and Rejected Alternatives
No seed selection or fits locally. R DGP uses independent plogis/pnorm/expm1 probabilities; realizations will be saved before either fit. Explicit observed cloglog matches the reference estimator and does not claim equivalence of Julia default Fisher. No full-cohort launch before measured sizing and approval when over30min.

## 4. Files Touched
binomial-paired-contract.toml, binomial-paired-plan.md, binomial-paired-preflight.json; binomial_case_contract.jl, test_binomial_case_contract.jl; core070_binomial_paired.jl and core070_verify_binomial_packet.py; master/catalogue/README and execution records.

## 5. Checks Run
26 local preflight assertions PASS, actual CLI --check PASS. Actual --execute with opt-ins off exits1 for missing required mode before numerical imports. Source and syntax checks only. All process plans, pins, outputs and exits retained. No DGP or fit run; no RCall qualification.

## 6. Tests of the Tests
Eleven malformed-contract tests reject missing/duplicate cases, changed seed, relabeled link/trials, Fisher substitution, weaker likelihood tolerance, unearned PASS, missing/corrupt source pins and wrong R reference. Unknown-ID check also rejects. Live opt-in rejection tests the actual runner entry point.

## 7a. Issue Ledger
Three family obligation groups now have prepared paired fixtures but remain numerically unpaid. Required logLik1e-6 and gradient1e-4 gates preserved. Full finite covariance/data/interface expansion and M1 review still outstanding. Remote previous job UNKNOWN; socket check empty, no restart.

## 8. Consistency Audit
All six rows remain NOT_EXECUTED. Independent evaluator/provenance designed, not certified by preflight. Source pins include runner, validator, shared oracle and preparation helper; runtime inventory additionally includes engine/environment/contract. No original fixture or likelihood tolerance changed.

## 9. What Did Not Go Smoothly
No authenticated Totoro socket is available. Preparation proceeds locally, but missing observation is not terminal evidence for the existing remote job. A source check cannot verify R macro expansion, optimizer behavior or mode convergence, so no numerical status promotion.

## 10. Known Residuals
No dataset generation, fits, runtime timing calibration, numerical runner execution, same-point likelihood validation or independent review. Packet is separate from17-ID smoke and must later be incorporated into the full finite manifest. Provisional2–8min per case and12–48min cohort need pre-run calibration.

## 11. Team Learning
Freeze model identity and failure accounting before generating a fixture. A guard that stops before toolchain import is directly testable without risking an accidental fit.

## 12. Cross-Product Coverage
This does NOT cover full Core/AGHQ, shared-X, formula/public bridge, default cloglog equivalence, inference, recovery, performance, docs rendering or release. Rose NOT RUN; M1 PARTIAL. No new production child, push, merge, release, foreign-lane edit or cleanup.
