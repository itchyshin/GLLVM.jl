# Core070 same-model delta required parity

## 1. Goal
Replace two required mismatched-dispersion diagnostics with actual frozen-R model comparisons, preserving the original fixtures.

## 2. Implemented
Callable same-DGP delta checker, required wrappers, runner/helper mappings, corresponding draft manifest rows and complete transitive execution pins. Scoped evidence verifier, negative controls and contract.

## 3a. Decisions and Rejected Alternatives
Use existing species dispersion and shared predictors. Preserve old shared-dispersion diagnostics for optional developer use. Do not count a higher R likelihood as equality. Tighten public R optimization with identical data/maps instead of changing engine, seed or tolerance.

## 4. Files Touched
Two required wrappers, parity runner/helper, Python aggregator input inventory, delta checker/verifiers, draft manifest and developer records. Numerical src and R oracle unchanged.

## 5. Checks Run
Totoro default attempt: each family10 DGP checks pass and13/14 health/model checks pass; both R gradient gates fail. Refactored attempt world-age error before fits retained. Final required runner48/48 PASS in30.40s, original data/15 parameters matched, R integrity before/after PASS. Seven negative controls, eight collection tests and aggregator selftest PASS. Both Unlazy gates reverified.

## 6. Tests of the Tests
Default convergence flags did not hide R gradients0.0006205/0.0016053. Tight controls reduce them to3.6795e-5/4.3240e-5. Negative controls reject false full completion, stale reference, missing process, altered data, invented gradient, shared dispersion and corrupt process. Existing collection tests retain nonzero exit/omission rejection.

## 7a. Issue Ledger
Two required same-model cells qualified. Master contract remains DRAFT, M1 PARTIAL; Student health, remaining exact obligations, original branch-RE environment and integrated broader evidence unpaid. No new production child or external-review retry.

## 8. Consistency Audit
Required IDs and wrappers align across runner/helper/manifest; tools and historical DGP bytes enter execution inventory in Julia and Python. Full contract status unchanged. Existing diagnostics retained verbatim. No loading-sign equality requirement or universal performance claim.

## 9. What Did Not Go Smoothly
Discovery found inaccurate blanket fixture dimensions and stale delta mismatch assumptions in draft rows. Callable DGP extraction hit Julia world age; returning evaluated data directly fixed harness plumbing. First Unlazy run selected old Python lacking tomllib; gates now pin qualified Python3.13. All failures remain recorded.

## 10. Known Residuals
No full package suite, recovery/coverage, wider delta capabilities or independent numerical review. Tighter R controls are part of these exact accepted calls; do not claim default R fit health. Current helper/contract changes invalidate prior execution-inventory receipts until revalidation.

## 11. Team Learning
Test names and successful exits do not prove model equality. Existing implementation can exceed stale fixture coverage; audit grouping and acceptance assertions before adding engine code. Explicit interpreter pins prevent shell-dependent verification.

## 12. Cross-Product Coverage
This does NOT cover complete Core/AGHQ parity, R bridge, formula or data combinations, calibrated intervals, performance, whole-site Documenter or release readiness. Foreign work and R0.7.1/article lanes untouched.

Rose verdict: NOT RUN; parent qualification only, no milestone sign-off.
