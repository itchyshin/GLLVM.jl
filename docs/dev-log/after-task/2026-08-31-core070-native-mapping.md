# After-task — native mapping and five-host DRAC access

## 1. Goal
Advance the approved Core070+AGHQ implementation by locating reusable native
kernels and exact missing interfaces; verify the newly confirmed DRAC access.
Programme ACTIVE/M1PARTIAL/fullmanifestDRAFT. No numerical completion claim.

## 2. Implemented
24-row source-pinned native mapping, four concrete existing internal evaluator
calls, shared long-observation covariance equation and implementation boundary.
Recorded live reachability/account checks for five DRAC clusters and routing
limits. No public API, numerical engine, dependency or scheduler job changed.

## 3. What Changed the Next Action
B1/B2 must preserve arbitrary source-group projections and independent residual
noise, not reuse trait-axis or matrix-normal APIs as if equivalent. The existing
spatial fitter has free loading and common field scales; establish identified
shared tau=1 matching the reference before calling that route parity-ready.

## 3a. Decisions and Rejected Alternatives
Keep proposed public constructors explicitly unbound rather than inventing
callable API names in the manifest. Four existing evaluator calls are candidates
for the new fixtures, not newly passed numerical results. Do not repair or
silently change the public spatial estimator during this mapping-only slice.

## 4. Files Touched
structured-native-mapping.md/json, drac-five-host-routing.md/readiness.json,
this report, check-log and checkpoint. Runtime mapping gates and connection
receipts are retained in ignored execution state. Mission Control correction
is recorded at closure. Other lanes and R engines remain untouched.

## 5. Checks Run
Source files and all24 original reference rows/roles hash-checked; four candidate
calls retain fixed-parameter-only status and zero public-fit claims. Previous
structured-input reference verifier still passes. Five existing SSH sockets
responded: Fir,Nibi,Rorqual,Trillium,Narval; sbatch/account reads succeed, user
queues empty. No compute, fits, RCall startup or job submission ran.

## 6. Tests of the Tests
This is a reversible mapping/documentation change, so no mirrored unit tests
were added. Existing numerical and source-receipt guards remain unchanged.
Mapping gate requires exact24case inventory, unchanged source pins/roles,
four named internal candidates and no promoted public/native status. The
independent review checks mathematical reuse, rather than treating the mapping
gate as evidence of model correctness or executed methods.

## 7a. Issue Ledger
Spatial shared-scale nonidentifiability identified from current parameterization;
no optimizer regression has yet run. Source-mode fitting, formula/bridge routes,
reference Psi loss/pedigree diagnostics and all prior numerical debts remain
unpaid. Five-host access does not establish compatible scientific environments.

## 8. Consistency Audit
No executable parity row added and no source obligation removed. All statuses
remain honest about missing public fitting. Dense covariance versus augmented
precision, common versus shared fields and residual independence remain distinct.
No full suite, recovery, coverage, speed or Documenter claim was made.

## 9. What Did Not Go Smoothly
Initial review launch used a misspelled events path and failed before dispatch;
corrected launch uses the intended isolated runtime directory. No duplicate
reviewer was started. General-source reuse requires new public plumbing despite
several existing functions having related names.

## 10. Known Residuals
Proposed source-object constructors still need exact B1/B2 binding. Native
fitting and downstream post-fit recovery, all interfaces, full manifest scope
review and all programme milestone panels remain required. DRAC availability
is a timestamped observation; recheck before dispatch and report failures.

## 11. Team Learning
Ask-brain MCP all-project search retrieved drac-setup; its five general-purpose
host list was checked live rather than assumed. Noether Terra/high fresh CLI
review checks source equations, concrete calls and the spatial scale finding.
Final reviewer disposition is appended below. No production child or programme
completion panel launched; parent owns the mapping.

## 12. Cross-Product Coverage
24structured reference obligations mapped, four current internal fixed-parameter
candidates, no claimed public fitted routes. This slice does NOT cover execution
of these new native fixture calls, native model fitting, inference, missing-data
extensions, non-Gaussian crossings, bridge qualification or final Core parity.

Noether verdict: scoped acceptance of all four candidate dimensions, covariance
axes/general source equation and the spatial parameter-redundancy finding.
Applied five reader/precision corrections: full explicit group-matrix calls,
qualified internal names, explicit Symmetric inverse wrappers (after valid
source construction), formula keyword-forwarding distinction and parenthesized
coevolution contrast. No fit or optimizer-failure claim. Review Terra/high,
fresh ephemeral/read-only,12:18:29–12:22:02UTC,exit0; no production child.

Mission Control 8972f48d687d25c6dbed8957669a13bf3e504332 Julia-only update:HTTP200/readbackPASS,
Rfieldsunchanged, exact-file leaseRELEASED. NM-MAP/NM-REFERENCE freshly
reverified2met/0unmet/0abandoned; allreviews and checks terminal.
