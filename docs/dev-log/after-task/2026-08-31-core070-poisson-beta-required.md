# Original Poisson/Beta required-runner integration

## 1. Goal
Integrate the qualified original Poisson/Beta checks into their registered
required cases. Programme ACTIVE, M1 PARTIAL; full manifest remains unfrozen.

## 2. Implemented
Two wrappers execute all16 health assertions each through runparity.jl.
Registry, runner and contract retain the same17 family IDs plus one formula ID.
The execution inventory now includes the shared helper, original DGP fixtures
and explicit R-refinement contract. No engine or original-fixture changes.

## 3a. Decisions and Rejected Alternatives
Keep original native fits and their models. Retain default R gradient failures,
then use the already qualified public start_from/nlminb policy. Exact body
comparison against a79fa9b4 guards against mathematical drift during packaging.
A standalone diagnostic packet cannot substitute for required-runner evidence.

## 4. Files Touched
New parity helper/wrappers, required contract and four immutable evidence
summaries; registry, runner, inventory and family-case plan; local verification
tools, this report, check-log and checkpoint. Julia-only Mission Control update.
Historical receipts/summaries remain available. No foreign-lane files changed.

## 5. Checks Run
Totoro Julia1.12.6, R4.5.3, TMB1.9.21, frozen R b4d5fee, one thread per job.
Required Poisson/Beta32pass37.863s; NB2/truncated-NB2/formula58pass63.185s;
shape boundaries50pass10.592s; ordinal/native link boundaries70pass23.064s;
original ordinal diagnostic5pass22.120s. All oracle-before/after checks pass.
Frozen R69descriptor replay passes separately. Pre-run estimate under two
minutes per targeted job; at most three concurrent. All jobs terminal.

Poisson/Beta health reports and raw R bytes exactly reproduce the prior qualified
packet. Default R gradient failures remain retained. Refined R gradients are
3.453e-5 and1.380e-5, below1e-4; native gradients, objective re-evaluation,
same-point likelihoods, parameter counts and source pins pass. No full suite,
Documenter build, recovery or coverage campaign was run.

## 6. Tests of the Tests
Registry red:22 pre-existing assertions pass and two new routing assertions fail;
green24pass. Twelve artifact/route corruptions and26 metric corruptions reject,
including missing case/dependency, stale contract, old light fixture, raw-fit
corruption, nonzero exit and standalone substitution. Eleven coverage regressions,
archived freeze-gap reproduction and aggregate self-test pass. Three scoped
Unlazy gates freshly reverified; source-bound summary regeneration also passes.

## 7a. Issue Ledger
The two original Poisson/Beta cases are now required-runner health checks.
Family plan:4 native+1 formula+16 boundary bindings,21 bound and76 unbound
of97 planned cases. Seventeen link dispositions remain unresolved. Original
Student-t R health/density failure remains open. No full-family promotion.

## 8. Consistency Audit
Prior summaries stay historical; current summaries use the new shared-harness
source pins and exact receipt hashes. All main executable mapping links remain
empty until full contracts are frozen. Required runner count18 is not a full
programme capability count. Mission Control readback is recorded in the leaf's
mission-update.json; R fields are protected by an exact-field comparison.

## 9. What Did Not Go Smoothly
The corruption-test driver initially did not recognize the verifier's typed
EvidenceError, although missing evidence was correctly rejected. Its registry-log
parser also assumed one aggregate summary instead of three Julia testsets.
Both test-driver errors were repaired; no numerical or acceptance tolerance
changed. The first Unlazy failure remains in the runtime history.

## 10. Known Residuals
No Poisson/Beta formula/bridge or recovery qualification. Public AGHQ, structured
multinomial, full covariance/data/postfit mappings, performance and final docs
remain unpaid. Original phylogenetic CI failure still needs exact-version
reproduction. Full-suite and independent programme panels remain open.

## 11. Team Learning
Parent-only integration, no new production child. Noether's prior Terra/high
review covered the identical qualified objective body and explicit R policy;
this wiring slice has fresh runtime verification but no new independent panel.
No model-routing receipt or aggregate agent-hours are inferred from planned
labels. B production remains behind the programme checkpoint.

## 12. Cross-Product Coverage
This does NOT cover full Core+AGHQ parity, R0.7.1, calibrated coverage,
performance or polished Documenter. No push, merge, release, R engine edit,
DRAC submission or destructive cleanup. Cursor/Claude/unknown Julia lanes,
gllvmTMB_main0.7.1 and the article lane remain protected. Census must be refreshed
before any disposition decision; the current slice did not refresh it.

Mission Control local commit 6aa79144af86ed8a9cc6d5b30f660a31443e101b: HTTP200, exact served Julia field verified; R fields unchanged; exact-file lease released.

Lifecycle audit returned its existing long-session warning (19 compactions for
this task). The approved disk-goal checkpoint-and-roll exception is used; this
slice is committed and backed up before further work. This is not a green
lifecycle audit or a reason to claim programme completion. No new task was
created without user instruction.
