# Executed Gaussian source guide — interim checkpoint

## 1. Goal
Execute the fixed-source Gaussian guide on the current candidate and inspect
its rendered desktop/mobile presentation. Core070 remains M1 PARTIAL; the full
manifest is DRAFT_NOT_FROZEN. This is not a programme completion report.

## 2. Implemented
Reworked the model chooser and covariance-axis explanation, added stable local
anchors and an executed deterministic analytic example. Updated README,
changelogs and source-fit docstring to reflect scoped numerical evidence.
No numerical algorithm or likelihood parameterization changed in this slice.

## 3a. Decisions and Rejected Alternatives
Use the existing Documenter/VitePress site. Keep source covariance distinct
from covariance among observed rows. Use analytic checks, not an invented
recovery claim. Preserve the six-case reference and all original tolerances.
Totoro build budget was bounded below five minutes, one Julia/BLAS thread.
Do not call a successful build zero warnings or a full-site sign-off.

## 4. Files Touched
README.md, CHANGELOG.md, docs/src/changelog.md, structured-dependence.md,
src/source_fit.jl (docstring only), check-log, LOOP checkpoint, source status
and evidence JSON, and the rendered figure-audit report. Raw runs/screenshots
are retained in .unlazy/core070-aghq/source-docs-01 and source-docs-02.
Only Mission Control's Julia fragment changed; commit4c4cf4b and HTTP200
readback confirmed all other fields unchanged.

## 5. Checks Run
Exact numerical source replay:117/117 unit assertions, six paired public-R
fits, both-engine fit health and R endpoint cross-evaluation passed. Strict
Documenter first build88.48s; final display-only revision89.01s, exit0. Final
example executes analytic value and convergence assertions. Eight screenshots
cover desktop1440x1000/mobile390x844 top, source, example and equation.
No page overflow or broken local hash links. Fresh Unlazy verification:
source-docs02 2/2 exit0; source checkpoint6/6 exit0; six-source-pair1/2 exit1
because its independent-review/recovery gate is still unmet. The latter is
not hidden or counted as completion. Totoro and Fir/Nibi/Rorqual/Trillium/
Narval hostname probes all returned exit0; no DRAC compute or fresh login.

## 6. Tests of the Tests
Seven source receipt negative controls reran successfully. Combined verifier
checks source/environment pins, original missing-API baseline, process exits,
117 assertions, six required IDs, final rendered values and screenshot census.
Historical first-build source bytes are checked against their archive when
only documentation changed. Implementation preceded valid red in recovered
source code; retrospective baseline evidence does not repair TDD ordering.

## 7a. Issue Ledger
First render's mobile chooser clipped model links; reduced to two columns.
Single-line output clipped; split into readable values. Corrected assert macro
syntax to explicit isapprox(...; atol=...). Independent source completion
review, recovery/coverage and the full package suite remain outstanding.

## 8. Consistency Audit
Numerical source is unchanged between the replay and final build. Deterministic
example is labeled analytic; source covariance axes and matrix orientation are
explicit. Six fitted comparisons are not all-source parity. Unique-Psi near-zero
curvature remains visible. Older changelog's misplaced heading was consolidated
without removing its changes. No R0.7.1 or article-lane files changed.

## 9. What Did Not Go Smoothly
Initial desktop success did not predict mobile readability; failed screenshots
remain retained. Local versions.js is absent and theme uses its local fallback.
Branding assets/default package.json/bundle-size warnings remain. One code line
scrolls within its code block; the page itself does not overflow. No warning
was suppressed to claim a clean build.

## 10. Known Residuals
Next qualify full package checks and finish the frozen capability manifest,
then remaining source/multinomial/AGHQ/data/post-fit/bridge contracts. Original
binomial/Student-t/default-unique failures and spatial scale identification
remain. No new hours forecast:70–120 working hours was a provisional allowance,
not measured remaining work. Large runs still need sized pre-run approval.

## 11. Team Learning
Documentation child requested Terra/medium, owned only the guide and returned
ownership. Parent supplied corrections and Florence/Pat/Noether review lenses;
these are not independent completion-panel verdicts. No actual model usage or
agent-hour receipt invented. Totoro can reconnect directly with key auth/no Duo,
as the user and brain runbook specify; DRAC retains the shared-session rule.

## 12. Cross-Product Coverage
This slice does NOT cover full-site search/navigation/gallery inspection,
new figures, calibrated intervals, recovery/coverage, formula/bridge source
parsers, non-Gaussian or estimated/spatial source models, full package/Aqua/JET
checks, broad performance or full Core070+AGHQ parity. No push, merge, release
or destructive cleanup. Rose verdict: NOT REQUESTED; interim checkpoint only.
