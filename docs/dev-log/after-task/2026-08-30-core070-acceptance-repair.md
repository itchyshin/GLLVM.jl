# Core070 acceptance repair checkpoint — PARTIAL

## 1. Goal

Make first-tranche evidence trustworthy on the actual Julia/R runtimes and
resolve the concrete interim-panel findings. This is not Milestone1 completion.

## 2. Implemented

Julia1.10/1.12 receipt-count compatibility, loaded-source equality, exact frozen
R Hessian-field lookup, and supervisor prevalidation/exception cleanup.
A separate frozen39-case R AGHQ control subset and source contract clarify B6.

## 3a. Decisions and Rejected Alternatives

Keep the original Student-t fixture, absolute likelihood tolerance and failed
R optimizer-health outcome. Do not promote old Tweedie snapshots. Reference
AGHQ comparisons explicitly disable the default finite loading ridge. Do not
claim the frozen-node AD gradient equals the total re-adapted derivative.

## 4. Files Touched

Parity receipt/health helpers and pure tests; targeted process supervisor and
fault-injection tests; AGHQ source-control manifest, replay tool and evidence
summary; check-log/checkpoint. Mission Control changed only two Julia fields.
No R engine or protected lane changes, push, merge, cleanup or release.

## 5. Checks Run

Candidate129f6043 qualification failed on Julia1.12 TestCounts. After repair,
e5e204ef qualification passed; paired Tweedie stopped on the exact-field bug,
and Student31pass/2fail retained the frozen R optimizer failure.
Candidate6e59ef54 qualification passed on TotoroJulia1.12.6: receipt24/24,
Tweedie metadata25/25, Student metadata8/8, marginal branch13/13 and original
branch44/44; R-health logic and exact-oracle startup also exit0. Raw logs and
hashes independently read back. This qualification took about61seconds.
Repaired paired replay: Tweedie28/28 PASS in710.62s, all three powers and
health/count gates; maximum absolute likelihood delta5.35840e-8. Student31pass/
2fail in34.4s, original delta0.00073058937 but Rcode1 false convergence persists.
Combined batchFAIL. Raw run/cell/process receipts and hashes read back intact.
The frozen39 R AGHQ helper/control checks passed on R4.6.0 and TotoroR4.5.3.

## 6. Tests of the Tests

Source-root mismatch fails. Recursive test counts retain nested failures.
Exact sd_report lookup rejects partial names and atomic diagnostics. Supervisor
fault injection proves malformed timeout settings launch nothing and unexpected
wait errors kill/reap and retain a failed receipt; prior timeout/nonzero/missing/
stale tests pass. AGHQ subset rejects omitted cases, stale sources and false
assertions. Unlazy reverified both supervisor and A3 checker gates; other gates
remain unmet. An initial ledger-relative cwd error was retained, then corrected
by explicitly binding the repository cwd; a status query was not used as proof.

## 7a. Issue Ledger

Resolved with regression evidence: TestCounts API change, wrong loaded checkout,
R partial-field match, supervisor validation after child launch. Open: external
process-result binding in the future full aggregator; complete frozen capability
manifest; Student reference health; original failing CI environment; full-suite
and broader capability validation. The draft aggregate remains fail-closed.

## 8. Consistency Audit

Rose's bounded repair follow-up found the three parity fixes correct on
inspection and identified the supervisor defect now repaired. This is not full
signoff. The AGHQ annex separates helper tests, public-fit admission, numerical
estimation and recovery. R0.7.1 and article fields/work remain independent.

## 9. What Did Not Go Smoothly

Pure local tests missed a Julia1.12 API change and the real fitted R object's
field layout. Fresh qualification and actual paired execution caught both.
The supervisor advertised broader failure cleanup than it implemented. Every
failed attempt and its raw exit/log remain retained rather than rewritten.

## 10. Known Residuals

Milestone1 is incomplete. Native/formula/bridge parity, public AGHQ, covariance
and multinomial gaps, full package/OS checks, recovery/coverage, performance and
rendered Documenter acceptance remain unpaid. No new capability or timing claim.
The equivalent marginal branch fallback has O(p²) memory cost.

## 11. Team Learning

Memory receipt: decisions are grounded in approved scope, frozen source and
fresh retained commands; no new user policy inferred or brain memory written.
Golden Set: original Student/Tweedie fixtures and failure denominators retained.
Rose Sol/high configured route and measured CLI elapsed time are recorded;
backend routing/native actual hours are not independently inferred. Noether confirmed the B6 objective/derivative distinction and identified the
required observed-curvature/repair branch; that implementation gate remains open.

## 12. Cross-Product Coverage

Covers this Julia lane and a pinned R0.7.0 oracle only. Does NOT cover R0.7.1,
the article lane, public AGHQ, recovery/coverage or full programme acceptance. Mission Control correction
2dacdae is served with HTTP200 and exact field readback; R0.7.1 content is
unchanged. No scientific campaign or full suite was launched. The long-running
programme remains active and its durable checkpoint will retain the next step.
