# Default-unique Gaussian AGHQ fallback checkpoint

## 1. Goal
Match the frozen reference's ineligible default-unique Gaussian fallback through
native and formula routes. Full programme remains ACTIVE/M1 PARTIAL, manifest draft.

## 2. Implemented
Per-variance Gaussian accepts validated aghq/aghq_control inputs and carries
requested/actual integration, nodes, reason and no-ridge provenance. k1 is quiet;
other fixed-residual unique-effect requests warn and retain exact Gaussian/Laplace.
Default off and six/eight-argument constructors remain supported. Show/summary
reveal fallback. No change to numerical objective, coordinates or optimizer.

## 3a. Decisions and Rejected Alternatives
Frozen R/fit-multi.R:6330-6408 checks k1 before random blocks, then structural
rejection before affordability/auto. Unique effects imply z_B+s_B; the reference
refuses Stage1a quadrature. Plain c=0 heteroscedastic Gaussian need not represent
that structure: its unpaid adapter has a separate pervar_aghq_unimplemented reason.
No automatic ridge, no fabricated mode gradient/adaptation, no scope exclusion.
R emits its warning once per session; Julia warns per request. Reasons and actual
method are preserved on every Julia fit. This is a presentation difference.

## 4. Files Touched
src/families/gaussian_pervar.jl, named fallback test and central runner, paired
replay helper, README/changelogs/tutorial/response guide, required-case evidence
cross-reference, leaf/evidence/visual/after-task records, check-log and LOOP.
Mission Control Julia next-action fragment only. No R engine or foreign edits.

## 5. Checks Run
Baseline unsupported aghq failure37.47s before implementation. Same test bytes
pass59new+23fixed-residual+35formula assertions49.14s. Original R fixture default,
k1,numeric3,auto:10native+9formula+29fallback assertions26.92s; oracle verification
before/after. Both engines preserve baseline coordinates; deltaLL3.864e-9 and both
absolute gradients<1e-4. Strict docs97.83s; warning, actual method and reason visible
on desktop/mobile. No full-suite or current full quality-battery claim.

## 6. Tests of the Tests
Nine corrupt receipts reject changed actual engine/baseline, failed health,
penalty, excess gradient/likelihood error, wrong requested nodes, missing request,
and stale fixture. Archive/source/plan/log/exit hashes and same final test bytes
are bound. Unlazy4/5 on fresh verification, fullsuite/independent review gate unmet.

## 7a. Issue Ledger
Stable CORE070-AGHQ-PUBLIC-UNIQUE-DEFAULT and K1-PRECEDENCE receive incremental
Gaussian native/formula evidence only; both remain PARTIAL for wider interfaces
and families. Historical unmatched-native receipts remain unchanged. Bridge,
intervals, plain pervar AGHQ and full Core070 obligations remain.

## 8. Consistency Audit
No likelihood, scale or optimizer changes. Integration uses existing abstract
metadata type; absence of quadrature caches/gradient is explicit. Fit convergence
is unchanged. Default constructors yield integration=nothing. Invalid controls
reject before fitting. Requested and actual nodes are separate. R's unused k=NA
is represented by actual Laplace node count1 in Julia, with no quadrature claim.

## 9. What Did Not Go Smoothly
Expected baseline @test_logs failures produce subsequent Nothing-field errors;
first cause is the missing keyword, retained verbatim. No fixture substitution
or tolerance change. Long warning/source lines scroll within the output block;
plain actual-method/reason lines remain readable. Existing docs warnings persist.

## 10. Known Residuals
Fullsuite and specific external numerical-review approvals remain pending. No
alternative dispatch bypasses that boundary. Recovery, coverage, other families,
covariance/multinomial/data/postfit/bridge scope, performance and full documentation
polish remain. No revised hours estimate or release readiness claim.

## 11. Team Learning
Ada parent implemented and verified this slice; no new child/reviewer dispatch.
No independent model/effort/hour receipt invented. Structural fallback and missing
implementation must have different reasons even when both retain the same exact
Gaussian likelihood. Reference warning frequency is distinct from fit provenance.

## 12. Cross-Product Coverage
This checkpoint does NOT cover R bridge, all AGHQ families/structures, intervals,
calibrated recovery/coverage, full suites, independent review, full manifest freeze
or release. No push, merge or destructive cleanup. Rose verdict NOT REQUESTED;
interim verified-scope local candidate. No numerical jobs/workers remain active.
