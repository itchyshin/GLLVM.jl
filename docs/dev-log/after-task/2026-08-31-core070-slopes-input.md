# After-task — random-slope preparation and source crossings

## 1. Goal
Resolve actual slope preparation contracts and remaining structured-source
cases needed for Core070+AGHQ. Full programme ACTIVE/M1 PARTIAL/manifest DRAFT.

## 2. Implemented
22 public slope calls with retained MakeADFun-boundary captures; exact RDS
transcription, map/design/source/warning verifier and corruption tests. Twelve
requested slope models prepare, eight calls reject, two animal multi-slope
calls are recorded as reference misroutes. Source review identifies11 next
tree/pedigree/kernel/spatial branches. No engine or public API changed.

## 3. What Changed the Next Action
Do not copy animal_dep multi-slope fallthrough into Julia or assume ordinary
indep/dep admits slopes. Use distinct ordinary augmented and structured
block-separated latent covariance contracts. Complete remaining structured
input qualification and native call specifications before full manifest freeze.

## 3a. Decisions and Rejected Alternatives
The frozen animal wrapper loses both slope terms while phylo_dep recognizes
them. Keep those cases BLOCKED_REFERENCE_MISROUTE with an explicit future
Julia disposition, not a fake matched model. No R repair or reference-pin
change. Source acceptance alone is insufficient evidence of intended behavior.

## 4. Files Touched
New slopes fixture/exporter/verifier/tests, leaf/contract/annex/evidence/report,
structured-source next-case record; central freeze validator and source-map
annex links, shared check-log/checkpoint. Mission Control Julia-only update
8cffb26e068f3b5895a9fe55095d9e60cf62cd60 verified HTTP200/readback, R fields
unchanged, exact-file lease released. No numerical src or foreign repo edits.

## 5. Checks Run
One-thread Totoro frozen R preparation only; all oracle before/after checks pass.
Attempt1 exit1,1.267s (20calls,5failed expectations); attempt2 exit0,1.267s;
attempt3 exact-map correction exit0,1.267s. Both latter attempts retain22calls.
No objective/optimizer or density run. Local exporter reads raw RDS; five
verifier tests pass, six adjacent source-census tests pass, prior masks-known
evidence still passes. Full package/Documenter/recovery/performance not run.

## 6. Tests of the Tests
13 exported-record corruptions reject: omitted case, promoted misroute/native
status, altered covariance map/pins/source/basis/response/unit order/score shape,
hidden warning, extra free coordinate and partial-name map substitution.
Missing dependency/receipt and nonzero exit reject. Historical FAIL cannot be
markedPASS. Unbound full slope plan and4 missing-role/misroute/preparation-only
freeze controls reject, alongside a positive synthetic role-binding control.
Total22 negative scenarios. Initial verifier assumed family IDs were per-trait;
actual data use54 rowwise IDs. Corrected; real positive and all negatives pass.

## 7a. Issue Ledger
Confirmed animal multi-slope source defect; explicit reviewed native disposition
unresolved. Original Student-t health, binomial k5, defaultunique and remaining
AGHQ debts unchanged. Structured source inputs, full covariance/multinomial,
data/postfit/bridge/recovery/performance/package/docs still required.

## 8. Consistency Audit
Master census remains769/715nonexcluded. Five existing source rows gain slope
annex links; no executable parity rows promoted. Three Unlazy gates freshly
reverified:3met/0unmet/0abandoned. Central full-freeze guard now
requires native/formula/bridge roles or explicit negative/defect disposition
for all22 slope cases. Native API calls remain unresolved, openly recorded.

## 9. What Did Not Go Smoothly
Initial expectations borrowed source-specific admission for ordinary wrappers;
four actually reject. Animal multi-slope was expected to reach the Poisson
guard, but silently became an intercept model. A diagnostic then mistakenly
read theta_rr_phy_slope via dollar partial matching; exact theta_rr_phy map is
NULL, not malformed. Third replay checks exact indexing. All attempts retained.

## 10. Known Residuals
Only public preparation, Gaussian/Poisson, p3,n18, dense relationship C and
specified slope bases. No likelihood, optimizer health, recovery, identification
or interface claim follows. Dense source jitter and coefficient transforms are
contracts; future native validation must exercise them, not repeat parser tests.

## 11. Team Learning
Noether fresh Terra/high CLI source review yielded11 concrete missing input
branches and runtime prerequisites. One bounded followup independently confirmed the animal
misroute, phylogenetic multi-slope branch and exact-map correction; no source
discrepancy finding. Its read-only sandbox blocked R_TempDir, so parent ran the
full verifier/mutation tests in the writable lane. No completion panel or Rose
signoff. Parent remains owner; no new B production worker or campaign launched.

## 12. Cross-Product Coverage
22 calls:12 requested models prepared,8 rejected,2 misrouted. This slice does NOT cover full source/family/mode/rank combinations, spatial or sparse numerical
behavior, native interfaces, fitting or inference. Remaining11 source cases are
planned from source review, not executed or automatically qualified.
