# Mixed-language programme evidence integration

## 1. Goal
Make Julia and public-R evidence independently required by the programme
collection, without confusing reference bridge behavior with same-model parity.

## 2. Implemented
Typed executor partition, exact public-R registry and inline obligations, and
raw-receipt component verification. Julia receipts cannot pay R obligations.
A mixed collection requires its exact five R IDs, registry hash and current
manifest pin. Native assertion totals explicitly exclude public-R checks.

## 3a. Decisions and Rejected Alternatives
Keep three original paired R models separate from two exact frozen-reference
behaviors: Gaussian unique-model change and truncated-NB2 rejection. Preserve
mandatory native/formula same-model checks. Never turn mapped bridge keys or
constructor-only families into fitting evidence. Full source freeze remains
mandatory, despite synthetic positive tests of the collection format.

## 4. Files Touched
Programme evidence helper, typed bridge helper and registry, TOML manifest,
source map, family plan and tests. Corrected copied normalization prose for
Poisson/Beta/NB2 contracts only. No engine, numerical fixture, data, tolerances,
optimization policies or R oracle source changed. Mutable evidence stays ignored.

## 5. Checks Run
Fresh Totoro native/formula replay family-formulas-03: ten cases, nine
executions,187 assertions,97.52977seconds. Fresh public-bridge-models-04:
three original models, six matrix/long fits,59.09261seconds;18 outer check fields
true. Oracle before/after checks pass in both runs. One thread per worker,
within predeclared2–4 and1–3minute estimates respectively. Source-bound native
verifier passes43 negative controls; bridge bundle passes38 component controls.
Complete draft-integrity battery61 tests passes. Five Unlazy runnable gates
reverify; full-programme manual gate remains unpaid (overall checker exit1).

## 6. Tests of the Tests
Grace independently owns nine mixed-receipt regression tests. Missing/duplicate
IDs, unknown executors, wrong manifest pin, stale selection, failed component,
wrong executor and helper failures reject. Native-only collections remain
supported; mixed synthetic positive collection keeps assertion scopes separate.
A bounded follow-up review found no further acceptance hole in the inspected
helper/collection. This is not an independent numerical or completion panel.

## 7a. Issue Ledger
Central mixed-language aggregation is implemented. Programme-wide contract and
source-scope completion remains open:710 nonexcluded source facts unmapped.
Ten additional reference-rejected descriptors still need model binding. Remaining
Student-t, Binomial/AGHQ, covariance, multinomial, data and fitted-object work
is not resolved by receipt plumbing. Full suites and the previously rejected
specific external numerical-review transfer still need their separate approvals.

## 8. Consistency Audit
Main manifest stays DRAFT_INCOMPLETE_NOT_FROZEN. Source map15 executable links
over the same five model source facts; family plan97cases/69facts/31bindings.
Normalization prose now describes ordinary Poisson/Beta/NB2 log densities, while
truncated-NB2 conditioning remains explicit. The changed source-contract pins
were followed by fresh native and public-R runs, not reuse of stale summaries.
Old runs and prior boundary contract remain retained. Current boundary proof is
programme-bridge-boundary-replay.json; combined R proof is
public-r-bridge-programme-evidence.json.

## 9. What Did Not Go Smoothly
Copied normalization prose required a coordinated replay. A same-model metadata
change invalidates source-bound evidence even when no numerical code changes.
No fits failed in the two new runs. Nibi's saved DRAC socket was absent at the
18:40UTC check; no fresh login was attempted. Other seven DRAC systems and Totoro
responded. Brain search worked; note read routed to the wrong default project,
so compute details were checked directly in local setup notes and live sockets.

## 10. Known Residuals
Full manifest scope, recovery/coverage, package checks, performance and final
Documenter remain incomplete. No independent Rose completion panel ran.
Mission Control5bb8f8a78c5fd986dd525755f7f370ea440e5b4b has HTTP200 served
readback and unchanged R fields. No new remaining-hours estimate is justified.

## 11. Team Learning
Ada integrates programme metadata and gates. Grace owned only the new test file;
requested Terra/high native fresh-context dispatch and one bounded review
follow-up. Requested routing is not an actual billing/model receipt; no agent
hours were fabricated. Independent tests protect a boundary that aggregate
assertion totals alone could conceal.

## 12. Cross-Product Coverage
This does NOT cover full family, covariance, multinomial, data, inference,
AGHQ, recovery, coverage, performance or polished Documenter completion.
R0.7.1/article and all foreign worktrees untouched. No push, merge, release,
destructive cleanup or DRAC submission. ACTIVE/M1 PARTIAL; local checkpoint.
