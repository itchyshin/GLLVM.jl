# Original NB2 formula in the required runner

## 1. Goal
Integrate the already qualified original NB2 formula model as a separate required
interface case without inflating the17-family count. Programme ACTIVE, M1 PARTIAL.

## 2. Implemented
Added a pure case registry with17 family IDs and one formula interface ID. The
required runner defaults to these18 registered cases, still only a programme
subset. Family scope compares actual IDs, not just their count. Contract rows
and fixture paths must match the registry; incomplete shared groups fail.
The formula fixture uses its own module and namespaced health/raw-fit artifacts.
Its execution inventory includes the original NB2 data-generation fixture even
when only the interface is selected. No numerical source or R engine changed.

## 3a. Decisions and Rejected Alternatives
Keep CORE070-FAMILY-05-LOG-FORMULA-INTERFACE separate from NATIVE-06-NB2.
Preserve seed45/p5/K2/n80,19 coordinates, original default R/native controls and
observed curvature. The formula's19 assertions form a separate execution from
native18 and truncated21. Do not relabel it as another family, overwrite native
artifacts, infer full parity from18 rows, or count shared fixtures multiple times.
The pre-run contract remains immutable; its replay-pending wording is a planning
snapshot. The new evidence summary records the later scoped outcome.

## 4. Files Touched
Registry/receipt/helper/runner and NB2 formula fixture under test/parity; central
test runner and pure registry tests; Python manifest and scoped verification
checks; family plan and evidence summary; developer-log records and checkpoint.
Mission Control's now.next_safe_action changed only in its Julia section.

## 5. Checks Run
Both existing Totoro/DRAC sockets verified; DRAC had no user jobs, none submitted.
Totoro one-thread original three-case replay:58/58 in63.534s, then58/58 in54.167s
after adding the transitive fixture hash. Both before/after oracle checks pass.
Estimated1–3min per run, hard cap5min. All processes terminal. Julia1.12.6,
R4.5.3/TMB1.9.21, exact Rb4d5fee. Retained NB2 delta logLik3.412827e-6 and
truncated delta8.673078e-8; original tolerances/health checks unchanged.
Wide/reversed-long formulas match all19 native coordinates and logLik within1e-10.

Pure Julia1.10 checks: original scope regression1/1, new registry21/21, existing
receipt kernel45/45. Python registry3 tests,11 coverage regressions and aggregate
selftest pass. Full package suite and Documenter render NOT RUN.

## 6. Tests of the Tests
Retained red:17 arbitrary IDs incorrectly received all17; now subset. Python
red: deleting the interface registry passed; now rejected. Duplicate/missing IDs,
wrong fixtures, incomplete shared groups and family/interface overlap reject.
Formula-only inventory must contain its native DGP. Thirteen corrupted artifact
copies reject;10 truncated-policy and6 native-health metric corruptions plus4
formula-summary corruptions reject. Whole R fit readback and complete supervisor,
source/environment, cell/contract/inventory hashes checked. Unlazy4 gates freshly
reverified. Initial approval-store EPERM resolved through the authorized scoped
escalation; missing-receipt gates failed before runs as expected.

## 7a. Issue Ledger
Closed count-only family scope, absent interface registration, formula/native
artifact collision risk and omitted transitive DGP hashing. Original formula is
now in the actual required runner. Public R bridge, additional family models,
Student R health/density and full source mapping remain unpaid.

## 8. Consistency Audit
Family plan remains97 typed cases,2 native plus1 formula bindings,94 unbound.
Main source map still has no complete executable-family promotion. New current
summary nb2-formula-required-evidence.json supersedes historical standalone and
native-refresh proof for current helper/contract hashes. Full manifest remains
DRAFT. Mission Control HTTP200/readback PASS; all R0.7.1 fields unchanged, local
vault commit992a080; status-file lease released. Independent Rose review NOT RUN.

## 9. What Did Not Go Smoothly
Initial registry implementation omitted the transitive original DGP when selecting
formula alone; review caught this and forced a fresh three-case replay. Both
passing attempts retained. Python interface omission was caught by a failing
negative test before repair. No failed history or recovered foreign work erased.

## 10. Known Residuals
No full17-family replay, broader formula test battery, bridge qualification,
recovery/coverage, full package checks or final independent panel this slice.
The complete family/model/interface manifest is not frozen. Metadata and raw
checks are scoped evidence, not general scientific validation.

## 11. Team Learning
Interface rows need separate identities and transitive executable inputs. Two
identical raw fits can have the same hash; distinguish output markers as well as
filenames to avoid substring-count ambiguity. Parent only; no new child/model
routing receipt or invented agent-hours. B production remains behind checkpoint.

## 12. Cross-Product Coverage
This does NOT cover Core+AGHQ completion, public R bridge, latent multinomial,
broader covariance, calibrated coverage, performance or polished rendered docs.
Foreign Julia/R0.7.1/article work is protected. No push/merge/release/cleanup.
