# Frozen covariance public-R bridge boundary

## 1. Goal
Characterize the frozen R 0.7.0 public Julia bridge for the same nine Gaussian
covariance contracts already qualified through native and Julia formula routes,
then integrate the result without claiming unsupported callable bridge parity.

## 2. Implemented
Nine explicit public-R case IDs now join the central bridge registry. The runner
uses unchanged frozen formulas and records the first error before any Julia call.
Eight cases reach the named structured-term rejection. Ordinary dependent `dep()`
fails earlier when the deferred trait-count symbol is coerced to integer. The
central component now binds fourteen public-R cases: three paired model fits, two
earlier reference boundaries and these nine covariance boundaries.

## 3a. Decisions and Rejected Alternatives
The `dep()` result is classified as a reference adapter failure and presentation
defect, not silently rewritten as the intended named rejection. None of the nine
rows is callable bridge parity. We did not add a structured payload to either
bridge, alter R source, imitate R syntax in Julia or let native/formula evidence
pay a bridge obligation. The strict evidence-freshness guard was retained.

## 4. Files Touched
New covariance bridge runner, contract builder, independent verifier, tests,
leaf and evidence. Updated the central public bridge registry, manifest, source
map, bridge component and draft-integrity tests. Refreshed the native/formula and
paired-bridge evidence state pointers. No `src/`, R oracle, dependency, workflow,
public tutorial or foreign-lane file changed.

## 5. Checks Run
Totoro attempt05: nine boundary calls in 0.22 seconds, R 4.5.3, one thread,
exact frozen installed package; eight named rejections and one adapter failure.
Fresh native/formula04: ten cases, nine executions and 187 assertions in 97.89
seconds. Fresh public-bridge-models05: three models and six routes in 59.23
seconds. Oracle checks passed before and after both model batches. The combined
fourteen-case bridge component passes. Draft integrity passes 104 Python tests;
42 executable bindings are present and 703 nonexcluded facts remain unmapped.

## 6. Tests of the Tests
Nine boundary mutations reject omitted cases, changed outcomes and stale source
or process evidence. Installed marker content and hash, all four failed attempts,
their exact failure mechanisms and the successful raw result/process hashes are
verified. Twelve paired-model corruptions, ten runtime corruptions, five Gaussian
boundary corruptions and eleven descriptor corruptions remain green. A dedicated
negative test removes bridge rows from both cases and mapping and confirms native
plus formula evidence cannot satisfy the bridge role.

## 7a. Issue Ledger
Full manifest remains DRAFT and Milestone 1 remains PARTIAL. The frozen public
bridge cannot transport these structured covariance terms. Hundreds of source
facts, other covariance/modifier combinations, latent multinomial, data and
post-fit surfaces, public Stage 1a AGHQ, recovery/coverage, performance, full
package suites and final Documenter/panels remain unpaid. Original Student-t
health and binomial AGHQ failures remain open.

## 8. Consistency Audit
Every boundary row keeps the native/formula model contract and source fact while
using a distinct executor and acceptance level. The combined receipt is pinned
to the current fourteen-row registry and current draft manifest. The historical
formula red baseline now permits only the programme-wide requested-case count to
change; all other registry-test text and the four first-green formula files stay
bound. A fresh current-manifest replay independently exercises the live code.

## 9. What Did Not Go Smoothly
Attempts01-02 lacked the full R library path. Attempt03 assumed all nine errors
would use the named gate. Attempt04 retained the real `dep()` adapter failure but
failed its blanket expectation. Attempt05 passed the exact outcome map. The first
fresh bridge launch was blocked locally by sandbox access to the existing SSH
socket; the authorized retry succeeded. The combined verifier then caught stale
native/formula provenance, prompting the fresh 97.89-second replay. Its historical
red guard was too broad for later programme-count and unrelated engine growth; it
was narrowed with regression tests rather than removed.

## 10. Known Residuals
This is an accurate frozen-reference boundary, not evidence that structured
covariance is callable from R through Julia. A future compatibility adapter needs
its own transported data contract, negative cases and paired numerical evidence.
The frozen `dep()` generic error also remains less usable than the intended named
gate. No full package or documentation build ran for this developer-only slice.

## 11. Team Learning
Hopper reviewed the frozen admission path and found that bridge dispatch accepts
only one ordinary reduced-rank block and carries no structured covariance payload.
The first evidence review found missing installed-marker enforcement and missing
local copies of failed attempts; both were repaired. A final independent review
after the fresh receipts found no P0--P3 issue and independently matched the raw
outcomes, source pins, retained failures, current registry/manifest, receipt state
and scope wording. Ada owned integration and did not edit the R or Julia engines.
This is not a Rose milestone panel.

## 12. Cross-Product Coverage
Nine Gaussian covariance model contracts now have three separately recorded
surfaces: native Julia PASS, Julia formula PASS, and frozen public-R bridge
boundary. Only the first two are callable fits. R 0.7.1, the article lane and
foreign Cursor/Claude work remain untouched. No DRAC job, push, merge, release or
destructive cleanup occurred.

This slice does NOT cover a callable structured R-to-Julia adapter, other
families or covariance modifiers, inference, recovery/coverage, Stage 1a AGHQ,
performance, full package suites, Documenter or programme completion panels.

Review verdict: Hopper scoped review PASS with no P0--P3 finding; no programme
completion claim. Rose full programme panel: NOT RUN.

Mission Control: local commit `0fd633f`; canonical server verified, served status
HTTP 200, both targeted Julia fields visible and the R 0.7.1 focus preserved.
Unlazy: boundary, combined bridge and draft-integrity gates pass. The after-task
structure check passes; its global-ledger phase correctly exits nonzero because
the wider AGHQ/binomial and other programme gates remain unpaid.
