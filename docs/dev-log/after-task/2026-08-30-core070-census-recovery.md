# Core070 census and restored remote observation

## 1. Goal
Account for newly visible paths and recover the interrupted historical job without changing foreign work.

## 2. Implemented
Refreshed registered inventory, compared dirty Git-visible bytes, preserved new primary refs and18 additional paths, restored files/history independently, and recovered Totoro receipts. Corrected Mission Control Julia status only.

## 3a. Decisions and Rejected Alternatives
Protected unknown ownership, old article path and new repaired path separately. No cleanup, reset, stash pop, fresh login or fit restart. Retained failed supervisor and numerical results. Current candidate evidence stays distinct from historical5be659b1.

## 4. Files Touched
Census/recovery evidence JSON and narrative, original recovery status pointer, check-log and LOOP checkpoint. Mutable census/archives/receipts and protected-lane manifest remain outside tracked source.

## 5. Checks Run
103 Julia and211 R registry rows;40 dirty trees/5,914 visible paths/80 patches compared. Additional19,314 file entries restored/read back;1,002 named tips and72,079 reachable Git objects independently imported/checked with baseline.153 historical source pins and every recovered log/cell checksum verified. Totoro and Fir hostname checks succeed; separate R oracle verify PASS. No numerical rerun. Mission Control HTTP200 and exact field readback PASS.

## 6. Tests of the Tests
Source pins reject using current changed source as the old run. Failed group remains exit1 despite successful Tweedie. Unique fixture accounting identifies273 attributed passes versus217 actual passes; shared28-assertion fixture counted three times. Readback comparisons cover file checksums and missing Git objects, not merely archive existence. Seven executable negative controls reject corrupt/missing artifacts, promotion, erased failure and duplicated assertion counts. Unlazy freshly reverified:1met/1unmet.

## 7a. Issue Ledger
168 registered missing/broken R paths plus13 additional broken Git remnants remain unresolved. Supervisor disconnect logging and duplicated receipt counts need repair. Student/truncated-NB2 health failures remain. No full-manifest or M1 completion.

## 8. Consistency Audit
Census denominators separate primary registrations from independent clones. No whole-filesystem or ignored-byte completeness claim. New7c88 path does not replace article ownership by inference. Original receipt remains failed; separate oracle verification is dated separately.

## 9. What Did Not Go Smoothly
A nested legacy repository required separate history capture; prior metadata estimate included its Git directory. Full independent history import was slower than working-file capture. Initial lane-preflight used project label rather than path; corrected exact-worktree preflight succeeded. Restored connection revealed supervisor BrokenPipeError and omitted oracle-after.

## 10. Known Residuals
Unreachable reflog-only history, missing-path recovery, current-source fitted replay, numerical repairs, independent review, full package suite and final docs render remain unpaid. Four source pins differ from historical run.

## 11. Team Learning
Do not confuse a connection failure with job termination, or per-family attribution with independent executed assertions. Preserve available bytes even when Git metadata is broken, and account for nested clones separately.

## 12. Cross-Product Coverage
This does NOT cover Core/AGHQ completion, R0.7.1/article implementation, complete Julia/R parity, calibrated recovery/coverage, performance or release. Rose NOT RUN; M1 PARTIAL. No new production child or external payload transfer.
