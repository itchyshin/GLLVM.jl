# First paired binomial pre-run and R stopping diagnosis

## 1. Goal
Qualify current source and execute the predeclared first binomial case without changing its model or health gates.

## 2. Implemented
Refreshed only helper source fingerprint; reran26-case packet preflight and opt-in rejection. Qualified integrated runtime on Totoro; executed first fixed case and retained data, metrics and failed receipt. Added bounded same-data/RNG R refinement diagnostic and source-bound verifier.

## 3a. Decisions and Rejected Alternatives
No seed selection, changed likelihood tolerance, R engine edit or default-result promotion. Refinement uses public start_from/optArgs on identical data/map/parameter names and remains separate. No other cohort cases run.

## 4. Files Touched
Binomial source-pin contract/preflight receipts, pre-run evidence, diagnostic/verifier tools, packet plan/catalogue, check-log and LOOP checkpoint.

## 5. Checks Run
26 local preflight assertions and CLI check PASS; opt-ins-off execute correctly exits1. First qualification failed due missing imports; corrected runtime/oracle checks PASS. Required paired case23.53s:13pass/1fail. Exact fixture/RNG diagnostic9.99s reproduces default result and improves R gradient under public tighter controls. Artifact/log/source pins and four negative evidence controls PASS.

## 6. Tests of the Tests
Required case remains failed despite nearly identical likelihood and convergence codes. Source-bound verifier rejects PASS relabeling, missing/corrupt artifacts and omitted remaining cases. Both default R gradient and likelihood reproduced exactly from retained data/RNG; refinement data/map/name equality asserted.

## 7a. Issue Ledger
R default gradient2.85818e-4 fails1e-4; native gradient2.84217e-8 passes. Refined R gradient1.44901e-5 passes and logLik delta to native3.13e-11. Reference precision needs explicit qualification in paired harness; original failed receipt retained. Other five cases, original Student/truncated-NB2 health, full manifest and panel remain unpaid.

## 8. Consistency Audit
Only helper fingerprint changed in predeclared contract; seeds/model/acceptance dictionaries identical. Updated catalogue and plan distinguish initial declarations from actual test outcomes. No universal speedup or broader capability claim.

## 9. What Did Not Go Smoothly
Qualification command omitted Test and LinearAlgebra; corrected without source/model change in a new retained attempt. R convergence code0 did not satisfy raw gradient gate. Default fit replay was necessary because the original runner did not serialize the R fitted object; RNG replay was checked against the saved fixture.

## 10. Known Residuals
Required default-control case still FAIL. Diagnostic is not formal case promotion. No probit/cloglog/varying-trial results, full package checks, calibrated recovery, performance or independent review. One logit timing cannot size all links reliably.

## 11. Team Learning
Matched likelihood does not imply matched stopping precision. Retain original controls and results, prove same model/data, then diagnose numerical tolerance through public controls instead of weakening acceptance.

## 12. Cross-Product Coverage
This does NOT cover Core/AGHQ completion, R0.7.1/article work, default-R health improvement, full paired cohort, bridge qualification, calibrated inference or release. Rose NOT RUN; M1 PARTIAL. No new child/push/merge/cleanup.
