# After-task — structured reference preparation

## 1. Goal
Establish tree, pedigree, named-kernel and spatial preparation contracts for
Core070+AGHQ. Programme ACTIVE, M1 PARTIAL, full manifest DRAFT_NOT_FROZEN.

## 2. Implemented
Qualified deterministic mesh and frozen-R input fixture; captured24 public
calls; added an RDS exporter, independent matrix/map checks, negative controls,
required-case annex and central freeze binding. No numerical engine edits.

## 3. What Changed the Next Action
Eleven requested models prepare; twelve calls reject; one multi-kernel request
silently loses Psi. Do not port that loss. Two pedigree rejections hide their
specific diagnostics and require explicit future diagnostic-quality tests.

## 3a. Decisions and Rejected Alternatives
Retain the frozen reference and every failed attempt. Do not treat parser or
preparation success as native parity. Keep multi-kernel unique loss blocked;
require reviewed rejection or documented extension. No R-engine repair here.

## 4. Files Touched
New structured fixture/qualification/export/verifier/test files and developer
leaf/contract/annex/evidence; source mapping and central manifest validator;
check-log and checkpoint. Mission Control update recorded at closure below.
No edits to foreign checkouts, R0.7.1, article, or separate REML lanes.

## 5. Checks Run
Totoro one BLAS/OMP thread, existing socket. Qualification1.016s PASS;
capture01 .666s FAIL before cases (Matrix method loading); capture02 1.316s
FAIL four diagnostic expectations; capture03 1.317s PASS all24. Every oracle
before/after check passes. No objective, optimizer or simulation executed.
Local structured verifier5tests PASS; source-census6tests PASS; prior slope
and mask/known-V verifiers PASS. Full package/docs/performance not run.

## 6. Tests of the Tests
Fifteen exported-record corruptions reject: missing case, wrong tree precision,
pedigree map, dense jitter, kernel tensor/offset, projection/FEM, tau tie,
extra parameter, random shape, promoted native/defect, hidden warning, response.
Four process/dependency corruptions reject; historical FAIL cannot becomePASS.
Six incomplete/incorrect full-freeze bindings reject, including poor pedigree
diagnostics. Positive synthetic role binding passes. Total26 negative scenarios.
Initial test fails for missing verifier before implementation. The first central
integration test failed because a synthetic non-covariance manifest was given
a structured requirement; corrected by binding to the actual spatial source
fact, whose presence is independently checked by the real source census.

## 7a. Issue Ledger
Multi-kernel auto-Psi loss BLOCKED_REFERENCE_PARAMETER_LOSS. Missing/cyclic
pedigree diagnostic quality BLOCKED_REFERENCE_DIAGNOSTIC. Native interfaces,
fitting, recovery and broader source crossings remain unpaid. Original
binomialk5, Student-t health, defaultunique and remainingAGHQ debts unchanged.

## 8. Consistency Audit
No source facts excluded or executable parity cases promoted. The full-freeze
validator now requires native/formula/bridge or rejection/defect roles for this
annex. Two pedigree rows also require executed specific diagnostic assertions.
Source bytes bind to the frozen Git archive's hash and PAX commit, not the
parent Julia worktree HEAD. Existing source census remains769/715nonexcluded.

## 9. What Did Not Go Smoothly
Explicit Matrix loading was needed before subsetting restored sparse objects.
Two exact diagnostic strings differed; two pedigree errors were swallowed by
the public preparation path. The independent reviewer initially ran Git inside
an extracted source directory, which walked up to Julia's repository and gave
a false provenance alarm. Archive verification is the correct test here.

## 10. Known Residuals
No claim of native support, estimator health, recovery, quadrature, likelihood
normalization, complete family crossings, adapter coverage or polished docs.
The35node mesh is a deterministic preparation fixture, not scientific evidence.
Source parameterization remains distinct: propto logvariance, kernel diagonal
logSD, spatial inverse tau, raw latent loadings. Identification untested.

## 11. Team Learning
Noether fresh Terra/high CLI independently confirmed matrix/map contracts and
flagged pedigree diagnostic quality. Follow-up checks archive provenance and
new gates; final disposition recorded below. No new B production child, no
programme completion panel, and no Rose programme signoff. Parent owns edits.

## 12. Cross-Product Coverage
24 public reference calls,11 prepared requested models,12 rejections,1parameter
loss. This slice does NOT cover native/formula/bridge fitting, non-Gaussian
crossings, slope variants, inference, recovery, performance or full Core parity.

Mission Control Julia-only correction a1bf4a176e0ec33333a23c47a8951c4ee99f7bcf verifiedHTTP200/readback;
Rfieldsunchanged, exact-file leaseRELEASED.

Review disposition: Noether follow-up withdrew the provenance blocker after
verifying the frozen Git PAX commit, archive hash, all4332 archived file hashes
and the five reviewed readback files. Independent pure-Python structure checks
pass. The read-only reviewer could not create R_TempDir; parent freshly ran
the full RDS-based verifier and all mutation tests in the writable lane.
Remaining capability blockers are the Psi-loss disposition and two pedigree
diagnostic-quality obligations, not unreported passing-model claims.
Unlazy final reverify: ST-CONTRACT/ST-NEGATIVES/ST-INTEGRATION3met/0unmet/
0abandoned. Reviewer dispatch receipts record Terra/high, fresh ephemeral,
read-only, source review11:58:27–12:04:12UTC and one bounded follow-up from
12:05:24UTC; both exit0. Parent authored this slice; no production child.
Noether's verdict is scoped acceptance of reference preparation, not Rose
programme completion. Mission Control and runtime records remain accurate.
