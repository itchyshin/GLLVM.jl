# Core070 postfit source and policy contract

## 1. Goal
Resolve the ungrounded postfit namespace rows and expose exact information/conditioning requirements before freezing capability cases.

## 2. Implemented
Source parser resolves all100 postfit entries to99 function definitions and one explicit tidy reexport. Inventory stores exact formals/source lines/pins. Added29 policy probes and a fail-closed verifier that reparses source rather than trusting stored signatures. Added source-subset pointer to the still-DRAFT master contract.

## 3a. Decisions and Rejected Alternatives
No function-name parity, no automatic100×17 family cross-product, no fitted-parity promotion from source probes. Preserve Julia idioms with explicit scale/conditioning adapters. Quantile residual comparison uses CDF bounds/shared uniforms rather than assuming equal seeds across languages. R truncated-Poisson response rate is not relabelled as expected positive count.

## 4. Files Touched
Postfit inventory parser, namespace probe runner/fixture/verifier,100-entry inventory,29-case policy subset, postfit contract notes, master source-subset pointer, check-log and programme checkpoint. No Julia/R engine, API, protected0.7.1 or article changes.

## 5. Checks Run
Parser resolves100/100 definitions including namespace-backed reexport. Totoro installed frozen package passes29/29 probes in0.366s; package integrity before/after passes. Original28-probe run also retained. Source and process/fixture/artifact hashes are bound in evidence. Fresh three Unlazy gates PASS; native-source retained-evidence verifier remains a separate check.

## 6. Tests of the Tests
Verifier rejects omitted surface, fabricated signature (fresh AST reparse), omitted policy, stale fixture, false parity label and nonzero process exit. Reexport omission originally caused a hard failure rather than silently dropping tidy. Minimal object probes are explicitly not fits; empty sdreport proves only guard admission, not valid standard errors.

## 7a. Issue Ledger
Postfit source inventory PASS and29 policy probes PASS. Numerical model/estimand/conditioning mappings remain UNPAID. Inference target-method-source branches, valid fitted fixtures, formula/bridge and rendering remain UNPAID. Full manifest remains DRAFT and M1 PARTIAL; Student reference health unresolved.

## 8. Consistency Audit
Checked defaults for predict/fitted/simulation/residuals/confint/tidy, observed-count precedence, named coefficients, likelihood/deviance fields, SE exclusions, per-row inverse-link dispatch and re_form normalization. Verified truncated-Poisson C++ fid10 uses underlying rate with truncation normalizer; response helper rate0.5 differs from expected positive count1.270747. Current Julia marker already states untruncated-rate link. No unsupported mean claim copied.

## 9. What Did Not Go Smoothly
First parser attempt assumed a symbol at every call head; namespaced calls require a symbol guard. Second correctly failed on missing local tidy; NAMESPACE explicitly reexports generics::tidy. Both failures retained as setup notes. No model fit or simulation was started. The successful28-probe policy set was extended to29 to test the rate/mean distinction, not altered to hide a failure.

## 10. Known Residuals
Source signatures do not enumerate every admitted method branch. All100 native mappings remain unqualified. Minimal placeholder objects intentionally isolate field/guard precedence and cannot prove real fit consistency or health. No interval computation, simulation, fit, full suite or documentation rendering ran.

## 11. Team Learning
A namespace census needs reexport resolution and executable policy probes. Separate inverse-link parameters from actual response means for truncated/two-part models. This parent-led source-contract slice adds no production child; no fresh milestone completion panel or Rose sign-off is implied.

## 12. Cross-Product Coverage
This does NOT cover fitted numerical postfit parity, covariance/source/family cross-products, bootstrap/recovery/coverage, newdata simulation, AGHQ postfit accuracy, full inference or graphics. It does NOT modify protected R0.7.1/article lanes. The full programme remains active and incomplete.
