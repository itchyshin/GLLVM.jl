# After-task — AGHQ required-case manifest

## 1. Goal
Advance the full Core070 manifest by separating actual public Stage1a obligations
from the historical helper-only inventory. Full programme ACTIVE / M1 PARTIAL.

## 2. Implemented
A source-pinned annex maps all 39 control facts, supplies 16 exact paired
normalization calls and records 32 additional public/numerical obligations plus
all 21 admitted family descriptors. Central freeze validation requires their
concrete case contracts. The full manifest remains DRAFT, not certified.

## 3. What Changed the Next Action
Stop treating absent annotations as absent implementation: public Gaussian,
Poisson and binomial adapters exist. Conversely, helper success does not establish
public admissions, other families or formula/bridge coverage. Next bind exact
public model fixtures and calls, then complete the remaining source domains.

## 3a. Decisions and Rejected Alternatives
No reference archive or engine changes. No automatic loading ridge. Numeric R
whole values and auto strings have explicit Julia integer/symbol adapters.
Kernel tests do not create artificial public bridge APIs. Fixed predictors are
not ruled out merely because predictor-informed latent scores are ineligible.
Recovery/coverage belongs to separate validation designs, not source assertions.

## 4. Files Touched
New AGHQ case-plan generator/validator, paired-control TOML and Julia runner,
regression tests, generated annex, developer explanation, evidence and leaf.
Updated central source-map planned links, freeze integration, source-annex status,
check-log and checkpoint. Foreign lanes and all source numerical files untouched.

## 5. Checks Run
Original frozen-R 39 controls passed locally without installation/fitting.
Totoro Julia1.12.6: first 16 checks passed in8.634s; final16 plus2 dialect checks
passed in8.785s, including startup, one Julia/BLAS thread, 120-second cap. Final
run additionally records loaded package path and process-bound output hash.
Local contract suite11 tests, adjacent manifest6 and family5 tests, known-source
index and aggregate self-test pass. No fits, package suite or docs build needed
for this developer-only manifest slice; none is claimed.

## 6. Tests of the Tests
Initial missing-module failure retained. The central-freeze integration test
then failed because only the old helper mapping was considered; now passes.
Mutation controls reject dropped facts, stale source/fixture, missing calls,
helper-as-fit claims, omitted public interfaces and unpaid promotion. Five
copied runtime-evidence corruptions reject: nonzero exit, missing receipt,
dropped dependency, stale plan, forged output. Positive structural controls are
synthetic mechanism tests, never programme evidence or an independent review.

## 7a. Issue Ledger
Binomial original k5 convergence/likelihood and Student-t reference health/density
remain unmet. The 22 unmapped native control policies, 32 public/numerical
obligations, family/interface qualification and other Core domains remain unpaid.
No issue messages, push, merge, release or destructive worktree cleanup.

## 8. Consistency Audit
752 overlapping known source facts unchanged; planned AGHQ links were added,
zero executable links promoted. Additional public obligations live in the annex
and are required by the central freeze check. Historical coverage-evidence hashes
predate these mapping changes and must not be advertised as current full coverage.
Existing family mappings and exclusions remain unchanged.

## 9. What Did Not Go Smoothly
Initial source anchors cited caller regions rather than gate/default/ranking
implementations. Noether found those errors. Ada repaired them using exact frozen
source, narrowed fixed-predictor/REML wording and separated calibration design.
The first control receipt omitted loaded-root/output binding; final replay adds it.

## 10. Known Residuals
The annex is a substantive requirements decomposition, not a complete executable
manifest. Many cases still need model/fixture/call design and numerical evidence.
No full semantic scope review or completion panel has signed off. Covariance,
multinomial, data/postfit, bridge, recovery, performance and visual Documenter
work remain required. No shrinkage of the original programme is authorized.

## 11. Team Learning
Noether native Terra/high, fresh context, one review plus one follow-up; all
findings retained and addressed by parent, final changes not independently
re-reviewed. No B production child. Record measured command durations only;
no inferred aggregate model-hours. Source anchors must name the decision-making
implementation as well as the public call site.

## 12. Cross-Product Coverage
The three public interfaces are listed separately for each public requirement
and each of the 21 family descriptors; numerical kernels are shared obligations.
These rows are not a tested Cartesian product. Unadmitted covariance/family/data
combinations are not invented. Only 16 normalization values/errors and two Julia
input-dialect checks were exercised here. This slice does NOT cover public fitted-model crossings, missing-predictor/response fits, REML fits, bridge fits, covariance combinations, recovery or coverage. Those cells remain unpaid.
