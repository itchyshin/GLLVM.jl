# Frozen inference routing — partial contract checkpoint

## 1. Goal
Advance the required finite inference contract while Totoro access is absent, preserving actual R target defaults, rejection branches and fallback semantics.

## 2. Implemented
98 stable-ID source-dispatch/guard cases, a disposable function-only R probe, source/receipt verifier, eight negative controls and an explicit B4 contract. Linked the subset from the master draft and postfit contract. No engine change.

## 3a. Decisions and Rejected Alternatives
Intercept numerical endpoints with typed errors rather than fabricate successful intervals. Keep method forwarding distinct from admission. Do not turn fixed/direct-target bootstrap fallback into a bootstrap capability. MSPL remains excluded. Frozen source stays unchanged; no remote job restart.

## 4. Files Touched
test/parity/fixtures/core070_inference_routes.tsv; tools/core070_inference_routes.R; tools/core070_verify_inference_routes.py; tools/core070_test_inference_routes.py; scoped evidence/contract/report, master draft, postfit contract, check-log and core070 checkpoint. Mutable process artifacts under .unlazy/core070-aghq/inference-routing.

## 5. Checks Run
R4.6.0 with cli3.6.6; all local control-flow runs below one second each and60second limits, no fits. Attempt1:84pass/14fail. Attempt2:97pass/1fail. Final98pass/0fail. Eight evidence and live-probe negative controls pass. Frozen files matched oracle inventory; process exits, logs and source pins retained. No installed R-package or Julia numerical test implied. Unlazy freshly reverified2met/1unmet; no abandonment. Mission Control Julia-only update served/readback verified, R fields unchanged.

## 6. Tests of the Tests
A deliberately false first expected route produces97pass/1fail and exit1. Evidence checker rejects numerical/installed claims, stale reference or fixture, omitted case and corrupt receipt. First failed run exposed hidden-function restoration leakage, proving helper guards were not exercised until repaired.

## 7a. Issue Ledger
Remaining B4 work: fitted estimands, later helper admission, loading scales/pins, Sigma/source combinations, malformed tokens, cluster correlations and native/formula/bridge mapping. Full manifest DRAFT; M1 PARTIAL. Totoro authentication absent; existing family-recheck-01 remains UNKNOWN. No new production child dispatched.

## 8. Consistency Audit
Subset explicitly labels source routing and unresolved numerical cases. Rejected nonlinear profiles do not become artificial obligations. No tolerance change, API change, public capability promotion or R-engine edit. Existing scalar kernel candidate source unchanged; fitted and independent-review gates remain open.

## 9. What Did Not Go Smoothly
Initial as.list(e) omitted dot-prefixed functions, leaking instrumentation across cases; changed to all.names=TRUE. A rho diagnostic expectation used the wrong literal; corrected after frozen-source inspection. Failed runner/fixture bytes and logs retained. Initial preflight supplied a basename instead of a directory; reran on the actual canonical repository, then continued only in owned isolated lane.

## 10. Known Residuals
Only selected helper rejection bodies execute; numerical endpoints never do. Synthetic profile-target labels do not prove fitted availability. Target bootstrap fallback may later fail parameter selection. No independent review, package check, new interval computation or recovery campaign. Do not restart the unobserved remote run.

## 11. Team Learning
Dispatch acceptance is weaker than numerical admission. A test harness that restores environment functions must include hidden bindings, particularly in R packages whose internal helpers start with a dot.

## 12. Cross-Product Coverage
This does NOT cover complete inference parity, actual interval values, all covariance tiers, all models, fitted health, calibrated coverage, AGHQ completion, performance or final Documenter. Rose verdict: NOT RUN. Programme remains active.
