# After Task: AGHQ A4(3) fail-loud gate (Identity-adjacent)

## Goal

Lock that `_aghq_stage1a_reject_extra` fail-loud is the A4(3) eligibility
gate, and record why Julia throws while the twin warns and falls back to
Laplace — without porting TMB `.aghq_gate`, adding a public `aghq=`, or
closing A4(4) / A4(5).

## Implemented

Identity-adjacent lock plus tests. Decision
`docs/dev-log/decisions/2026-08-18-aghq-a43-gate.md` (@ `b2d646fc`)
records Hopper's pin: the twin's `.aghq_gate` measures TMB `spHess`
sparsity then min-fill treewidth (`tw ≤ 4` proceeds; else warn+Laplace).
Julia already rejects anything that is not a single loadings-only `z_B`
block by throwing. That throw is the gate while Identity §A3 forbids a
stub knob. `test/test_aghq_gate.jl` (@ `5b4d9666`) proves the Stage-1a
fences at `k > 1` go through the existing helper. No new `src/` gate
engine. Optional `k^d` / `d ≤ 5` bound was **not** invented. Both AGHQ
ledger rows stay `missing`.

## Mathematical Contract

Twin AGHQ remains Liu & Pierce 1994 adaptive Gauss–Hermite of the joint
integrand at the Laplace mode (Identity
`docs/dev-log/decisions/2026-08-17-aghq-identity.md`). A4(3) is
eligibility, not a new estimand. Hopper pin: cite
`gllvmTMB` `R/aghq-gate.R` @ `b926f47f`; do not re-derive. A complete
graph on `d` nodes has treewidth `d − 1`, so the twin's `tw ≤ 4` cut is
`d ≤ 5` on this dense loadings-only block — an optional cost analogue,
not a graph algorithm, and not present as an engine helper.

## Files Changed

- `docs/dev-log/decisions/2026-08-18-aghq-a43-gate.md` — Identity-adjacent lock (@ `b2d646fc`)
- `test/test_aghq_gate.jl` — fail-loud gate suite (@ `5b4d9666`)
- `test/runtests.jl` — include the gate file
- `docs/dev-log/check-log.md` — this slice
- `docs/dev-log/after-task/2026-08-18-aghq-a43-gate.md` — this report
- `LOOP/arcs.md` / `LOOP/checkpoint.md` — A0–A2 done; A3 sibling push/PR

`src/families/aghq_grid.jl` unchanged in this DoD commit (helper already
on `origin/main`). `docs/design/capability-status.md` **not** edited;
both AGHQ rows stay `missing`. Tweedie / Dropbox checkout **not** touched.

## Tests Added

`test/test_aghq_gate.jl` (34 tests): named helper is the gate; Stage-1a
fences throw from the helper and from the `k > 1` site evaluator
(failure-path clause); `k = 1` still matches dense Laplace (independent
calculation + neighbouring Stage-1b golden); no `_aghq_kd_bound` /
`_aghq_d_bound` / `aghq_gate` symbol (absence check — do not invent the
optional cost helper).

Regression Mac-light: `test/test_aghq_adapt.jl` 17/17;
`test/test_aghq_grid.jl` 70/70.

## Benchmark Numbers

N/A — no hot-path change. This slice locks eligibility and writes DoD;
it does not touch `likelihood.jl`, `fit.jl`, or the sparse phylo path.

## R-Parity Verdict

Parity: N/A — change does not touch the parity surface. Twin `.aghq_gate`
is cited from `R/aghq-gate.R` @ `b926f47f`, not from a Julia number. A
light Δ would invent a TMB treewidth measurement Julia does not run.

## JET / Allocs / Aqua Verdicts

- JET: not run locally (Mac-light; CI is the verifier)
- Allocs: N/A — not an inner-loop claim
- Aqua: N/A — no Project.toml change

## Checks Run

```
julia --project=. --startup-file=no test/test_aghq_gate.jl
# Test Summary: AGHQ A4(3) fail-loud gate | Pass 34  Total 34

julia --project=. --startup-file=no test/test_aghq_adapt.jl
# Test Summary: AGHQ Stage-1b Liu–Pierce adaptation | Pass 17  Total 17

julia --project=. --startup-file=no test/test_aghq_grid.jl
# Test Summary: AGHQ Stage-1a live-pin grid | Pass 70  Total 70
```

Mac-light: no local `Pkg.test`. Full suite = GitHub CI.

## Consistency Audit

```
rg -n 'aghq=' src/families/fit_gllvm.jl
# not added

rg -n '_gauss_hermite\(' src/families/aghq_grid.jl
# no call

rg -n 'function aghq_gate|_aghq_kd_bound|_aghq_d_bound|spHess|min.?fill' src
# empty — not a TMB .aghq_gate port

rg -n 'AGHQ estimator|Broad AGHQ' docs/design/capability-status.md
# both rows still missing
```

README / CLAUDE.md / user-facing docs not edited (no public surface).

## GitHub Issue Maintenance

PR #253 is OPEN at `b2d646fc` (decision + tests; no check-log / after-task
on that head). This commit pays those DoD files on the same branch. No
issue close. No `gh pr merge` from this worktree. OPEN GATE = sibling
push onto #253.

## What Did Not Go Smoothly

A0 (decision) and A1 (tests) landed first; check-log + after-task were
unpaid until this commit. Do not treat the why-not paragraphs in the
decision note as A4(4) or A4(5) closures.

## Team Learning

While there is no public `aghq=`, fail-loud is the honest gate. Porting
the twin's warn+Laplace path would hide ineligibility inside the default
Laplace fit — the EVA-honesty failure Identity already rejected.

## Remaining Risks

- A4(4) adaptation-loop / fit-time `k = 1` skip is **not** closed.
  Julia already re-solves mode+Cholesky every site eval; a canned TMB
  freeze loop would be pad.
- A4(5) report honesty (`used`, `k`, engine label) is **not** closed.
  It needs a public fitted-object surface; Identity §A3 still forbids a
  stub knob.
- Both AGHQ ledger rows stay `missing`. A green gate suite must not
  advertise a quadrature-fitted model.
- Full suite not run locally (Mac-light; CI is the verifier).
- Sibling must push this DoD onto open #253; this worktree must not.

## Known Limitations

Not a TMB `.aghq_gate` / `spHess` / min-fill port. No public `aghq=`.
No estimator. No twin Δ. Optional `k^d` / `d ≤ 5` helper not added.
Tweedie `fit_gllvm` still STOP. leftover-1 `none × dep()` not started.
Closed Stage-1b LOOP at `21e24e97` is cited, not resumed.

## Next Command

Sibling: push this branch onto open PR #253
(`claude/lane-overnight-a43-20260817`). Do **not** open a second PR.
Do **not** start A4(4), A4(5), leftover-1, or a TMB gate port. Do
**not** `gh pr merge` from this worktree.

## Rose Verdict

Rose verdict: not claimed here. Documentation-writer records the
Mac-light tallies and fences only (gate 34/34, adapt 17/17, grid 70/70;
pin holds; not a TMB port; no public `aghq=`; ledger rows stay
`missing`; A4(4)/A4(5) not closed; Parity N/A). Rose owns
PASS / PASS WITH NOTES / FAIL.
