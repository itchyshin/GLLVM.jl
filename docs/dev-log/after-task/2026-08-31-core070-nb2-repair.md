# Ordinary NB2 precision repair checkpoint

## 1. Goal
Repair the diagnosed density precision loss without changing the original
seed45/p5/K2/n80, per-trait-dispersion, observed-curvature target. Programme
ACTIVE, Milestone1 PARTIAL; full manifest remains DRAFT, not frozen.

## 2. Implemented
Mean-based ordinary NB2 density with bounded rising-factorial/log-beta algebra;
shared and grouped observed curvature avoid overflowing size products.
Added 569 scalar, normalization, moment, support and derivative assertions to
the central test runner. Saved paired-fit, neighbour and corrupted-evidence
checks. See ../core070/nb2-repair-evidence.json for source and process hashes.

## 3a. Decisions and Rejected Alternatives
Reused the existing truncated-NB2 rising-factorial remainder bound rather than
adding count-dependent production work. No optimizer tuning, dispersion cap,
R edit, model substitution or tolerance widening. Large fitted size means the
Poisson boundary is weakly identified; this is not evidence that raw size
estimates agree between engines. The original required fixture remains unchanged
until its health checks and stricter tolerance can be integrated and replayed.

## 4. Files Touched
src/families/negbin.jl and the NB2 weight in grouped_dispersion.jl;
test/test_nb2_precision.jl, test/runtests.jl; scoped kernel/neighbour Julia
wrappers and Python verifiers under tools/core070_*nb2*; mathematical decision,
evidence JSON, both changelogs, check-log and programme checkpoint.

## 5. Checks Run
Totoro, one thread, Julia1.12.6, R4.5.3, TMB1.9.21, frozen Rb4d5fee.
Scalar runs estimated under one minute; paired fit1–3min; all capped at5min.

- Original engine:340pass/229fail in11.196s. Exact same tests after repair:
  569pass in9.639s, including compilation.
- Original paired fit:14/14 pass in28.333s. Native gradient9.4810148e-7;
  R gradient6.5090605e-5; absolute logLik difference3.4128270e-6 satisfies
  relative1e-6. Same-point nll difference-3.1150194e-11.
- Unchanged truncated-NB2 neighbour:352pass in9.689s; test dependency imports
  bound through the already-loaded GLLVM modules, no assertion changes.
- Every oracle-before/after check passed. All four batches terminal.
- DRAC existing ControlMaster running, pid79308; no new login or DRAC compute.

## 6. Tests of the Tests
The actual pre-repair engine fails229 assertions. Eight copied artifact
corruptions fail (missing receipt/plan/archive, stale source, bad exit, omitted
test, changed environment/output). Eight paired-summary corruptions fail.
Source pins and source archives, log hashes, complete RDS fit readback and
the realized400-observation data hash checked. Unlazy:3/3 scoped gates pass.
Kernel gate bound before green execution; fit/neighbour criteria were declared
before runs, but their added readback gate was bound afterwards. No claim that
these are independent pre-approved completion-panel verdicts.

## 7a. Issue Ledger
Ordinary NB2 density precision loss and observed-weight overflow repaired at
the tested scope. Required runner still uses old rtol1e-3; next integrate1e-6
plus fit-health assertions and replay. Student R health/density remains open.

## 8. Consistency Audit
No public API or likelihood parameterization changed. Changelogs explicitly
label local candidate scope. Existing truncated source untouched. All earlier
whole-source evidence is now historical until replayed. Independent Rose and
numerical review NOT RUN; full suite and Documenter rendering NOT RUN.

## 9. What Did Not Go Smoothly
First gate text lacked an explicit ID and was rejected; corrected before
approval. A lookup used a nonexistent docs/src/families.md, then located actual
parity/changelog pages. Neither issue affected fits or evidence. Related NB1
and two-part expressions need their own consistent derivative investigation.

## 10. Known Residuals
NB1, HurdleNB and ZINB retain probability-conversion expressions. No claim of
repair for those families. Required-runner replay, full package checks,
recovery/coverage, bridge qualification and independent review remain unpaid.
All protected Cursor/Claude, R0.7.1 and article lanes untouched.

## 11. Team Learning
Test a stable density's derivatives and the fitted target: scalar agreement
alone did not establish fit health. Here both improved with unchanged controls.
Parent implementation and verification only; no new child or panel dispatched.

## 12. Cross-Product Coverage
This does NOT cover complete Core+AGHQ parity, well-identified dispersion at
the Poisson boundary, full covariance/multinomial support, interval coverage,
benchmark claims, release readiness or documentation rendering. No push,
merge, release, destructive cleanup or R engine edit.
