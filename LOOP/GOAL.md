# GOAL — gllvmjl-parity-beyond (IMMUTABLE — re-read at the top of EVERY arc)

Read this first, every cycle. Auto-compact eats messages, not this file.
Unsure after a compaction? Re-read THIS, then `checkpoint.md`, then continue.

**G0 is approved (2026-08-18).** This **is** a running `/goal`.
Lane: `cursor/lane-parity-beyond-20260818` at
`~/local-scratch/lanes/GLLVM.jl-parity-beyond-20260818`.
Do **not** abort the in-flight A4(3) affordability `/goal` on
`cursor/lane-aghq-a43-afford-20260818`. Do **not** start Phase E until that
lane STOPs. Phase P is file-disjoint from `aghq_grid.jl`.

Ledger pin (do not invent; do not use stale 2026-08-16 surface lists as
current): `origin/main` **`3d5acba0`**. Capability table
**56 implemented · 0 partial · 13 planned · 4 missing · 7 rejected**
(80 cells). Refresh the pin with `git fetch` + a re-count of
`docs/design/capability-status.md` before any promote.

## Mission

**Solo platform:** whoever runs this `/goal` (read
`session_ownership.sh`; do not invent Codex). **Lane:** this bounded
worktree off `origin/main` — never the Dropbox checkout, never the
honesty/`a43` LOOP, never mid-arc on `aghq_grid.jl`.

Make GLLVM.jl **as capable as `gllvmTMB` on the twin-admitted surface,
and faster on work the public README does not already claim.** The unit
of progress is a **ledger cell moved on evidence**, or an honest written
fence that the cell stays `missing` / `planned` / `rejected`.

Finish line: the four `missing` cells are either promoted on engine+test
(or a sized coverage certificate) **or** still `missing` with a Rose
fence that names why; the 13 `planned` covariance/special rows named
below are either promoted or still `planned` with an after-task that
says what blocked them; Tweedie `fit_gllvm` is still STOP unless T2–T5+T6
are paid **and** a later G0 admits the surface; both AGHQ rows stay
`missing` until a real estimator **and** a real public knob exist.

## Headline

Close the **only missing twin family** (`multinomial` / categorical) and
the **owed twin-family bridges** that the live ledger still names, while
**repairing Tweedie** (T2–T5+T6) without opening `fit_gllvm`. Then, after
the in-flight affordability `/goal`, serialize remaining AGHQ engine
chips **2 → 3 → 4**; **5 waits**. Covariance grammar is file-disjoint.
Coverage is a Totoro/DRAC campaign, not a Mac-light code arc.

## Anchor (quote the ledger — do not invent a twin Δ)

**Missing (the real holes) — `docs/design/capability-status.md` at the pin:**

| Cell | Status | What this programme may do |
|---|---|---|
| `multinomial` / categorical | `missing` | **IN (Phase P).** Only missing twin family (twin `.valid_family` id 16). Identity → likelihood → engine → test. Not a surface admit. |
| Simulation-validated coverage certificate (broad grid) | `missing` | **Phase X only** if Shinichi sizes+asks Totoro/DRAC. Not a laptop-N certificate. |
| `AGHQ estimator` | `missing` | **Phase E.** Stay `missing` until a real estimator **and** a real knob. No stub `aghq=`. |
| `Broad AGHQ (Julia)` | `missing` | Same lock. Do not promote on k=1 ≡ Laplace or on VA `_gauss_hermite`. |

**Planned (13) — Phase C / later; do not treat as already paid:**
`none × dep`; `phylo_dep`; `animal_dep`; `animal_latent`; `spatial_dep`;
`kernel_indep`; `kernel_dep`; `kernel_latent`; keyworded random slopes;
uncorrelated double-bar slope; `mi()`; mixed-family response; bridge
phylo/animal/spatial/kernel source parity.

Twin `scalar()` is **not even a planned row**. A Rose ledger-row chip
may add it; do not invent the row as if it existed.

**Quoted light-Δ cells already on the ledger (the only Δ numbers this
GOAL may repeat):** named-route **63/63**; shared-X Gauss/Bin/Pois
**18/18**; NB2+X/Beta+X **16/16**; Gamma+X **Δ≈3e-8**; Ordinal+X
**Δ≈5e-9**; NB1+X abs **Δ≈1.53e-9** (seed=48); Poisson species-XB
**Δ≈4e-9**; BetaBinomial+X abs **Δ≈1.50e-8** (seed=49). Do **not**
invent an AGHQ, Tweedie, ZIP/ZINB/ZIB, hurdle, ordered-beta, COM-Poisson,
or censored_poisson Δ.

**Already on the ledger / README — do not re-claim as unpaid “beyond”:**
ZIP/ZINB/ZIB; hurdle / ordered-beta; COM-Poisson; censored_poisson
(Δ **forbidden**); VA/ELBO; public README **10–100×** faster. Do **not**
quote a 340× Gaussian headline in this GOAL (not in README / capability
table). Gaussian closed-form speed is paid.

**Leftover chips — named honestly:**

| Chip | Verdict |
|---|---|
| `truncated_nbinom2` Arc1b (per-trait `log_phi_truncnb2`, then bridge; twin fid 11) | **IN (Phase P)** — still OWED on the live note. Shared-`r` Arc1 is paid. |
| `lognormal` light RCall Δ / bridge (twin fid 3) | **IN (Phase P)** — engine+admit paid; Δ and bridge still OWED. Quote a live cell when it exists; do not invent one. |
| Other twin-family bridge debt the live notes still name (`truncated_poisson` fid 10, etc.) | **IN (Phase P)** only where the twin admits the family **and** the live note still says OWED. |
| `REML` `test_reml.jl` | **DEFERRED — already paid.** Row is `implemented` on `src/reml.jl` + `test/test_reml.jl` + bridge `reml=true`. Do not rebuild the test. Remaining unpaid REML (`fit_gaussian_gllvm(reml=true)`, phylo REML) is **out** unless a later G0 names it. Non-Gaussian REML stays `rejected`. |
| Coverage certificate | **DEFERRED** unless Phase X is sized+asked. |
| `multinomial` | **IN (Phase P).** |

## In

**Phase P — parity that is still unpaid (headline of this programme)**

1. **Multinomial / categorical Identity → engine → focused FD/tests.**
 Twin admits id 16. Julia has no likelihood, no marker, no test. Own
 G0 inside the programme before `src/`. Ledger stays `missing` until
 engine+test exist.
2. **Owed twin-family bridges / light Δ** only where the live note still
 names them **and** the twin admits the family: at minimum
 `truncated_nbinom2` Arc1b, `lognormal` bridge+Δ, other fid-admitted
 OWED bridges on the live pin. Light RCall only when twin admits.
 Quote the cell; never invent.
3. **Tweedie engine repair (T2–T5 + T6), not a `fit_gllvm` admit.**
 Power pin (T2a) or an explicit T2b deferral; `power_init` unification
 (T3); per-trait `φ` coerce + stated shared-power gap (T4); public
 `Tweedie` name/ctors without exporting the inert two-field trap (T5);
 stop start-pinned / sentinel / `converged=true` lies (T6). Identity
 `docs/dev-log/decisions/2026-08-16-tweedie-fit-gllvm-identity.md`.
 The surface stays shut until every unpaid T-gate is paid **and** a
 later G0 opens the admit.

**Phase E — AGHQ, after the in-flight affordability `/goal` STOPs**

4. Serialize remaining AGHQ engine chips **2 → 3 → 4** on one lane.
 Identity `docs/dev-log/decisions/2026-08-17-aghq-identity.md` §A3–A4.
 Current A4(3) affordability (`k^d` / `d ≤ 5`) is **someone else’s
 `/goal`** — wait, do not rebase onto an open `aghq_grid.jl` PR, do
 not 7-way parallel `src/` on that file.
5. **A4(5) waits** until 2 → 3 → 4 have landed. Report honesty
 (`used`, `k`, engine label) so AIC/print cannot mix Laplace and AGHQ.
 No public `aghq=` / `method="AGHQ"` until a real estimator **and**
 this honesty exist (arcs **4 then 5**). Both AGHQ rows stay `missing`.
 VA `_gauss_hermite` is not AGHQ. Do not port TMB `.aghq_gate` or
 `aghq_ridge = "auto"`.

**Phase C — covariance grammar, file-disjoint from `aghq_grid.jl`**

6. Walk the 13 `planned` rows in cheap-first order. Default first
 covariance chip after E’s 2 → 3 → 4 (or earlier only if G0 names a
 file-disjoint slice and Shannon shows no overlap): **`none × dep()`**
 (unstructured trait covariance, no LV). Then phylo/animal/spatial
 `dep`, `animal_latent`, kernel trio, slopes, `mi()`, mixed-family,
 bridge source parity — each its own STOP. `scalar()` = Rose row
 first, not a silent grid insert.

**Phase X — coverage (gated)**

7. Broad-grid simulation-validated coverage certificate **only** if
 Shinichi sizes+asks. Machine choice from
 `~/shinichi-brain/projects/COMPUTE-PLAYBOOK.md`: Mac-light smoke
 locally; Totoro for ≤150-core CPU now; DRAC SLURM arrays for
 replicated multi-seed. A laptop N is not the certificate.

**Beyond (specific; not vibes)**

8. **Speed.** Public claim remains README **10–100×**. Beyond = make
 **non-Gaussian structured-dependence** paths fast enough to measure
 (sparse phylo / SPDE / animal at p the laptop cannot honestly time),
 including the already-planned Takahashi O(p) selected-inverse on
 `sparse_phy_grad.jl` when a G0 names it. Do not re-sell Gaussian
 closed-form speed.
9. **Scalable non-Gaussian structured dependence** — planned
 `*_dep` / kernel / animal_latent cells with engine+test, not a
 grammar rename.
10. **Two-part remaining surfaces** that the live notes still mark OWED
 (+X / `disp_group` / row effects / bridge / CI-under-X). Those
 *families* are already `implemented` — do not re-claim the family
 row. Pay the named surface, or leave it OWED in the after-task.
11. **Public AGHQ** only after Phase E arcs 4 then 5. Then, and only
 then, a later G0 may discuss promoting `AGHQ estimator`. Broad AGHQ
 stays `missing` until the Stage-1a eligibility fence is actually
 widened.

## Out

- Aborting or hijacking the in-flight A4(3) affordability `/goal`.
- Scaffolding this programme, merging, or pushing before G0.
- Tweedie `fit_gllvm` / `@formula` / bridge **admit** until T2–T5+T6
 are paid and a later G0 opens it.
- Stub `aghq=` / `method="AGHQ"` that only errors (§A3).
- Promoting either AGHQ row on a grid, a k=1 golden, or a renamed VA GH.
- Invented twin light Δ (ZIP/ZINB/ZIB, hurdle, ordered-beta, COM-Poisson,
 censored_poisson, AGHQ, Tweedie, any Julia-forward family).
- TMB `.aghq_gate` port; `aghq_ridge = "auto"`; twin Laplace k=1 skip
 as a capability claim.
- 7-way parallel `src/` on `aghq_grid.jl`.
- Phylo Model A public `lv` intervals (`rejected`).
- Non-Gaussian REML (`rejected`).
- Rebuilding `test_reml.jl` / re-promoting Gaussian REML.
- Re-admitting no-X surfaces already on `3d5acba0` (ZIB, student,
 COM-Poisson, delta_*, hurdle_*, beta-hurdle, ordered-beta, NB1,
 BetaBinom, …).
- `scalar()` as if the Julia grid already had a `planned` row.
- Coverage / ADEMP on a laptop presented as the certificate.
- Full-family R↔Julia parity claim (`rejected`).
- Engine surgery on R `gllvmTMB`. Writes on the Dropbox checkout
 (`claude/jl-bridge-capabilities-20260619`, PROTECTED).
- `git add -A` / `git add .`. Merge from the agent. Push without an
 explicit Shinichi instruction.
- Two programme lanes on this repo at once.

## Invariants

- **One programme lane when executing.** Shannon pre-flight first.
 Foreign lane on `aghq_grid.jl` → wait or take a file-disjoint Phase P/C
 chip; genuine overlap is Shinichi’s call (D-87/D-88), never unilateral.
- **Workspace:** new `~/local-scratch/lanes/…` worktree from
 `origin/main` after affordability STOP. Dropbox checkout PROTECTED.
- **Mac-light locally.** Focused suites + FD. Full suite = GitHub CI
 (`Pkg.test()`, Aqua/JET). Do not claim Workflow-Q from a laptop run.
- **Never `git add -A`.** Stage by explicit path.
- **Never merge from the agent.** PR-ready STOP; Shinichi merges on
 full Julia + Documenter SUCCESS (never `--auto`).
- **Compute:** ask “Totoro or DRAC?” before any heavy sim; follow
 COMPUTE-PLAYBOOK. Local ≤10 cores for smokes.
- **No silent rtol widen.** If a test breaks, fix the cause.
- **Identity locks bind:** AGHQ §A3 no stub knob; Tweedie STOP;
 both AGHQ rows `missing` until estimator+knob.
- **Pause at OPEN GATES:** merge/push, public claim, ledger promote,
 Totoro/DRAC spend, reopening a frozen decision, any new `fit_gllvm`
 admit.

## Authoritative WHAT

This file is the mission fence. Binding detail:

1. `LOOP/ultra-plan.md` (frozen at G0 approval 2026-08-18).
2. Live `docs/design/capability-status.md` (re-count at the then-tip;
 do not freeze the 2026-08-16 gap sheet’s surface lists).
3. Identities: AGHQ `docs/dev-log/decisions/2026-08-17-aghq-identity.md`;
 Tweedie `docs/dev-log/decisions/2026-08-16-tweedie-fit-gllvm-identity.md`;
 multinomial `docs/dev-log/decisions/2026-08-18-multinomial-identity.md`.
4. Twin files are **read-only** pins (`R/enum.R` `.valid_family`,
 `R/aghq-*.R`, `R/cv-*.R`). No invented Δ.

Detail in (1)–(3) wins on *how*. This file wins on *what must never
be lost*.

## Definition of done (checkable — not vibes)

The programme is done only when **all** of the following are true.
Each line is a file, a ledger token, or a pasted tally — not a mood.

1. **In-flight affordability `/goal` has STOP’d** (its own checkpoint
 says DONE or NEXT-GATE). This programme did not abort it.
2. **Phase P**
 - `multinomial` / categorical: Identity ACCEPTED + engine under
 `src/` + focused test wired into `runtests.jl` + FD ≤ 1e-6 +
 ledger token `implemented` **or** still `missing` with a written
 blocker (not a stub marker).
 - Every Phase-P OWED twin-family chip (`truncated_nbinom2` Arc1b,
 `lognormal` bridge+Δ, other live-note OWED fid-admitted bridges)
 is either landed with a **quoted** live cell or still OWED in the
 after-task with the exact note path.
 - Tweedie: T2–T5+T6 paid in decisions + tests; `fit_gllvm` admit
 still **absent** unless a later G0 opened it after those gates.
 No Tweedie Δ invented.
3. **Phase E**
 - AGHQ chips **2 → 3 → 4** landed after affordability, sequential,
 one lane; `rg -n` shows no 7-way concurrent writers on
 `src/families/aghq_grid.jl`.
 - A4(5) either landed (report honesty: `used` / `k` / engine label)
 or still **waiting** with that wait named in `checkpoint.md`.
 - Both AGHQ ledger rows still `missing` unless a real public knob
 exists that can evaluate AGHQ (not an error-only stub).
 - No twin AGHQ Δ exists in any after-task unless a Julia AGHQ
 engine produced it.
4. **Phase C**
 - `none × dep()` is `implemented` (engine+test) **or** still
 `planned` with a named blocker.
 - Each other planned row this G0 included is promoted on evidence
 or still `planned` with a one-line OWED in the after-task.
 - `scalar()` is either a new honest `planned`/`implemented` row
 from a Rose pass, or still absent from the grid (not silently
 inserted).
5. **Phase X**
 - Coverage cell is still `missing`, **or** a Totoro/DRAC-sized
 campaign produced a certificate artifact + ledger promote.
 A local smoke does not flip the cell.
6. **Hygiene on every landed arc:** after-task under
 `docs/dev-log/after-task/YYYY-MM-DD-*.md`; `check-log.md` entry;
 board pointer; Rose verdict (OK or named blockers); no
 `git add -A`; Dropbox checkout untouched.
7. **Rose programme fence:** no full-family parity claim; no 340×
 quote; README speed remains **10–100×** unless a new measured
 campaign (apples-to-apples, COMPUTE-PLAYBOOK) is filed; CI green
 on each merge Shinichi performs.

Checkpoint RESUME says **PROGRAMME DONE** (or the next OPEN GATE).
