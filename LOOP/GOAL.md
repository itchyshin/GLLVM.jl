# GOAL — overnight-a43-20260817 (IMMUTABLE — re-read at the top of EVERY arc)

Read this first, every cycle. Auto-compact eats messages, not this file.

## Mission

A4(3) Identity-adjacent lock plus tests that `_aghq_stage1a_reject_extra` fail-loud is the gate; optional cheap `k^d` / `d` bound (`d≤5` analogue of twin `tw≤4`). One PR, Mac-light + GitHub CI. Finish line is PR-ready STOP on this branch — not a push/merge from this worktree.

## Headline

A4(3) Identity-adjacent lock + tests that `_aghq_stage1a_reject_extra` fail-loud is the gate; optional cheap k^d / d bound (d≤5 analogue of twin tw≤4). 1 PR, Mac-light + GitHub CI.

## In

- Why Julia throws (no public knob) while the twin warns+Laplace; name TMB hard-exclusions we do not carry.
- A4(4)/A4(5) “not closed / why” paragraphs in the SAME decision note only (not engines).

## Out

- A4(4) adaptation loop as engine (Julia already re-solves mode+Cholesky every eval — a fake loop is pad).
- A4(5) report honesty as surface (needs public aghq=; A3 still forbids a stub knob).
- leftover-1 none × dep() · CV · Tweedie · multinomial · ledger promote · Totoro/DRAC.
- Do NOT port TMB `.aghq_gate` min-fill/spHess. Do NOT add public `aghq=`. Ledger AGHQ rows stay `missing`.

## Invariants

- One lane: `claude/lane-overnight-a43-20260817` at `~/local-scratch/lanes/GLLVM.jl-overnight-a43-20260817`.
- Never Dropbox checkout `claude/jl-bridge-capabilities-20260619`.
- Never `git add -A` or `git add .`.
- Never push or merge from this worktree (`lane_launch` DENIES). OPEN GATE = sibling push/PR.
- Do not resume or mutate closed `LOOP/GOAL.md` on `claude/lane-aghq-stage1b` (checkpoint `21e24e97`).
- Do not start A4(4) engine, A4(5) surface, or leftover-1 `none × dep()`.
- No new fitter. No public `aghq=`. Ledger AGHQ rows stay `missing`.

## Authoritative WHAT

Shinichi’s pasted GOAL (this file) wins over any A4(4) kernel remaining in `LOOP/ultra-plan.md`. Hopper pin: twin `R/aghq-gate.R`; Julia `src/families/aghq_grid.jl` `_aghq_stage1a_reject_extra`. Identity `docs/dev-log/decisions/2026-08-17-aghq-identity.md` §A4(3).

## Definition of done

- [ ] Decision note `docs/dev-log/decisions/2026-08-18-aghq-a43-gate.md` (docs sibling).
- [ ] Tests that `_aghq_stage1a_reject_extra` covers Stage-1a fences at k>1; optional `k^d`/`d` cost bound (test sibling).
- [ ] Mac-light tallies for `test/test_aghq_adapt.jl` and `test/test_aghq_grid.jl` (and any new test file).
- [ ] check-log + after-task; Rose fence: not a TMB gate port; not an estimator; rows stay `missing`.
- [ ] Named-path commits on this branch. STOP at push/PR (OPEN GATE = sibling).
