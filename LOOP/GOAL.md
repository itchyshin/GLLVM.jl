# GOAL — none-dep-identity-20260818 (IMMUTABLE — re-read at the top of EVERY arc)

Read this first, every cycle. Auto-compact eats messages, not this file.
Unsure after a compaction? Re-read THIS, then `checkpoint.md`, then continue.

**G0 is already approved** (parity-beyond Phase C item 1). This file is
the pasted GOAL, not a second programme.

## Mission

ACCEPTED docs-only Identity for none × dep() at
`docs/dev-log/decisions/2026-08-18-none-dep-identity.md`. Identity only.
No `src/`. No engine G0. No ledger promote.

## Headline

Lock twin estimand for standalone `dep(0+trait|g)` before any Julia
`dep()` sugar.

## Authoritative WHAT

Parent Phase C item 1; gap sheet planned; twin `R/brms-sugar.R` `dep()`.
Binding detail lives in `LOOP/ultra-plan.md` (one-page pointer, not a
new seven-arc plan). This file wins on what must never be lost.

## Invariants

- Fresh `origin/main` `3d5acba0`
- Never sibling WTs (honesty, afford, parity-beyond, lognormal-bridge, truncnb2)
- Dropbox checkout PROTECTED
- Ledger planned; no `capability-status.md` this run
- STOP sibling push/PR
- Mac-light N/A

## Definition of done

ACCEPTED note + twin cites; no `src/` in diff; check-log; after-task;
named-path; STOP no engine no merge.

## Out

phylo_dep / animal / kernel, AGHQ, Tweedie admit, invented Δ, aborting
#255–259, Dropbox, `git add -A`, merge from agent, second seven-arc plan.
