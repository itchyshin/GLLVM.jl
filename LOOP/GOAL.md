# GOAL — gllvm-jl-catchup-loglik (IMMUTABLE — re-read at the top of EVERY arc)

Read this first, every cycle. Auto-compact eats messages, not this file. Unsure after a compaction?
Re-read THIS, then `checkpoint.md`, then continue.

## Mission

Solo platform: **Cursor**. From GLLVM.jl `origin/main` paired with gllvmTMB `origin/main`, land live light oracle cells comparing marginal logLik / objective vs gllvmTMB (primary), optional light CRAN gllvm. Fix parameterization mismatches that block same-model agreement. Bridge/API only as needed. Fence ADEMP, coverage, Totoro/DRAC, structured-source public claims.

## Headline

First live Gaussian gllvmTMB logLik parity cell green on `origin/main` (validate/replace DRAFT RCall scaffold in `test/parity/`), then one-part family cells only where the model is the same.

## Invariants

- One write lane: worktree `.worktrees/gllvmjl-catchup-loglik-20260801` on branch `catchup/loglik-oracle-20260801` from `origin/main` (tip at plan time `05210eca`). Twin: `/tmp/gllvmtmb-parity-restart-20260801` @ `cee55a07` or gllvmTMB `origin/main`.
- **FORBIDDEN:** coding on stale fork `claude/jl-bridge-capabilities-20260619`; R engine surgery in gllvmTMB; ADEMP; coverage campaigns; Totoro/DRAC; promoting ledger `n_drift=0` as fit parity; silent tolerance widening; `git add -A`; push without explicit instruction.
- Family order (locked): Gaussian → Binomial → Poisson, then #132/#148/#133 before NB2/Beta/Ordinal.
- Align Julia dispersion/cuts toward R public surface; gate those family cells until aligned.
- Fence #129 (and #128) outside this arc.
- NotebookLM search: no.
- Verify = read logLik/objective numbers, not exit codes.
- Platform = Cursor (do not invent Codex as default executor).

## Authoritative WHAT

→ `LOOP/ultra-plan.md` (frozen copy of approved plan). Detail wins there; this file wins on “what must never be lost.”

## Definition of done

1. Live Gaussian logLik cell vs gllvmTMB green with cited numbers on the lane branch.
2. Binomial + Poisson cells green or explicit same-model blocked receipts.
3. #132/#148/#133 fixed with tests **or** paused at OPEN GATE with decision text for Shinichi.
4. #129/#128 remain fenced (receipt only).
5. check-log + after-task + Rose claim fence; Melissa reconcile at meaningful close.
6. No ADEMP/coverage/Totoro-DRAC language introduced as claims.
