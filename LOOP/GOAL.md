# GOAL — GLLVM.jl honest 0.7 R↔Julia **true parity** (IMMUTABLE — re-read at the top of EVERY arc)

**Lane:** `honest-070-true-parity` · **Platform:** Cursor `/goal` armed 2026-09-14 (post-DestB).

## Mission

Drive **earned true parity** with **frozen gllvmTMB 0.7.0** (`b4d5fee64def88bc768dda1f1f77c29b295edd86`) per `docs/dev-log/core070/true-parity-decision-map.md` — **not** harness-only or Arc 0 wrapper parity (`docs/src/gllvmtmb-parity.md`). Close **DestB carry-over OWED** (**#323**, **S4 probe**), then second-order + defect-row work (**T5**, matched-θ, realistic-size / real-data when unblocked).

This programme **does not** ship `0.7.0`, **does not** edit `Project.toml` version, and **does not** claim two-directional qualification (T1).

## Headline

**Close #323 (run or waive) → T5 partial-defect re-binds → second-order receipts (T3 contract) →** optional S4 probe / G11 formal decision — toward an honest **0.7.0 version proposal**, not a bump.

## Prior programme (Destination B — receipts on `main`)

**CLOSED on scope ledger** via **PR #336** @ `8f9e4828` (2026-09-14): G1–G11, FINAL-REVIEW PASS-WITH-CORRECTIONS. Authority: `docs/dev-log/after-task/2026-09-14-destb-final-review-panel.md` · plan: `docs/dev-log/plans/2026-09-14-honest-070-destination-b-ultraplan.md`.

**Carry-over into this goal (not re-done):**

- [ ] **#323** — advisory Frozen R smoke **executed** (Totoro + receipt) **or** maintainer **waive** in writing (`docs/dev-log/decisions/2026-09-14-advisory-frozen-r-smoke-323-pending.md`).
- [ ] **S4 probe** — **only** after **second explicit yes** (recorder `97214679c` on origin; G9 done).
- [ ] **Formal joint decision markdown** (optional promote from G11 stub) — **`Project.toml` unchanged** until separate maintainer act.

## G0 answers (carry + open)

| Q | Topic | Answer |
|---|--------|--------|
| **Q1** (DestB) | B1 closed-as-limit | **Permanent.** `B1-RECOVERY` **NOT AUTHORIZED**. |
| **Q2** (DestB) | S4 recorder + probe | **Push done.** **Probe deferred** until **second explicit yes**. |
| **Q3** (DestB) | Compute host | **Totoro first**; DRAC if sized (D-50; D-139 before spend). |
| **Q323** | Frozen R smoke | **OPEN** — Shinichi: **`run #323`** on Totoro (D-139 ack) **or** **`waive #323`**. |
| **QS4** | S4 public-formula probe | **OPEN** — second yes only (same as Q2 probe). |

## Authoritative WHAT

- **Destination map:** `docs/dev-log/core070/true-parity-decision-map.md`
- **Harness vs true:** `docs/src/gllvmtmb-parity.md` · second-order: `docs/dev-log/core070/second-order-parity-contract.md`
- **Version semantics:** D-183 — `docs/dev-log/decisions/2026-09-13-honest-070-parity-aim.md`
- **Execution tracker:** `LOOP/arcs.md` (DestB arcs done; **#16 T5 next**, #25 #323 execution owed)
- **Resume:** `LOOP/checkpoint.md`

## Definition of done (this programme — high level)

True parity is a **multi-arc programme**; this `/goal` completes tranches, not the entire decision-map destination in one chat. **Slice-complete** when:

- [ ] **#323** dispositioned (receipt **or** documented waive).
- [ ] **T5** — eight `PARTIAL_PARITY_DEFECT` rows re-bound or dispositioned (`LOOP/arcs.md` #16).
- [ ] **Second-order** — progress on contract §7 (SE + fixed-effect `vcov` + Wald endpoints); no promotion without Fisher/Rose receipts.
- [ ] **Matched-θ** blockers (e.g. beta_logit, nb2_log) dispositioned per `second-order-matched-coordinates-2026-09-04.md`.
- [ ] **S4 probe** — receipt **or** remains **HELD** with maintainer no.
- [ ] **Joint 0.7 proposal** — formal decision doc **optional**; **no** `Project.toml` edit in-programme.

**Parallel / blocked (track, do not fake):** realistic-size grid (T4/T22), real-data T7 (gllvmTMB **#1236**), T11 (R lane), full Core070 497-row re-campaign.

## Invariants (never violate)

- **Never bump `Project.toml` `version`.** Stays **`0.3.0`** until maintainer signs off after earned parity — not during this programme.
- **Never run S4 probe** without **second explicit maintainer authorisation**.
- **Never edit gllvmTMB engine** from this lane — coordination/read-only reference only.
- **Never claim two-directional parity** (T1); **never** promote Arc 0 to full family parity without Rose fences.
- **Foreign lanes:** Shannon preflight + `tools/lane_lease.sh` before engine edits (D-87/D-88).
- **Never push / merge / release** without explicit maintainer instruction.
- **Verification:** read the **log** and inspect the **artifact**, not exit code alone.

## STOP fences

| Gate | Rule |
|------|------|
| Version | No `Project.toml` bump, tag, General registration, public “0.7 shipped” wording. |
| S4 probe | No probe until second yes (QS4). |
| #323 | No “smoke passed” claim without Totoro log + artifact; no waive without maintainer text. |
| gllvmTMB engine | No engine surgery from GLLVM.jl lane. |
| Compute | No Totoro/DRAC campaign without D-139 + compute-routing skill. |
| B1-RECOVERY | **NOT AUTHORIZED**. |

## Pre-authorisation

**CONTINUE:** scoped branch/worktree edits; LOOP + `docs/dev-log/`; focused tests; local commits **by path**; graft/brain; `gh` read-only; checkpoint overwrites.

**OPTIONAL:** push feature branch; **draft** PR — never merge without maintainer.

**MUST STOP:** STOP fences; foreign-lane files without ownership call.

## Out of scope (do not drift)

- 0.7.1 oracle re-freeze (T2) before second-order contract landed.
- gllvmTMB T11 API-alignment (R lane).
- Inventing #323 pass or second-order parity from toy harness alone.
