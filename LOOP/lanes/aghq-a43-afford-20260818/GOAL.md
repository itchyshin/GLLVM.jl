# GOAL — aghq-a43-afford-20260818 (IMMUTABLE — re-read at the top of EVERY arc)

> Live afford GOAL for `cursor/lane-aghq-a43-afford-20260818`.
> `LOOP/GOAL.md` on this tree is the overnight A4(3) file from `origin/main`
> — **IMMUTABLE**. Do not edit it.

Read this first, every cycle. Auto-compact eats messages, not this file.

## Mission

A4(3) **affordability**: ship a cheap `k^d` / `d≤5` cost bound on dense loadings-only `z_B` (analogue of twin `tw≤4`, not a treewidth measurement). When the helper ships, delete the `#253` `!isdefined` absence tests. One PR, Mac-light + GitHub CI. Finish line is PR-ready STOP on this branch — not a push/merge from this worktree.

## Headline

Cheap `k^d` / `d≤5` bound on dense loadings-only `z_B`. Helper on `src/families/aghq_grid.jl`. Not TMB min-fill. Not a public `aghq=`. Ledger rows stay `missing`.

## In

- Internal helper (tensor size / latent dimension on dense loadings-only `z_B`). Name it without copying twin `.aghq_gate`.
- Tests that the bound fail-loud at `k>1` for ineligible `k^d` / `d`; `k=1` still ≡ Laplace.
- Delete `#253` `!isdefined(GLLVM, :_aghq_kd_bound)` / `_aghq_d_bound` / `aghq_gate` absence tests when the helper exists.
- Decision addendum: this slice **closes** the A4(3) affordability half. Eligibility (#253 / #255) stays declared-kwargs.
- check-log + after-task. Rose fence: not a TMB gate port; not an estimator; rows stay `missing`.

## Out

- Arc 3 gate wiring (`false` vs omitted kwargs).
- A4(4) site-evaluator / fitter engine (not a TMB `DATA_` / `n_adapt` copy).
- A4(5) print/AIC/`used` / public `aghq=` (Identity §A3 forbids a stub).
- Arcs 6–7 (`none × dep()` Identity, CV Identity).
- TMB `.aghq_gate` / `spHess` / min-fill port.
- Ledger promote of either AGHQ row. Tweedie `fit_gllvm`. Invented twin Δ. Dropbox checkout writes.

## Invariants

- One lane: `cursor/lane-aghq-a43-afford-20260818` in a **fresh** worktree from `origin/main` (after #255 merges, or without editing #255’s four paths until then).
- Never the honesty worktree `GLLVM.jl-a43-honesty-20260818`. Never mutate overnight `LOOP/GOAL.md` (IMMUTABLE, closed `/goal`).
- Never Dropbox checkout `claude/jl-bridge-capabilities-20260619`.
- Never `git add -A` or `git add .`.
- Never `gh pr merge` / `--auto` from this worktree. OPEN GATE = sibling push/PR.
- Do not start arcs 3, 4, 5, 6, or 7 from this GOAL.
- No public `aghq=`. Both AGHQ ledger rows stay `missing`.

## Authoritative WHAT

Shinichi’s pasted GOAL (this file) wins. Cite, do not rewrite: `.worktrees/gllvmjl-aghq-stage1b-20260817/docs/dev-log/plans/2026-08-18-seven-arcs-ultraplan.md` (G0: 2→3→4; 5 waits; 6 and 7 wait). Hopper pin: twin `R/aghq-gate.R`; Julia `src/families/aghq_grid.jl`. Identity `docs/dev-log/decisions/2026-08-17-aghq-identity.md` §A4(3). Eligibility lock `docs/dev-log/decisions/2026-08-18-aghq-a43-gate.md` (#253); honesty PR #255.

## Definition of done

- [ ] Helper on `src/families/aghq_grid.jl`: cheap `k^d` / `d≤5` on dense loadings-only `z_B` (not min-fill / `spHess` / `.aghq_gate`).
- [ ] `test/test_aghq_gate.jl` proves the bound; `#253` `!isdefined` absence tests **deleted**.
- [ ] Mac-light: `test/test_aghq_gate.jl` (neighbours `test/test_aghq_adapt.jl`, `test/test_aghq_grid.jl`). Full suite = GitHub CI.
- [ ] check-log + after-task; Rose: not a TMB port; not an estimator; rows stay `missing`. Do not self-sign Rose.
- [ ] Named-path commits on this branch. STOP at push/PR (OPEN GATE = sibling).
