GOAL: see GOAL.md.   STATE: this `/goal` DONE — A4(2) PR-ready and landed on origin/main as #252. Q1 held. GOAL FINISHED = yes.

ARCS DONE (verified):
- A0 wait/recon #251 — `17857481` is an ancestor of `origin/main` (merge `fc845404`, 2026-08-17). This lane did not merge #251.
- A1 A4(2) Liu–Pierce — implemented on `claude/lane-aghq-stage1b` (`32967ef4` + `c3670cec`); PR #252 MERGED at 2026-08-17T22:48:06Z as `17f4a415e71c96bf99976c2db28e494b10c41bbc`. Mac-light 17/17 (`test_aghq_adapt.jl`) + 70/70 (`test_aghq_grid.jl`); independent review PASS; full CI SUCCESS. k=1 still ≡ Laplace; no public `aghq=`; ledger rows stay `missing`.

ARC IN PROGRESS: none. This `/goal` is closed.

NEXT: leftover-1 `none × dep()` Identity — **new `/goal`, not this run.** Twin `dep()` at `R/brms-sugar.R` ~1653; Julia capability-status `planned`; no `dep` in `src/`. Do not confuse with slope Σ_b. Do **not** start A4(3). Do **not** start leftover-2 (CV Identity). Do **not** write Identity decision files from this closed run.

DEFERRED (later `/goal`, Q1/Q2/Q3 held):
1. leftover-1 Identity = `none × dep()` — first leftover after this STOP.
2. leftover-2 CV Identity — after leftover-1. Twin internals `.cv_run` / `.cv_score` (NOT a NAMESPACE `crossval` export).
3. A4(3) structural-gate Identity → A4(4) loop → A4(5) honesty — later AGHQ rungs, not leftovers.

OUT (do not start): Tweedie admit · multinomial · AGHQ public knob · twin Δ · A4(3) from this closed run · coverage/Totoro · #247 · Dropbox checkout.

OPEN GATES (need human): none for this run. Push/PR of A4(2) already happened (#252 merged). Q1 STOP honored — no A4(3), no leftover Identity, no CV Identity in this LOOP.

TRUTH LIVES IN: `origin/main` @ `17f4a415` (merge of #252). Lane branch `claude/lane-aghq-stage1b` @ `c3670cec` plus this closeout commit. Worktree `~/local-scratch/lanes/GLLVM.jl-aghq-stage1b`. After-task `docs/dev-log/after-task/2026-08-17-aghq-stage1b-adapt.md`. Plan `docs/dev-log/plans/2026-08-17-estimator-covariance-ultraplan.md`. Leftover cites: twin `R/brms-sugar.R` ~1653; `docs/design/capability-status.md` (`none × dep` = planned).

RESUME: Do **not** resume this `/goal`. It is finished. If Shinichi wants leftover-1, open a **new** `/goal` on a fresh lane after G0: `none × dep()` Identity only. READ FIRST that new LOOP kit. Do not continue A4(3) from these files. Do not write Identity docs here. Do not touch #247. Do not use the Dropbox checkout.
