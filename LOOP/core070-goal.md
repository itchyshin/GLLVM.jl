# GOAL — core070-parity-beyond (IMMUTABLE for the run — re-read at the top of EVERY arc)

Read this first, every cycle. Auto-compact eats messages, not this file. Unsure after a
compaction? Re-read THIS, then `LOOP/core070-checkpoint.md`, then continue. The old
`LOOP/GOAL.md` (cursor parity-beyond, 2026-08-18) is retained historical evidence only
and is NOT authorization for this programme.

## Mission
Bring GLLVM.jl to a verified, Julia-idiomatic implementation of the approved gllvmTMB
R 0.7.0 Core + callable Stage 1a AGHQ contract, frozen at
`b4d5fee64def88bc768dda1f1f77c29b295edd86`; then demonstrate scoped, measured
performance improvements and produce executed, accurate, attractive Documenter pages.
Delivery target: a verified local integration candidate demonstrating true R–Julia
parity and named beyond-parity capabilities. Milestones: M1 closure → M2 Core + AGHQ
qualification → M3 performance / Documenter / reconciliation.

## Headline
Every required capability-manifest row PASS / PARTIAL / BLOCKED on fresh paired
evidence at the frozen reference — a required missing row prevents any
capability-complete claim.

## Invariants (never violated, even after compaction)
- One lane: this worktree, branch `codex/core070-aghq-20260830`, Claude-operated since
  the 2026-08-31 handover. Never touch the main Dropbox checkout, R 0.7.1
  `gllvmTMB_main`, article lanes, or any foreign Cursor/Claude worktree.
- The frozen R reference is read-only; no engine surgery on gllvmTMB, ever.
- No tolerance widening, convergence-flag weakening, or contract-hash changes. A red
  gate opens a diagnosis, never an edit to the gate.
- Compare invariant estimands (loading crossproducts), never signed loadings.
- Retain every attempt with receipts; classify failures; honest PARTIAL over
  overclaimed PASS; recovery-to-truth before any fitting-capability claim.
- Totoro for bounded qualification (1 Julia/BLAS thread convention); DRAC only via a
  pre-approved sized campaign (D-139); never a DRAC login-node run.
- Estimate before any run; > 30 min needs a pre-run test and approval.

## Authoritative WHAT
`docs/dev-log/plans/2026-08-30-core070-aghq-execution.md` (programme decomposition) +
`docs/dev-log/plans/2026-08-31-core070-reconciliation-plan.md` (reconciliation) +
`LOOP/core070-checkpoint.md` (live state). Detail wins there; this file wins on what
must never be lost. Programme definition:
`docs/dev-log/handover/2026-08-31-claude-handover.md`.

## Definition of done
M1: preservation census closed; parity harness and the known phylogenetic, Tweedie and
Student-t failures repaired or classified; full suite green on this lane;
Melissa/Noether/Rose milestone verdicts recorded. M2: every required manifest row
(covariance sources/modes, formula + modifier grammar, latent/structured multinomial,
data + fitted-object surfaces, separate R bridge, public Stage 1a AGHQ)
PASS/PARTIAL/BLOCKED with fresh evidence. M3: benchmarks on correctness-qualified
models with measured-bottleneck optimizations; executed Documenter tutorials inspected
desktop + mobile; lane dispositions refreshed; Mission Control updated and read back;
independent Melissa/domain/Rose verdicts. Then STOP — merge/release is Shinichi's gate.

## Pre-authorisation (G0 envelope)
Scoped edits in this worktree; local Julia/R/python runs; the retained targeted-run
supervisor; Totoro replays via the existing ControlMaster socket (1-thread convention);
local commits; checkpoints; the frozen strict verifiers and contract tests.

## Must stop for
Push, merge, release, or any public claim; DRAC campaigns (sized pre-run plan first);
contract or tolerance changes; destructive work or any write outside this worktree;
edits to protected lanes; new compute beyond a stated ≤ 30-min estimate; any genuine
surprise that invalidates the approved plan (back to G0, not improvisation).
