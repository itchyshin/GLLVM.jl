# Arcs — Destination B frozen-0.7.0 G1 static audit

| # | arc | status | gate? |
|---|-----|--------|-------|
| 1 | Scaffold `LOOP/` at worktree root (GOAL, arcs, checkpoint, ultra-plan copy) | done | — |
| 2 | B1 domain: graft-first orientation on grouping call paths + protocol-pair disambiguation + HOLD-untouched confirmation | done | — |
| 3 | S3b domain: graft-first orientation on `phylo_rr` adapter contract + pilot-receipt inventory + flag fit-risk tests unexecuted | done | — |
| 4 | S4 domain: graft-first orientation on public-formula validator + recorder-absence confirmation + flag one over-reaching test | done | — |
| 5 | Integrate: write `docs/dev-log/2026-09-13-destination-b-g1-audit-matrix.md` (row-by-row DONE/OWED/RETRACTED/PROTECTED) | done | — |
| 6 | Update Unlazy leaf notes/gate comments for G1 progress WITHOUT approving numerical CHECKs | done | — |
| 7 | check-log entry for G1 closure | doing | — |
| 8 | after-task report for G1 | doing | — |
| 9 | Commit LOOP/ + matrix + check-log + after-task, staged by name | doing | — |
| 10 | STOP — explicit G2 authorisation ask to Shinichi | doing | **G2 (human gate)** |

Status: todo / doing / done / paused / blocked. `paused` = awaiting Shinichi's
named decision; `blocked` = external dependency. Ordinary repair work stays
`doing`.

## Notes on arcs 2–4 (parallel-scoutable, ran integrated in one session)

Shinichi asked for three parallel read-only scouts (B1/S3b/S4). This session
ran them as three sequential graft-first orientation passes inside one
conductor context rather than three spawned subagents, because the total
read-only surface was small enough (a dozen graft queries + a handful of
peeked files) that spawning would have cost more than it saved. The output
shape is identical to what three scouts would have returned: bullet matrix
rows with file-path citations, integrated into one matrix doc. No numerical
work occurred in any domain.
