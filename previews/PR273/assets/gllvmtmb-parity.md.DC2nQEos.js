import{_ as t,o as a,c as i,ao as o}from"./chunks/framework.BXAjcRPS.js";const p=JSON.parse('{"title":"Capability parity with gllvmTMB","description":"","frontmatter":{},"headers":[],"relativePath":"gllvmtmb-parity.md","filePath":"gllvmtmb-parity.md","lastUpdated":null}'),r={name:"gllvmtmb-parity.md"};function d(s,e,n,l,c,g){return a(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="Capability-parity-with-gllvmTMB" tabindex="-1">Capability parity with gllvmTMB <a class="header-anchor" href="#Capability-parity-with-gllvmTMB" aria-label="Permalink to &quot;Capability parity with gllvmTMB {#Capability-parity-with-gllvmTMB}&quot;">​</a></h1><p>GLLVM.jl is a from-scratch Julia twin of R&#39;s <code>gllvmTMB</code>, built for fitting speed at moderate-to-large species counts while reproducing point estimates and likelihoods to <strong>at least six significant digits</strong> on the shared Gaussian + phylogenetic path (worst case across the benchmark grid: <code>|Δ logLik| = 2.343e-07</code>, <code>Σ_y</code> relative Frobenius <code>4.424e-05</code>). This</p><div class="tip custom-block"><p class="custom-block-title">Corrected 2026-08-25</p><p>This sentence previously claimed agreement &quot;to machine precision&quot;. Machine precision is ~2.2e-16; the measured worst case is 2.343e-07 — roughly nine orders of magnitude larger. The Benchmarks page (linked below) always reported the honest figure and its gates; this summary did not.</p></div><p>page is the live <strong>catch-up scoreboard</strong> — where GLLVM.jl stands against the <code>gllvmTMB</code> feature set. For <em>speed</em> comparisons see <a href="/GLLVM.jl/previews/PR273/comparison">Comparison</a> and <a href="/GLLVM.jl/previews/PR273/benchmarks">Benchmarks</a>.</p><p>Legend: ✅ available · 🔨 in progress · ⬜ planned · ⚡ GLLVM.jl advantage.</p><h2 id="Response-families" tabindex="-1">Response families <a class="header-anchor" href="#Response-families" aria-label="Permalink to &quot;Response families {#Response-families}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Family</th><th style="text-align:center;">GLLVM.jl</th><th style="text-align:right;">Notes</th></tr></thead><tbody><tr><td style="text-align:right;">Gaussian</td><td style="text-align:center;">✅</td><td style="text-align:right;">closed-form marginal</td></tr><tr><td style="text-align:right;">Binomial (Bernoulli / counts)</td><td style="text-align:center;">✅</td><td style="text-align:right;">logit / probit / cloglog</td></tr><tr><td style="text-align:right;">Beta-binomial</td><td style="text-align:center;">✅</td><td style="text-align:right;">overdispersed binomial; <code>fit_beta_binomial_gllvm</code>; <code>BetaBinomial(N, μφ, (1−μ)φ)</code>, precision <code>φ</code> matches gllvm family 15; <code>φ→∞ ⇒ Binomial</code></td></tr><tr><td style="text-align:right;">Poisson</td><td style="text-align:center;">✅</td><td style="text-align:right;">log link</td></tr><tr><td style="text-align:right;">Negative binomial (NB2)</td><td style="text-align:center;">✅</td><td style="text-align:right;">size <code>r</code> jointly estimated; <code>Var = μ + μ²/r</code>. <strong>gllvm uses dispersion <code>φ = 1/r</code></strong> (<code>Var = μ + μ²φ</code>) — see the bridge map below</td></tr><tr><td style="text-align:right;">Negative binomial (NB1)</td><td style="text-align:center;">✅</td><td style="text-align:right;">linear variance <code>Var = μ(1+φ)</code>; matches gllvm <code>negative.binomial1</code> (same <code>φ</code>)</td></tr><tr><td style="text-align:right;">Beta</td><td style="text-align:center;">✅</td><td style="text-align:right;">precision <code>φ</code> (matches gllvm)</td></tr><tr><td style="text-align:right;">Ordinal (cumulative)</td><td style="text-align:center;">✅</td><td style="text-align:right;">logit + probit links (<code>link=ProbitLink()</code> matches gllvm&#39;s default cumulative-probit); <code>P(y≤c)=F(τ_c−η)</code> convention verified == gllvm; <code>fit_ordinal_gllvm()</code> keeps the shared-cutpoint Julia route, while <code>fit_ordinal_gllvm_pertrait()</code> and the R bridge use trait-specific cutpoints for native <code>gllvmTMB</code> parity</td></tr><tr><td style="text-align:right;">Gamma</td><td style="text-align:center;">✅</td><td style="text-align:right;">shape <code>α</code></td></tr><tr><td style="text-align:right;">Delta-lognormal</td><td style="text-align:center;">✅</td><td style="text-align:right;">first two-part family; shared 2-block Laplace substrate</td></tr><tr><td style="text-align:right;">Delta-Gamma</td><td style="text-align:center;">✅</td><td style="text-align:right;">occurrence Bernoulli × positive Gamma (log-link mean) on the substrate</td></tr><tr><td style="text-align:right;">Hurdle (Poisson / NB)</td><td style="text-align:center;">✅</td><td style="text-align:right;">occurrence Bernoulli × zero-truncated Poisson / NB2; <code>fit_gllvm(Y; family = HurdlePoisson())</code> / <code>HurdleNB()</code> (marker <code>r</code> is a tag payload). Julia-forward — twin has no hurdle family</td></tr><tr><td style="text-align:right;">Zero-inflated (ZIP / ZINB / ZIB)</td><td style="text-align:center;">✅</td><td style="text-align:right;">structural zero × Poisson / NB2 / Binomial; zero-inflation intercept-only (Λ_z = 0) so the coupled-zero cross-term drops out</td></tr><tr><td style="text-align:right;">Ordered-beta</td><td style="text-align:center;">✅</td><td style="text-align:right;">proportions / cover with point masses at 0 and 1; <code>fit_gllvm(Y; family = OrderedBeta())</code> (marker <code>c0</code>, <code>c1</code>, <code>φ</code> are tag payloads). Julia-forward — twin has no ordered-beta family</td></tr><tr><td style="text-align:right;">Beta-hurdle</td><td style="text-align:center;">✅</td><td style="text-align:right;">occurrence Bernoulli × positive Beta; <code>fit_gllvm(Y; family = BetaHurdle())</code> (marker <code>φ</code> is a tag payload). Julia-forward — twin has no beta-hurdle family</td></tr><tr><td style="text-align:right;">Exponential</td><td style="text-align:center;">✅</td><td style="text-align:right;">positive continuous, <code>Var = μ²</code> (Gamma with shape α=1)</td></tr><tr><td style="text-align:right;">Tweedie</td><td style="text-align:center;">✅</td><td style="text-align:right;">compound Poisson–Gamma (1&lt;p&lt;2); <code>fit_tweedie_gllvm</code>, Dunn–Smyth density series</td></tr><tr><td style="text-align:right;">Conway–Maxwell–Poisson</td><td style="text-align:center;">✅ ⚡</td><td style="text-align:right;">under- or over-dispersed counts; <code>fit_gllvm(Y; family = COMPoisson())</code>; marker <code>ν</code> is a tag payload (always estimated). Julia-forward — twin has no CMP family</td></tr></tbody></table><h2 id="Model-structure" tabindex="-1">Model structure <a class="header-anchor" href="#Model-structure" aria-label="Permalink to &quot;Model structure {#Model-structure}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:center;">GLLVM.jl</th><th style="text-align:right;">Notes</th></tr></thead><tbody><tr><td style="text-align:right;">Latent-variable ordination (loadings)</td><td style="text-align:center;">✅</td><td style="text-align:right;">any <code>K</code>; canonical SVD rotation</td></tr><tr><td style="text-align:right;">Fixed-effect covariates (<code>Xβ</code>)</td><td style="text-align:center;">✅ Gaussian · ✅ non-Gaussian (GLM families)</td><td style="text-align:right;">Shared site-X: Poisson/Binomial via <code>fit_gllvm_cov</code>; NB2/NB1/Beta/Gamma public/bridge default via <code>fit_*_gllvm_grouped_cov</code> (per-trait φ/α + shared <code>γ</code>; twin API B; NB1 = <code>fit_nb1_gllvm_grouped_cov</code>); Ordinal via <code>fit_ordinal_gllvm_pertrait_cov</code> (per-trait cutpoints τ₁=0 / K−2 + shared <code>γ</code>; light RCall vs <code>ordinal_probit</code>). Shared-dispersion + X remains <code>fit_gllvm_cov</code> where that path exists (incl. shared-φ NB1 opt-in). Gaussian <code>β_fixed</code> / non-Gaussian <code>γ_fixed</code> zero masks supported.</td></tr><tr><td style="text-align:right;">Between / within (multilevel)</td><td style="text-align:center;">✅ Gaussian</td><td style="text-align:right;"><code>K_W</code> + per-trait diagonal</td></tr><tr><td style="text-align:right;">Phylogenetic random effect</td><td style="text-align:center;">✅ ⚡</td><td style="text-align:right;">fast <strong>O(p)</strong> sparse path, benchmarked to p = 10⁴</td></tr><tr><td style="text-align:right;">Animal model (relatedness / GRM)</td><td style="text-align:center;">✅ Gaussian</td><td style="text-align:right;"><code>relatedness_cov</code>, via the <code>Σ_phy</code> input</td></tr><tr><td style="text-align:right;">Spatial (Matérn / exponential)</td><td style="text-align:center;">✅ Gaussian</td><td style="text-align:right;"><code>spatial_cov</code>, via the <code>Σ_phy</code> input</td></tr><tr><td style="text-align:right;">Structured dependence × non-Gaussian</td><td style="text-align:center;">✅ phylo · 🔨 spatial-latent / animal</td><td style="text-align:right;">phylogenetic GLM landed (<code>fit_phylo_glm</code>, augmented-state joint Laplace); SPDE / Matérn spatial latent field (<code>fit_spde_latent_gllvm</code>) for the non-Gaussian GLLVM</td></tr><tr><td style="text-align:right;">Random slopes <code>(1 + x | g)</code></td><td style="text-align:center;">🔨</td><td style="text-align:right;">formula front-end (c)</td></tr><tr><td style="text-align:right;">Per-species / grouped dispersion (<code>disp.group</code>)</td><td style="text-align:center;">✅ all 5 dispersion families</td><td style="text-align:right;"><code>fit_{nb,beta,gamma,nb1,tweedie}_gllvm_grouped(Y; K, group)</code> give each species (or group) its own dispersion; reduces exactly to the shared fit at <code>G=1</code>. <strong>gllvm&#39;s default is per-species</strong> dispersion, so for parity route Julia through a grouped fitter with <code>group = 1:p</code> (or set gllvm <code>disp.formula = ~1</code> for the shared model)</td></tr><tr><td style="text-align:right;">Row effects (fixed <strong>and random</strong>)</td><td style="text-align:center;">✅</td><td style="text-align:right;">fixed per-site intercepts (<code>fit_roweffect_gllvm</code>) <strong>and</strong> random <code>ρ_s ~ N(0, σ_row²)</code> (<code>fit_row_random_gllvm</code>, gllvmTMB <code>row.eff=&quot;random&quot;</code>); <code>σ_row→0</code> reduces exactly to no-row-effect</td></tr></tbody></table><h2 id="Post-fit-and-inference" tabindex="-1">Post-fit &amp; inference <a class="header-anchor" href="#Post-fit-and-inference" aria-label="Permalink to &quot;Post-fit &amp; inference {#Post-fit-and-inference}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:center;">GLLVM.jl</th><th style="text-align:right;">Notes</th></tr></thead><tbody><tr><td style="text-align:right;"><code>getLV</code> / <code>getLoadings</code> / <code>rotation</code></td><td style="text-align:center;">✅</td><td style="text-align:right;">all families</td></tr><tr><td style="text-align:right;"><code>predict</code> / <code>fitted</code></td><td style="text-align:center;">✅</td><td style="text-align:right;">all families (ordinal adds <code>:prob</code> / <code>:class</code>)</td></tr><tr><td style="text-align:right;"><code>residuals</code> (Dunn–Smyth + Pearson)</td><td style="text-align:center;">✅</td><td style="text-align:right;">all families</td></tr><tr><td style="text-align:right;"><code>simulate</code> (parametric draw from a fit)</td><td style="text-align:center;">✅ selected non-Gaussian</td><td style="text-align:right;"><code>simulate(fit, n)</code> for scalar-mean GLM-style fits and Tweedie; <code>simulate(fit, X)</code> for covariate fits. Two-part bootstrap CIs use internal samplers, but public <code>simulate</code> methods are not universal.</td></tr><tr><td style="text-align:right;"><code>aic</code> / <code>bic</code> / <code>show</code></td><td style="text-align:center;">✅</td><td style="text-align:right;">all families</td></tr><tr><td style="text-align:right;">Σ_y / communality / correlation / phylo signal H²</td><td style="text-align:center;">✅ Gaussian</td><td style="text-align:right;">report-ready extractors</td></tr><tr><td style="text-align:right;">Confidence intervals (Wald / profile / bootstrap)</td><td style="text-align:center;">✅ scalar/grouped dispersion · 🔨 per-trait ordinal</td><td style="text-align:right;">Gaussian, scalar-dispersion GLM families, grouped-dispersion NB2/NB1/Beta/Gamma, the two-part families, and shared-cutpoint ordinal via <code>confint(fit, Y; method=…)</code>; per-trait ordinal-cutpoint CI endpoints are follow-ups; bootstrap is thread-parallel</td></tr><tr><td style="text-align:right;">Ordination biplot</td><td style="text-align:center;">✅</td><td style="text-align:right;"></td></tr></tbody></table><h2 id="Interface" tabindex="-1">Interface <a class="header-anchor" href="#Interface" aria-label="Permalink to &quot;Interface {#Interface}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:center;">GLLVM.jl</th><th style="text-align:right;">Notes</th></tr></thead><tbody><tr><td style="text-align:right;">Matrix-level fit API</td><td style="text-align:center;">✅</td><td style="text-align:right;"><code>fit_gllvm(Y; family, K, …)</code></td></tr><tr><td style="text-align:right;"><code>@formula</code> front-end</td><td style="text-align:center;">✅ fixed effects (wide + long) · 🔨 rest</td><td style="text-align:right;"><code>gllvm(@formula(y ~ 1 + x), Y, data; …)</code> and <code>gllvm(@formula(y ~ 1 + x), long; species, site, …)</code>; random slopes, <code>traits()</code>/<code>phylo()</code>, categoricals deferred</td></tr><tr><td style="text-align:right;"><code>traits()</code> / <code>phylo()</code> formula terms · random slopes <code>(1+x|g)</code></td><td style="text-align:center;">🔨</td><td style="text-align:right;">custom StatsModels terms + RE substrate (design spec&#39;d)</td></tr></tbody></table><h2 id="Performance-—-the-differentiator" tabindex="-1">Performance — the differentiator <a class="header-anchor" href="#Performance-—-the-differentiator" aria-label="Permalink to &quot;Performance — the differentiator {#Performance-—-the-differentiator}&quot;">​</a></h2><p>⚡ Large per-fit speedups on the <strong>Gaussian closed-form path</strong>, and an O(p) phylogenetic gradient benchmarked to p = 10,000.</p><div class="warning custom-block"><p class="custom-block-title">Read the published grid, not the headline</p><p>The grid published in these docs (<a href="./benchmarks">Benchmarks</a>) measures <strong>161.2×, 185.3×, 194.9×, 335.3×, 398.8×, 698.1×</strong> — median <strong>265.1×</strong>.</p><p>A <code>~340×</code> figure appears elsewhere in this repo, attributed to a &quot;Gaussian + phylogenetic&quot; grid. <strong>That grid is not published here</strong>, so a reader cannot check it against anything in this repository, and it does not match the one grid that is. Treat <code>~340×</code> as unverified in-repo pending publication of its source.</p><p>Agreement is <strong>at least six significant digits</strong>, not machine precision: the measured worst case across the published grid is <code>|Δ logLik| = 2.343e-07</code> and <code>Σ_y</code> relative Frobenius <code>4.424e-05</code>.</p><p>None of this generalises beyond the Gaussian closed-form path. Measured non-Gaussian speedups include zero-truncated Poisson ≈ 2.2× and Gamma ≈ 1.6×. Poisson, NB2, Binomial, and</p></div><p>Beta use analytic Laplace outer gradients by default on plain no-mask/no-offset fits, with finite-difference fallback; Gamma and the remaining finite-difference Laplace paths stay conservative until their analytic gradients clear the runtime accuracy gate. The sparse-Cholesky / CHOLMOD marginals are not generic-AD-friendly; the VA estimator adds analytic inner and envelope-theorem outer gradients for further fit-time gains.</p><h2 id="R-bridge:-parameterization-map" tabindex="-1">R bridge: parameterization map <a class="header-anchor" href="#R-bridge:-parameterization-map" aria-label="Permalink to &quot;R bridge: parameterization map {#R-bridge:-parameterization-map}&quot;">​</a></h2><p>R <code>gllvmTMB</code> can call GLLVM.jl as its default Julia fitting path through the R-side bridge. For results to agree, the bridge must reconcile a few <strong>convention differences</strong> — the underlying models are the same, but the parameter scales/structures differ. These are translation rules for the bridge, not bugs on either side.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Quantity</th><th style="text-align:right;">gllvm (R)</th><th style="text-align:right;">GLLVM.jl</th><th style="text-align:right;">Bridge rule</th></tr></thead><tbody><tr><td style="text-align:right;">NB2 dispersion</td><td style="text-align:right;"><code>φ</code> (dispersion), <code>Var = μ + μ²φ</code>; larger <code>φ</code> ⇒ more overdispersion</td><td style="text-align:right;"><code>r</code> (size), <code>Var = μ + μ²/r</code></td><td style="text-align:right;"><strong><code>r = 1/φ</code></strong> (invert in both directions). Also propagates to ZINB / Hurdle-NB / grouped-NB</td></tr><tr><td style="text-align:right;">NB1 dispersion</td><td style="text-align:right;"><code>φ</code>, <code>Var = μ + μφ</code></td><td style="text-align:right;"><code>φ</code>, <code>Var = μ(1+φ)</code></td><td style="text-align:right;">identity (maps 1:1)</td></tr><tr><td style="text-align:right;">Gamma dispersion</td><td style="text-align:right;"><code>φ</code> = <strong>shape</strong>, <code>Var = μ²/φ</code></td><td style="text-align:right;"><code>α</code> = <strong>shape</strong>, <code>Var = μ²/α</code></td><td style="text-align:right;">relabel <code>α ↔ φ</code> (no inversion)</td></tr><tr><td style="text-align:right;">Beta precision</td><td style="text-align:right;"><code>φ</code>, <code>Var = μ(1-μ)/(1+φ)</code></td><td style="text-align:right;"><code>φ</code> (same)</td><td style="text-align:right;">identity</td></tr><tr><td style="text-align:right;">Tweedie</td><td style="text-align:right;">power <code>ν</code>, <code>Var = φ·μ^ν</code>, default start <code>ν = 1.1</code></td><td style="text-align:right;">power <code>p</code>, <code>Var = φ·μ^p</code>, default start <code>p_init = 1.5</code></td><td style="text-align:right;">identity; set <code>p_init = 1.1</code> to reproduce gllvm&#39;s optimiser path</td></tr><tr><td style="text-align:right;">Gaussian dispersion</td><td style="text-align:right;">per-species SD <code>φ_j</code></td><td style="text-align:right;">single shared <code>σ</code> (profiled)</td><td style="text-align:right;">needs a per-species-variance Gaussian fit for exact parity</td></tr><tr><td style="text-align:right;">Dispersion <strong>structure</strong></td><td style="text-align:right;">per-species by default (<code>disp.formula = NULL</code>)</td><td style="text-align:right;">shared scalar by default; per-species via the grouped fitters</td><td style="text-align:right;">route Julia through <code>fit_*_gllvm_grouped(Y; K, group = 1:p)</code>, <strong>or</strong> set gllvm <code>disp.formula = ~1</code></td></tr><tr><td style="text-align:right;">Estimation method</td><td style="text-align:right;">default <code>method = &quot;VA&quot;</code></td><td style="text-align:right;">default Laplace; VA available via <code>fit_*_gllvm_va</code></td><td style="text-align:right;">pin matching methods; VA and LA differ in finite samples</td></tr></tbody></table><p>Engine-side parity is broader than the current R bridge admission surface. The current <code>gllvmTMB(..., engine = &quot;julia&quot;)</code> bridge admits complete, balanced, one-part reduced-rank models for Gaussian, Poisson, Binomial, NB2, NB1, Beta, Gamma, and Ordinal-probit no-X fits. For NB2, NB1, Beta, and Gamma, the Julia bridge default now routes through per-trait grouped-dispersion fitters (<code>group = 1:p</code>) so the point-fit nuisance structure matches native <code>gllvmTMB</code>/<code>gllvm</code>; grouped-dispersion Wald/profile/bootstrap CI payloads are routed through the same no-X bridge contract. Ordinal and ordinal-probit bridge rows now use per-trait cutpoints by default and return <code>cutpoints</code> as a NaN-padded trait x threshold matrix plus per-trait <code>n_categories</code>, <code>cutpoint_mode = &quot;per_trait&quot;</code>, and <code>cutpoint_link</code>; per-trait ordinal CI endpoints remain unavailable-status rows until a per-trait cutpoint CI engine lands. Fixed-effect covariates (<code>X</code>) are admitted for complete, balanced one-part Gaussian, Poisson, Binomial, NB2, NB1, Beta, and Gamma fits (NB1 via per-trait <code>fit_nb1_gllvm_grouped_cov</code>; light RCall <code>nbinom1</code>+X cell abs Δ ≈1.53e-9 @ rtol 1e-6, seed=48). <code>GLLVM.bridge_capabilities()</code> exposes the current Julia bridge surface as a flat, JuliaCall-friendly ledger so the R side can enforce a one-way drift guard: every R-admitted row must have a Julia route with explicit status metadata, while Julia-only rows must remain explicitly planned or rejected in <code>gllvmTMB</code>. For Gaussian covariate fits the bridge returns <code>mean_coef</code>, the full coefficient vector for the supplied <code>X</code> array, so the R side can reconstruct in-sample fitted values without guessing from the per-trait mean summary. When the R side passes a fixed-zero coefficient mask through <code>options[&quot;coef_fixed&quot;]</code>, the bridge returns the full coefficient vector with constrained entries equal to zero plus <code>mean_coef_status</code> (Gaussian) or <code>gamma_status</code> (non-Gaussian) so the R package can print fixed rows without treating them as estimated parameters. Predictor-informed latent-score covariates (<code>X_lv</code>) are admitted for complete-response ordinary Gaussian, Poisson (log link), shared-dispersion NB2, shared-shape Gamma, shared-precision Beta, binomial logit/probit/cloglog, and native shared-cutpoint Ordinal logit point fits. The Gaussian bridge centres responses by trait means and returns those means as <code>alpha</code>; the Poisson, NB2, Gamma, Beta, and binomial bridges keep per-trait link-scale intercepts in <code>alpha</code> (the NB2, Gamma, and Beta <code>X_lv</code> routes use the shared-dispersion/shape/precision fitter, not the per-trait grouped route). Native shared-cutpoint Ordinal <code>X_lv</code> is Julia-side only for now; it does not promote the per-trait ordinal R bridge. These routes return total latent scores in <code>scores</code> and add <code>scores_mean</code>, <code>scores_innovation</code>, <code>alpha_lv</code>, and rotation-stable <code>lv_effects = Lambda * alpha_lv&#39;</code>. Native GLLVM.jl can compute uncertainty for the ordinary <code>B_lv</code> product, including selected-entry profile-likelihood canaries, but the R bridge still transports only the Wald <code>X_lv</code> payload for promoted rows. Response masks, simultaneous fixed-effect <code>X</code>, mixed-family fits, grouped-dispersion <code>X_lv</code>, bridge profile/bootstrap <code>X_lv</code> intervals, per-trait ordinal <code>X_lv</code>, and two-part <code>X_lv</code> remain deliberate follow-ups rather than inferred parity. Initial response-missing masks are admitted only for no-X one-part non-Gaussian bridge fits through an explicit <code>mask</code> (<code>true = observed</code>); the R bridge live-tests Poisson, Bernoulli Binomial, NB2, NB1, Beta, Gamma, and Ordinal-probit routes end to end. Gaussian response masks remain an explicit follow-up. Ordinal-probit is fit/nobs/mask/link-tested, and the Julia payload carries per-trait cutpoints plus category counts so R-side prediction can be gated explicitly by the paired <code>gllvmTMB</code> branch. NB1 post-fit prediction, residual, augmentation, and conditional simulation are routed for complete-data no-X fits and for masked fits where the fitted means are available; masked simulation and masked CI/profile/bootstrap refits remain rejected with explicit CI-status messages. X+mask fits, ordinal covariate fits, structured covariance terms, and user-selectable Julia-side optimizer controls remain explicit bridge follow-ups, not silently supported cells.</p><p>The mixed-family R bridge is guarded and intentionally limited: complete balanced trait-aligned no-X/no-mask/no-CI Julia-engine point fits are admitted for Gaussian, Poisson, Binomial, NB2, Beta, and Gamma components. The bridge stores row-aligned per-trait <code>families</code> and <code>link</code> labels, validates the native <code>gllvmTMB</code> selector oracle, checks direct-wrapper logLik equality, and routes current in-sample post-fit methods with unavailable-CI status. Mixed-family X, masks, cbind/weights, REML, ordinal/NB1/two-part components, and CI endpoints remain rejected deliberately.</p><p>REML is a Gaussian-only bridge/engine claim in this project. HSquared&#39;s very fast AI-REML work is useful design input for exact Gaussian variance-component cells, but it is not terminology to use for non-Gaussian Laplace GLLVMs. Non-Gaussian speedups should be described as observed-information, Fisher/natural-gradient, reverse-mode, or implicit-Laplace-adjoint work, each gated by reference-gradient, point-estimate, and CI/status evidence.</p><p>The engine still carries additional gllvm/gllvmTMB parity rows that are not all public through the R bridge yet:</p><ul><li><p><strong><code>ZNIB</code></strong> (zero-and-N-inflated binomial) — deferred: the gllvm TMB template&#39;s <code>case ZNIB</code> appears to fall through (missing <code>break;</code>) into beta-binomial, so its likelihood needs upstream confirmation before building to it.</p></li><li><p><strong>corAR1 / corExp / corCS structured row effects, and <code>lvCor</code> correlated latent variables</strong> — these are <code>gllvm</code> features, <strong>not in gllvmTMB</strong>, so they are out of scope for this bridge. (GLLVM.jl does carry more general SPDE/Matérn-spatial and phylogenetic substrates, which gllvm/gllvmTMB lack.)</p></li><li><p><strong>Per-trait nuisance-parameter intervals</strong> — grouped NB2/NB1/Beta/Gamma CIs are routed; grouped Tweedie and per-trait ordinal-cutpoint CI endpoints remain follow-up work.</p></li></ul><h2 id="Honest-gaps" tabindex="-1">Honest gaps <a class="header-anchor" href="#Honest-gaps" aria-label="Permalink to &quot;Honest gaps {#Honest-gaps}&quot;">​</a></h2><h3 id="Laplace-curvature:-Fisher-vs-observed" tabindex="-1">Laplace curvature: Fisher vs observed <a class="header-anchor" href="#Laplace-curvature:-Fisher-vs-observed" aria-label="Permalink to &quot;Laplace curvature: Fisher vs observed {#Laplace-curvature:-Fisher-vs-observed}&quot;">​</a></h3><p><code>gllvmTMB</code> is built on TMB, whose <code>MakeADFun(..., random = ...)</code> differentiates the coded joint negative log-likelihood. Its Laplace log-determinant therefore uses the <strong>observed</strong> joint Hessian, structurally and without ever making a choice about it. GLLVM.jl hand-codes its Laplace kernels, and several of them used the <strong>Fisher (expected)</strong> information in that role instead.</p><p>The two coincide at canonical links — Poisson/log and Binomial/logit, where the curvature is free of <code>y</code> — which is why the launch families were unaffected and why the discrepancy went unnoticed. They differ everywhere else.</p><p><strong>Status, stated plainly rather than as a capability claim:</strong></p><ul><li><p><strong>Fixed and on <code>main</code>:</strong> NB1 (grouped route), <code>truncated_nbinom2</code>, <code>Exponential</code>, <code>DeltaGamma</code>, and <strong><code>Gamma</code></strong> — the last being the one that sat on the public default path <code>fit_gllvm(Y; family = Gamma())</code>.</p></li><li><p><strong>Still using the Fisher weight:</strong> Tweedie, GP-1, and <code>Binomial</code> at the <strong>probit</strong> and <strong>cloglog</strong> links — nothing else. (NB2, Beta, NB1 and Student-t all flipped to observed 2026-08-27 on the curvature-adjudication campaign evidence — decision A; Exponential&#39;s registry default was declared the same day.) Every one of these is reachable, so a log-likelihood from them will not match <code>gllvmTMB</code> to machine precision.</p></li><li><p><strong>Not a uniform improvement.</strong> Against numerical quadrature, the observed curvature is decisively closer for Gamma (12/12 seeds, 20–60× smaller error) and for NB2 (87% of 150 curvature-adjudication campaign cells, 2026-08-27), but <em>not</em> for Beta, and measurably worse for GP-1&#39;s dispersion recovery. So each family is decided on its own evidence. Matching TMB is the goal; &quot;the numbers get better&quot; would be an overstatement.</p></li></ul><p>Where a curvature has been corrected, the previous behaviour stays reachable through <code>hessian = :fisher</code> on the corresponding marginal.</p><h3 id="Other-gaps" tabindex="-1">Other gaps <a class="header-anchor" href="#Other-gaps" aria-label="Permalink to &quot;Other gaps {#Other-gaps}&quot;">​</a></h3><p>The rows above describe engine capabilities and the narrower R bridge admission surface separately. Engine-side work now covers the major response-family rows, fixed-effect covariates for the GLM families, the VA estimator, ordination extractors, SPDE / Matérn spatial latent fields, phylogenetic GLMs, and confidence-interval machinery. Those are not automatically public <code>gllvmTMB(..., engine = &quot;julia&quot;)</code> claims: each bridge row still needs its own R-side admission, parity test, CI-status handling, and documentation.</p><p>The remaining gaps are each scoped by an execution-ready spec in <code>docs/superpowers/specs/</code> (design + slice plan + verifiable goals), so they can be built <em>with</em> validation rather than shipped unverified:</p><ul><li><p><strong>Structured dependence × non-Gaussian (animal / spatial extensions)</strong> — the phylogenetic GLM has landed (<code>fit_phylo_glm</code>, an augmented-state joint Laplace), and the SPDE / Matérn spatial latent field is wired into the non-Gaussian GLLVM (<code>fit_spde_latent_gllvm</code>). The remaining work is the general dense-<code>S_u</code> species random effect <code>u ~ N(0, σ²Σ)</code> shared across sites and the scalable large-<code>p</code> determinant. Spec: <code>2026-05-31-nongaussian-structured-dependence-design.md</code>.</p></li><li><p><strong><code>@formula</code> front-end</strong> — <strong>v1 landed</strong>: <code>gllvm(@formula(y ~ 1 + covariates), Y, data; family, K)</code> for continuous fixed effects over wide data routes to the engine (StatsModels + Tables added). Still deferred (design spec&#39;d in <code>2026-05-31-formula-frontend-random-slopes-design.md</code>): long-format data, the <code>traits()</code>/<code>phylo()</code>/<code>latent()</code> custom terms, categorical covariates, and the headline random slopes <code>(1 + x | g)</code> (which need the new RE engine substrate).</p></li><li><p><strong>R bridge (<code>engine = &quot;julia&quot;</code>)</strong> — in progress through the R package bridge. Complete-data one-part fits, selected fixed-effect-X rows, selected missing-response-mask rows including NB1, scalar-CI transport, and NB1 post-fit methods are admitted only where live R tests cover them. Mixed-family point-fit metadata, grouped-dispersion CI endpoints, NB1-X, masked CIs, structured dependence, and broader post-fit methods remain bridge follow-ups.</p></li></ul>',36)])])}const m=t(r,[["render",d]]);export{p as __pageData,m as default};
